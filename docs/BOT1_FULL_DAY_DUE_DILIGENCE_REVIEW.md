# Bot 1 — Full-Day Due-Diligence Review

First real pass at the due-diligence tier (Group J of the task specification), which had zero
coverage across all three prior Bot 1 passes. Source snapshot for everything below:
`6dbba457f1077393cf0b7882716a6c71dc94bf2f` (real repo `HEAD` at the time this section was written —
see the main report's delta-loop note; it was `8201f17d` at the start of this session's continuation
and moved 4 commits forward before this section was written).

One row per claim actually checked. Absence from this table means not yet checked this pass — see
"Not attempted" at the bottom, not silently implied to be fine.

| # | Claim / area | Evidence | Verdict |
|---|---|---|---|
| J01 | Product scope: "Havenpaw is a dedicated European animal ecosystem, not a general marketplace" | `docs/PRODUCT_VISION.md` (read in full) states this explicitly and lists an 8-item permanent priority hierarchy. Cross-checked against `CLAUDE.md`'s own framing (identical). | Supported by the codebase's own internal docs; consistent framing across 2 documents. |
| J01 | **Roadmap claim: fundraising "policy defined, not yet built"** | `docs/PRODUCT_VISION.md` line 169-171 and `docs/IMPLEMENTATION_PLAN.md` line 376-377 both state fundraising has "no schema/RLS/UI... yet." But: `supabase/migrations/20260101005600_fundraising.sql` creates real `fundraising_campaigns`/`fundraising_contributions` tables with RLS; 7 migrations total touch fundraising; real UI routes exist (`src/routes/_public.fundraising*.tsx`, `dashboard.foundation.fundraising*.tsx`, `dashboard.admin.fundraising.tsx`); a feature flag (`VITE_FUNDRAISING_ENABLED`, `src/lib/fundraising-flag.ts`) gates it; **this exact session's own delta** (Stage FA-1 through FA-4) found and fixed a real self-publish security bug in this "not yet built" feature (§5.1 of the prior audit lineage, confirmed fixed by this pass's own migration read). `docs/IMPLEMENTATION_PLAN.md`'s own body text (lines 190-220, a *different* section of the same file) correctly and specifically describes the real, built state ("What's real": tables, RLS, admin approve/activate/suspend UI) — **the file directly contradicts itself between its own detail section and its own summary section.** | **Documentation-accuracy finding, not a security finding**: two separate high-level docs (and one doc's own summary vs. its own detail section) materially understate how built the fundraising module actually is. For due diligence this cuts against the codebase, not for it — stale docs like this are exactly what an acquirer's technical diligence team would flag as a red flag on documentation hygiene / process discipline, independent of the code being fine. Recommend Bot 2 reconcile `PRODUCT_VISION.md` line 169-171 and `IMPLEMENTATION_PLAN.md` line 376-377 against line 190-220 of the same file. |
| J02 | Capability matrix (partial) | `docs/TECH_DEBT_REGISTER.md` (read in full, but itself dated to an early "Stage CF" snapshot and already shown stale by other checks this pass — e.g. it claims error-translation is "wired into only 2 call sites," contradicted by this pass's own §7.5 re-check finding 34 files) lists real, current gaps as of *its own* writing: no outbound email delivery (org invites are in-app-link only, no provider SDK), no real ownership-transfer business action (`animal_ownership_history` has RLS but nothing ever writes `animals.owner_profile_id` post-creation — a genuine capability gap, not a security bug), no bulk puppy-add UI, no photo/document delete UI, ~127 unindexed FK columns (deliberate, not urgent per the doc's own reasoning), no Storage-specific upload rate limiting (row-level rate limiting exists for Postgres-insert paths only, not the Storage API). | These items are real per the cited evidence but the register itself is confirmed stale in at least one respect (see above) — treat its "still open" claims as directionally right, not to-the-line current, without independently re-checking each one (not done this pass). |
| J03 | Architecture (shallow) | `CLAUDE.md`/`docs/DOMAIN_MODEL.md` pointer confirms: TanStack Start (React 19 SSR) + local Supabase (Postgres 17.6) + Cloudflare Worker deploy target. 142 migrations, 69 test files, RLS-first authorization model (not app-layer). | Standard, coherent, well-documented stack for its stated stage; not independently assessed for maintainability/scaling beyond what's already covered in the findings-based sections of the main report. |
| J06/J20 | Commercial claims / monetisation infrastructure | `grep -in "stripe\|twilio\|sendgrid\|postmark\|resend\|paypal" .env.example .env` → empty in both. No payment-provider SDK in `package.json` dependencies (72 total deps scanned, none is a payment/SMS/email-provider client library). | **No real payment, SMS, or transactional-email provider is integrated anywhere in this codebase.** Any due-diligence or sales claim implying live payment collection, real fundraising money movement, or automated email/SMS delivery is not supported by the current code. Consistent with the codebase's own `TECH_DEBT_REGISTER.md` ("No outbound email delivery... this session is barred from configuring [real provider credentials]"). |
| J08/J09 | IP / dependency surface (shallow) | `package.json` has no top-level `"license"` field (checked via `grep -n '"license"' package.json` → no match). All 72 runtime+dev dependencies are version-range-pinned to registry packages (no git-url or `file:` dependencies found via a scan for entries not starting with `^`/`~`/a digit). `.github/workflows/ci.yml` (117 lines, read in full) runs `npm ci`, `tsc --noEmit`, lint, build, a migration-duplicate-prefix check, and **a dedicated `database-tests` job that provisions Supabase in CI and runs `npm run test:db` twice** (a real, automated, repeat-run idempotency check, not just a claim in a doc). | No unusual/abandoned dependency pattern observed in a visual scan (not a full license-compliance audit — not run this pass). Missing `license` field in `package.json` is a minor IP-hygiene gap worth flagging for an actual acquisition data room (ambiguous default copyright status), not itself evidence of anything wrong. The CI database-tests job is genuine, positive evidence for "test count/pass claims are at least CI-enforced," partially answering the acquisition-blocker note in the finalisation pass's own §5 row 3 about test-suite scale claims never being independently re-run by any Bot 1 pass — **this pass still did not itself re-run the suite** (see main report §10), but confirms an automated process does, on every push to `main`. |
| J13 | New-team takeover (partial) | The same `ci.yml` `database-tests` job is a real, machine-executed rehearsal of "clone, install, stand up Supabase, run migrations, run the test suite twice" on every push — the closest evidence in the repo to an actual new-team-takeover rehearsal, though it is CI's own rehearsal, not a documented human walkthrough. `docs/LOCAL_SETUP.md` (referenced, not fully re-read this pass) is the documented human-facing equivalent. | Directionally positive; not independently executed by this pass (would require running CI or reproducing it manually, not attempted). |
| J14/J15/J16/J18/J19/J21/J22 | Roadmap credibility, KPI framework, analytics taxonomy, product tour, sales material, acquirer persona, valuation readiness | `ls docs \| grep -iE "sales\|market\|kpi\|pricing\|analytic\|roadmap\|valuat\|acqui\|invest\|demo\|tour\|checklist"` → only `DEPLOYMENT_CHECKLIST.md` matches (a technical deployment doc, not a due-diligence checklist). `grep -rliE "testimonial\|customer success\|case study\|paying customer\|revenue\|valuation\|acquirer" --include="*.md" docs/` → **zero matches**. | **None of these documents exist in the codebase at all.** This is itself the finding, not an invented risk: there is no KPI framework, no analytics taxonomy, no sales/marketing material, no valuation document, no acquirer-persona document, no formal product-tour script anywhere in this repository. For a due-diligence data room, this is a real, concrete gap (absence, not a fabricated concern) — whatever exists for these areas, if anything, lives outside this codebase and was not evaluated by this pass (out of scope — this is a code repository audit, not a company-wide data-room audit). |
| J07 | Legal boundaries (partial) | `docs/FUNDRAISING_POLICY.md` (existence and role confirmed, not fully re-read line-by-line this pass) is the one explicit legal/policy document found. `docs/PRODUCT_VISION.md` explicitly defers a "per-country legal knowledge base" to a later phase and lists it as a known, deliberate gap (not hidden). | The codebase itself is honest about not yet having jurisdiction-specific legal coverage beyond Poland/Germany/Netherlands/Belgium initial focus — this is disclosed, not concealed, in the product's own vision document. |

## Not attempted this pass

J04 (security due diligence beyond what the findings-based sections of the main report already
cover), J05 (operational due diligence — jobs/support/moderation/incident-response detail), J10
(formal data-room completeness checklist), J11 (risk-disclosure quality as its own review), J12
(founder/session dependency — a real and plausible risk area for a project built almost entirely by
autonomous agent sessions per `docs/AUTONOMOUS_BACKEND_PROGRESS.md`'s own framing, but not
independently assessed this pass), J17 (demo-environment fictional-data/reset verification), J23
(acquisition checklist evidence table). These remain genuinely open for a future pass.

## Overall due-diligence verdict for this pass's own scope only

No fabricated commercial claim was found in the codebase itself — the codebase does not claim
customers, revenue, or valuation anywhere this pass searched. The one real, load-bearing finding is
the fundraising documentation/reality mismatch above (code is *more* built than two summary
documents claim), which is a documentation-hygiene issue, not a security or commercial-honesty
issue. The larger, structural due-diligence gap is an *absence*: no KPI/analytics/sales/roadmap-
credibility/valuation materials exist in this repository at all, which any real due-diligence
process would need to source from elsewhere or flag as missing.
