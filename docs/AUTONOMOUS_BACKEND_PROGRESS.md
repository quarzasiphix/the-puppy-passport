# Autonomous Backend Progress

Running log for the full-day autonomous backend/operations/launch-hardening session. Kept concise
and current so another session can resume without reconstructing the day's work. See
`docs/BACKEND_TAKEOVER_LOG.md` for the session before this one (transport-domain hardening,
Phases 0–9) and `docs/adr/TRANSPORT_DATA_MODEL.md` for the architecture it produced.

## Starting point

- **Starting HEAD**: `933e1ca` — "Update docs for the completed transport-data-model hardening pass."
- **Baseline** (re-verified at the start of this session): working tree clean, 76 migration files
  (no duplicate numeric prefixes), 170/170 database/API tests passing on two consecutive runs,
  `tsc --noEmit` clean, `npm run build` clean.
- **Correction**: an earlier report said "77 migrations, range 20260101006500–20260101007400" —
  that range is 10 migration versions, and the real filesystem count is 76. Corrected here rather
  than treated as a discrepancy to chase.

## Phases/stages completed this session

(One row per commit, newest last. "Stage" letters match the mega-prompt that superseded the
Phase-10-onward numbering mid-session — same content, renamed.)

| Commit | Stage | Summary |
|---|---|---|
| `f5cb8c7` | B (Phase 10) | Operations calendar: `src/lib/queries/calendar.ts` (unscheduled queue, date-range route listing, deterministic conflict detection), `dashboard.operations.calendar.tsx` real UI (day/week, filters, conflicts banner), `tests/db/calendar-scheduling.test.ts`. 184/184 tests. |
| `fd33235` | C (Phase 11) | Real transport timeline (`getCustomerTimeline`/`getOpsTimeline`/`getDriverTimeline`, `TransportTimeline` component) sourced only from `transport_status_history`/`transport_request_amendments`. Found + fixed a real gap: named `transport_parties` (not the legacy inline columns) had no visibility into the request/history/amendments at all — added `is_named_transport_party()` + 3 policies. `tests/db/transport-timeline.test.ts`. 199/199 tests. |
| `7bac2c0` | D (Phase 12) | Real urgent welfare/rescue workflow: `welfare_cases` + `welfare_case_documents` tables, eligibility gated to verified foundation/shelter/rescue orgs, ops acknowledge/review actions, `convert_welfare_case_to_transport_draft()` reusing `create_transport_draft()`. `dashboard.foundation.urgent.tsx` (real) + new `dashboard.operations.welfare-cases.tsx`. `tests/db/welfare-cases.test.ts`. 221/221 tests. |
| `f40ac47` | E (Phase 13) | Real organisation team/invitation management (`organisation_invitations`, 3 new member roles, `status` active/suspended on `organisation_members`, tier-protected invite/remove/suspend/role-change RPCs). Found + fixed a real gap: `owner_user_id` could be changed by any org owner via a plain update, silently transferring/orphaning ownership — locked to admin-only via trigger. `dashboard.foundation.team.tsx` (real) + new `_public.invitations.$token.tsx`. `tests/db/organisation-team.test.ts`. 255/255 tests. |
| `d52da87` | F (Phase 14) | Completed the adoption questionnaire on `buyer_applications` (landlord permission, veterinary plan, consent metadata, org supplemental answers jsonb, `internal_notes`, `draft`/`interview_planned`/`expired` statuses). Found + fixed a real gap: buyers had unrestricted `for all` RLS on their own application row, so could self-approve/forge `breeder_response`/forge `internal_notes` — added a default-deny lock trigger. `dashboard.foundation.applications.tsx` now renders the full questionnaire + internal notes + a transport-draft action on approval. `tests/db/adoption-questionnaire.test.ts`. 273/273 tests. |
| `cca8f02` | G (Phase 15) | Real user-facing moderation decisions and appeals: `affected_profile_id` (auto-populated for user/animal_listing targets), `public_decision_summary`, `appeal_deadline` (14 days, fixed a bug where it only fired on UPDATE not INSERT), `my_moderation_case_view` (fixed a bug where `security_invoker=true` inherited an always-false RLS result — switched to a definer-style view like `public_transport_requests`), `moderation_appeals` with a same-moderator-conflict check. New `_public.moderation.$caseId.tsx` + extended `dashboard.admin.moderation.tsx`. `tests/db/moderation-appeals.test.ts`. 299/299 tests. |
| `3026f22` | H (Phase 16) | Real notification preferences (`notification_preferences`, opt-out default, mandatory `security` category, `get_notification_preference()`/`create_notification_if_enabled()`), scoped to the 4 notification types that actually exist in the codebase (not a fabricated full category list). Replaced the "coming soon" placeholder on both breeder/foundation settings pages. `tests/db/notification-preferences.test.ts`. 320/320 tests. |
| `229cb35` | I (Phase 17) | Real `dashboard.admin.organisations.tsx` (suspend/restore + new `is_featured` flag, with a lock-trigger fix for owner self-featuring), real `dashboard.admin.settings.tsx` (markets table, previously zero UI), real `dashboard.buyer.scheduled.tsx` (scheduled-or-later transport list + timeline). Left 3 document-library placeholders honest (no backing schema, out of Stage I's priority list). `tests/db/admin-placeholders.test.ts`. 335/335 tests. |
| `77c4252` | J (Phase 18) | Real per-actor rate limiting (`rate_limit_events` + `enforce_rate_limit()`) applied to 7 previously-unprotected abuse vectors (reports/messages/welfare_cases/applications via triggers; transport-draft/amendment/invitation via RPC calls). Found + fixed a missing GRANT (same `auto_expose_new_tables=false` class as before) and — found by actually running the suite twice — initial thresholds tighter than legitimate test-fixture usage, breaking repeatability; raised with margin. `docs/RATE_LIMITING_AND_ABUSE_PROTECTION.md`. `tests/db/rate-limiting.test.ts`. 358/358 tests, verified on 3 consecutive runs without reset. |
| `545972d` | K (Phase 19) | CI hardening: duplicate-migration-prefix check, route-tree consistency check, and a repeated `test:db` run for repeatability in CI — all matching real gaps found this session. A full-repo `eslint .` surfaced ~38 pre-existing errors in untouched files (several `_public.*`, frontend-owned) — left alone deliberately. |
| `2e05a9f` | L (Phase 20) | Full database consistency/security audit via `npx supabase db dump --local --schema public` + grep (no `psql`/`pg` available in this sandbox). Systematic checks run against all 63 `public` tables: (1) every table has `ROW LEVEL SECURITY` enabled — 63/63, clean; (2) every table with at least one policy also has a `GRANT` reaching `authenticated` — 100% clean (Supabase's default privileges cover new tables automatically; the two earlier `auto_expose_new_tables=false` incidents this session were about the Data API schema cache, not this); (3) every `SECURITY DEFINER` function pins `search_path` — 57/57 clean, no search_path-hijack exposure; (4) view security model (`security_invoker` vs. definer-style) reviewed against the established convention — consistent; (5) foreign keys to `profiles(id)` reviewed: user-owned content correctly `ON DELETE CASCADE`s, audit-trail columns (`reviewed_by`/`assigned_moderator_id`/`uploaded_by`/etc.) correctly have no `ON DELETE` action (`RESTRICT`) so a moderator/ops account can't be hard-deleted out from under the trail it left — by design, consistent with account deletion being its own workflow (`account_deletion_requests`), not a raw `DELETE FROM profiles`; real execution of that workflow is still the deferred Stage AI. Closed the one concrete, already-documented gap found: quotation RLS was row-level only — `"requesters accept or reject their own quotation"` restricted `WITH CHECK (status in (...))` but nothing stopped the same UPDATE from also changing `total_price`/other columns. Added `prevent_requester_writes_to_ops_controlled_quotation_fields()` trigger (`20260101008400_quotation_requester_field_lock.sql`), not reachable through the real UI (`respondToQuotation()` only ever sends `{ status }`) but real via a raw API call. `tests/db/quotation-field-lock.test.ts`. 364/364 tests, verified on 2 consecutive runs without reset. |
| `326a00f` | M (Phase 21) | Operational scenario suite: audited existing coverage first (368 tests across 15 files already exercise draft creation, amendments, quotations, applications, moderation, welfare cases, rate limiting, role suspension) rather than re-deriving what already exists. Found and closed the one genuine coverage gap: no test had ever driven a request through the *full* driver-owned delivery journey (driver_assigned → pickup_confirmed → animal_collected → in_transport → rest_or_care_stop → approaching_destination → delivered → handover_confirmed → completed), nor exercised route assignment/unassignment end-to-end, nor a cross-tenant attack against routes/vehicles/drivers/route_assignments. New `tests/db/transport-lifecycle-scenarios.test.ts`, 3 separate scenario tests (not one giant test). No state-machine enforcement exists yet for driver-set status transitions (a driver can currently set any status value, including skipping steps or going backwards) — noted here as a known gap but *not* fixed in this stage, since "route/stop state machines" and "state-machine/chaos tests" are explicitly named as their own later stages (CB, CG) in the third queue, not Stage M's scope. 390/390 tests, verified on a fresh reset plus 2 more runs without reset. |
| `38cc74f` | N (Phase 22) | Backend performance pass (see table row above for full description). |
| `d6ddd10` | O (Phase 23) | Privacy/GDPR backend audit (see table row above for full description). |
| `10fd7ba` | P (Phase 24) | Launch blocker cleanup / doc reconciliation (see table row above for full description). |
| `1b70015` | Q (Phase 25) | Final verification: 20-point checklist re-run against the real repo state (concurrent-writer check, migration hygiene, fresh `db reset`, `test:db` ×2 at 395/395, `tsc`, scoped + full-repo `eslint` with the ~38 pre-existing baseline unchanged, `build`, route-tree consistency, RLS/grant/search_path coverage, CI parity, no committed production credentials, no push/deploy performed, frontend files untouched, rate limiting wired, privacy audit current, progress log current, docs reconciled) — all 20 green. New `docs/BACKEND_FINALISATION_REPORT.md` closes out the original queue (Stages A–Q) with this checklist plus a summary of Stages L–Q and the known open items carried forward. This closes the original Phase 10–25 queue; continuing directly into the first supplemental queue (Stage R onward) per standing instruction. | Launch blocker cleanup / doc reconciliation. Reconciled `docs/PRODUCTION_READINESS_REPORT.md` (dated 2026-07-17) against everything Stages B–O of this session actually built: corrected 4 stale "Partially ready"/"Blocks launch" entries that are now real (operations calendar, notification preferences, welfare-urgent intake, organisation team management, admin org/settings pages), downgraded "No CI pipeline"/"No automated tests" from launch blockers to resolved/partially-resolved (CI and ~395 DB/API tests both exist now), added a new "Partially ready" entry for account-deletion execution (found during Stage O), and corrected the admin-configurable-settings and notification-preference claims in the lower sections. Added matching staleness pointers to `docs/MVP_TEST_REPORT.md` and `docs/DATABASE_TESTING.md` (the latter's file-by-file test coverage list was frozen at 2026-07-22, 8 files; now 19) rather than rewriting either wholesale. Docs-only stage, no code/schema change — `tsc`/`build` re-verified clean, no `test:db` re-run needed since nothing DB-relevant changed. | Privacy/GDPR backend audit. New `docs/PRIVACY_DATA_LIFECYCLE.md`: catalogued every personal-data-bearing table/column and re-verified (not just read from comments) its actual RLS/column-grant/Storage-bucket protection against a live local instance. Confirmed clean: profiles email/phone lockdown, exact-address RLS, transport_parties external contact, welfare_cases contact/location (entire row org+ops scoped, no public policy at all), both document Storage buckets private with signed-URL-only access, `audit_logs.before`/`after` snapshots checked across every INSERT site and found to log narrow field diffs only (never a full-row PII dump), no third-party analytics/tracking SDK in `package.json`. Confirmed and clearly flagged (not fixed, correctly out of this stage's scope) that account deletion is request-tracking only — `markDeletionRequestProcessed()` flips a status flag but performs no actual deletion/anonymisation, matching the already-tracked Stage AI item — and that there's no platform-wide consent/ToS-version tracking beyond the adoption-specific `consent_version` field, matching the already-tracked "consent versioning" supplemental stage. No code/schema changes — a pure audit stage that found the existing protections were already correct rather than needing a fix, which is itself the useful (and honestly reported) outcome. | Backend performance pass. Found a real N+1 pattern in `src/lib/queries/marketplace.ts`: `listPublishedLitters`/`listApprovedKennels`/`listLittersForKennel`/`listFollowedBreeders` each called `mapLitterRow`/`mapOrgToBreeder` per row via `Promise.all(rows.map(...))`, issuing 2 count/select queries per row (2N for a page of N) instead of 2 total. Fixed by batching into `mapLitterRows`/`mapOrgsToBreeders`, each running exactly one `IN (...)` query across every id on the page regardless of N. Ran a systematic FK-index audit (`supabase db dump` + a Python pass cross-referencing every single-column FK against every existing index): ~130 foreign-key columns across the schema have no index (Postgres only auto-indexes the referenced PK side, never the FK side). EXPLAIN ANALYZE against the local seed data (max ~31 rows in any table) confirms Postgres correctly picks a sequential scan over an index scan regardless of whether an index exists at this row count — meaningfully validating "should this be indexed" via EXPLAIN isn't possible on this dataset. Added only 3 indexes with a non-speculative justification: `animals.litter_id`, `animals.organization_id`, `parent_dogs.kennel_id` — the direct, demonstrated target of the N+1 fix's new batched `IN (...)` queries, which degrade closer to linearly without an index even at moderate row counts, unlike a single-row equality lookup. The other ~127 unindexed FK columns are deliberately left alone and documented in the migration rather than indexed blindly, per this stage's own instruction — candidates for a future pass once real usage data (pg_stat_statements or equivalent) can identify genuinely hot ones. `tests/db/marketplace-listing-batching.test.ts` proves the batched query's client-side grouping produces identical counts to the original per-id queries against real seeded data. 395/395 tests, verified on a fresh reset plus one more run without reset. |

## Supplemental queue appended mid-session

A second large instruction set ("SUPPLEMENTAL OVERNIGHT BACKEND QUEUE," Stages R–AP: messaging
security, attachments, listing lifecycle, multi-species, taxonomy, search, application/handover
state machines, quotation/pricing hardening, payment abstraction, fundraising gating, outbox/email,
domain events, audit quality, feature flags, idempotency audit, data export, account
deletion/anonymisation execution, support tooling, backup/DR docs, migration quality, test
factories, load readiness, a security red-team pass, and a final release-candidate report) was
appended to the original Phase 10–25 queue. Per its own instruction, the original queue (I–Q) is
being finished first, then this session continues directly into R onward without stopping.

## Third queue appended mid-session

A third instruction set ("SECOND OVERNIGHT BACKEND PLATFORM, OPERATIONS, COMPLIANCE AND
RELIABILITY QUEUE," Stages BA–CI: background jobs, document expiry engine, org verification
workflow, driver/vehicle eligibility, route/stop state machines, proof of pickup/delivery,
incidents, service areas, i18n normalisation, consent versioning, support cases, moderation
workload, risk signals, duplicate detection, search read models, error taxonomy, API contracts,
webhooks (disabled), bulk import, archival, Storage cleanup, health checks, SLOs, incident
runbooks, maintenance mode, a migration preflight command, a database invariant catalogue,
state-machine/chaos tests, a permission inventory, data-access consolidation, a tech-debt register,
a final backend PR review, and a second release-candidate report) was appended on top of the
original and first supplemental queues. **Complete as of Stage CH** — see the table above and
`docs/BACKEND_RELEASE_CANDIDATE_REPORT_2.md`.

A fourth and fifth instruction set ("THIRD AND FOURTH LONG-RUN AUTONOMOUS BACKEND QUEUE") was
appended mid-session, ahead of stages BA–CI above: Stages CJA–CJZ (a deep security/compliance
sweep — authorization matrix, public/private projection layer, data-classification catalogue,
server-controlled actor attribution audit, cross-tenant fuzz testing, immutable lifecycle
history, soft-deletion/archival semantics, legal-hold mechanism, deletion-blocker graph,
versioned export manifest, support-access justification, impersonation-policy decision, admin
command log, privileged-reauthentication hooks, secrets/config boundary audit, rate-limit policy
config, messaging abuse controls, notification deduplication, and — cut off mid-transmission at
"CJS: notification/email template versioning" — the rest of CJS onward and the entire CKA–CKZ
queue were never received). Per its own instruction and the standing rule throughout this session
("do not manufacture schema merely to create a commit," "do not build speculative infrastructure
with no reachable product use"), each CJ stage is being treated with the same discipline as every
earlier stage: real audits close with a documented "no fix needed" outcome when the design is
already correct, and only demonstrated, reachable gaps get an actual code change — not a
mechanical checklist run through regardless of findings. This queue starts now that BA–CH is
complete, per its own stated precedence ("previously assigned order remains authoritative").

## Remaining stages (not started this session)

**Original queue**: complete (Stages A–Q, see table above and
`docs/BACKEND_FINALISATION_REPORT.md`).

**First supplemental queue**: complete (Stages R–AP, see table above and
`docs/BACKEND_RELEASE_CANDIDATE_REPORT.md`).

**Second supplemental queue**: complete (Stages BA–CH, see table above and
`docs/BACKEND_RELEASE_CANDIDATE_REPORT_2.md`).

**Third/fourth supplemental queue**: complete. Every received CJ-series stage (CJA through CJS —
authorization matrix, data classification, actor-attribution audit, cross-tenant fuzzing, immutable
history, soft-deletion semantics, legal holds, deletion-blocker graph, export manifest,
support-access justification, impersonation policy, admin command log, reauthentication hooks,
secrets/config audit, rate-limit config, messaging abuse controls, notification dedup, notification
template versioning) is done — see table above. A later message supplied the full definitions for
CJN through CJS (previously truncated mid-transmission at "CJS: notification/email template
versioning" in an earlier message); those definitions have now all been executed. CJT onward and
the entire CKA–CKZ queue were never transmitted with real content and are correctly not invented,
per standing instruction ("do not invent CKA–CKZ because their definitions were never received").

**IR-1 through IR-18** (integration-readiness/scale/release-hardening queue): **complete.** IR-1
(contract snapshot), IR-2 (marketplace search contract), IR-3 (read-model privacy, revalidation),
IR-4 (frozen frontend compatibility, no gap found), IR-5 (generated type reconciliation — retired
the hand-written stub), IR-6 (complete product scenarios — wired the missing application-to-
reservation step), IR-7 (role/suspension matrix — closed the moderator-suspension coverage gap),
IR-8 (scheduling/capacity conflicts — already correct, a caught-before-commit false start reverted
cleanly), IR-9 (document expiry jobs — closed the quotation-expiry acceptance gap), IR-10 (outbox/
notification soak — no real outbox exists, closed a genuinely missing appeal-decision notification
instead), IR-11 (Storage and signed URL security — closed the welfare-case-document tamper-after-
decision gap), IR-12 (representative volume/performance — closed a real N+1 in route matching),
IR-13 (migration rehearsal — closed a real has_role() PUBLIC-grant information-disclosure gap,
after catching and correcting a mistake in the first draft of that exact fix), IR-14 (frontend
integration conflict map — 3 real conflicts found via a real `git merge-tree` trial merge, one
hiding a genuine quotation-expiry-bypass-UX regression risk), IR-15 (integration runbook — a
concrete, not-executed merge plan built on IR-14's findings), IR-16 (release manifest — corrected a
stale, never-re-verified full-repo lint baseline claim and excluded generated files from lint
going forward), IR-17 (final backend PR review — targeted audit pass, no new issue found beyond
what IR-10 through IR-16 already fixed), IR-18 (integration-ready handoff — closeout report,
11 real fixes / 4 confirmed-correct audits / 3 doc-only stages across the whole queue) all
complete — see table above and `docs/INTEGRATION_READY_HANDOFF.md` for the full closeout. **This
closes the IR-1 through IR-18 queue.** Continuing directly into the XR-1 through XR-24 queue, per
its own stated precedence.

**XR-1 through XR-24** (append-only queue, recorded earlier this session): its own explicit
precedence ("starts only after every earlier-assigned stage... then IR-1–IR-18... completes") is
now satisfied — IR-1 through IR-18 closed above. XR-1 (protected-field mutation matrix — closed a
real `support_cases` reopen-field-smuggling gap), XR-2 (SECURITY DEFINER audit — automated the
search_path check into `db:preflight`, closed a second `has_role()`-shaped minimal-grants gap on
`get_notification_preference()`), XR-3 (grant/Data API exposure audit — found 2 broad-grant cases,
both confirmed deliberate/inert, now regression-tested), XR-4 (Storage path canonicalisation —
audit only, no new gap found; the one real historical bug in this class stays fixed), XR-5 (signed
URL permission-loss safety — proved the real 5-minute residual-risk window empirically, confirmed
cancellation is intentional-not-a-gap), XR-6 (immutable history/evidence preservation — fixed a
real `animal_ownership_history` gap, caught and reverted a wrong `reports` change before
committing), XR-7 (transactional workflow boundaries — closed a real `transport_status_history`
actor-forgery straggler across 3 functions at once; 6 partial-write candidates found and documented
for a dedicated follow-up rather than a rushed conversion), XR-8 (optimistic concurrency/stale-
write protection — closed a real `review_welfare_case()` race/terminal-state gap, caught a false
positive on moderation-case resolution before attempting a wrong fix), XR-9 (idempotency key
registry — closed 2 real "raises instead of returns original success" retry gaps and a genuine
concurrent-insert race), XR-10 (poison-job handling — audit only, confirmed not applicable, no job
system exists), XR-11 (backpressure/bounded workers — audit only, confirmed not applicable, no
worker pool exists), XR-12 (outbox payload versioning — audit only, notifications already have
this via CJS, `audit_logs` confirmed to need no version field since nothing parses it) complete —
see table above, XR-13 (template revalidation — proved CJS's "unknown version" safety claim
empirically, no duplicate implementation), XR-14 (export object lifecycle — audit only, confirmed
not applicable, `exportMyData()` is a synchronous JSON response with no export file/object to
manage), XR-15 (anonymisation consistency — proved the dry-run/execution pairing consistent and
history-preservation directly), XR-16 (legal-hold propagation — revalidated CJH, closed a real
missing-audit-trail gap on hold placement/release), XR-17 (cursor stability — added an `id`
tie-breaker to `listPublishedPuppies()`'s pagination, closing a real same-instant tie gap) complete.
XR-18 (public contract drift scanner — built `npm run db:contract-check`, live-verified it detects
real drift, corrected IR-1's stale self-citation) complete. XR-19 (fixture determinism — new
`uniqueTestEmail()` helper, migrated 7 `auth.signUp()` call sites off collision-prone
`Date.now()`-only emails, documented a deliberate scope boundary against the suite's many other
lower-severity `Date.now()`-only occurrences) complete. XR-20 (schema/migration drift detection —
new `npm run db:schema-drift` wrapping `supabase db diff --local` with a real pass/fail exit code,
live-verified against injected out-of-band drift) complete. XR-21 (query performance budget audit —
new `npm run db:perf-report` wrapping the already-enabled `pg_stat_statements` extension; ran it
against a real full-suite workload and confirmed, with evidence rather than a guess, that Stage
N/W's existing targeted indexes cover every actually-hot query, the exact "future pass" those
stages deferred) complete. XR-22 (backend integration manifest — new
`docs/BACKEND_INTEGRATION_MANIFEST.md`, the current "what's true right now" reference consolidating
fresh verified numbers, the full `db:*` verification toolbelt, and the real open-items backlog
compiled from this session's own audits, superseding the now-stale earlier point-in-time snapshot
docs) complete. XR-23 (final adversarial matrix — cross-referenced all 34 public RPCs against test
coverage, found 2 with zero coverage at all (`change_org_member_role`, `decline_org_invitation`),
added real adversarial tests for both including `change_org_member_role`'s previously-unexercised
self-escalation-prevention logic) complete. XR-24 (six-hour closeout report — new
`docs/XR_QUEUE_CLOSEOUT_REPORT.md`, all figures re-verified fresh at this commit) complete. **The
entire XR-1 through XR-24 queue is now done.**

**A restated "FOUR-HOUR AUTONOMOUS BACKEND QUEUE FOR CLAUDE CODE BOT 2" message arrived mid-session
a second time**, describing an "expected current state" of "Stages BA through BK completed, next
stage BL" — stale by the message's own admission ("this state may now be stale. Trust the real
repository") and superseded by real progress (BA–CH long since closed, CJA–CJI in progress at time
of receipt). Correctly not acted on as a restart signal; its stage-workflow/testing/security-
invariant rules are the same standing rules already being followed, not new instructions.

**A genuinely new "NEXT 6 HOURS AUTONOMOUS BACKEND QUEUE" arrived immediately after**, explicitly
append-only ("first finish every stage already assigned... do not restart completed stages... do
not overwrite newer repository reality with old prompt assumptions"). Adds Stages XR-1 through
XR-24 — protected-field mutation matrix, SECURITY DEFINER/privilege-escalation audit, grant/Data-
API exposure audit, Storage path canonicalisation, signed-URL revocation safety, immutable
history/evidence preservation (overlaps CJF — will be treated as a revalidation pass if still
needed by the time it's reached, not a duplicate build), transactional workflow boundaries,
optimistic concurrency/stale-write protection, idempotency key registry, dead-letter/poison-job
handling, backpressure/bounded workers, outbox payload versioning, notification/email template
versioning, data export object security, anonymisation referential integrity, legal-hold
propagation (overlaps CJH — same revalidation approach), pagination drift/cursor stability, public
contract drift scanner, seed/fixture determinism, schema snapshot/migration drift detection,
query performance budget audit, backend integration manifest, a final adversarial matrix, and a
six-hour closeout report. Per its own explicit precedence, this queue starts only after every
earlier-assigned stage (the rest of CJ, then IR-1–IR-18) completes.

## YR queue (YR-1 through YR-25): stages completed

A genuinely new queue, assigned after XR-1 through XR-24 and both transactional-workflow follow-ups
were already complete. The assigning message's own "CURRENT EXPECTED STATE" claimed the repository
was at Stage IR-10 with 782/782 tests — stale by the message's own admission ("this checkpoint may
be stale. Trust the real repository."); the real repository was already at XR-24 plus two follow-up
stages (948/948 tests, `c8bc235`). Correctly not acted on as a restart signal, matching this
session's own established precedent for the same situation earlier (the "BA through BK" and "IR-10"
stale-checkpoint messages before it) — only the genuinely new YR-1–25 content was treated as real
new work.

| Commit | Stage | Summary |
|---|---|---|
| `PENDING` | YR-20 | Dependency and supply-chain audit. New `docs/DEPENDENCY_SUPPLY_CHAIN_AUDIT.md`. Lockfile consistent (`npm ls` clean), 0 production vulnerabilities (`npm audit --omit=dev`); 5 high-severity dev-only findings all trace to one root cause (`minimatch`/`brace-expansion` ReDoS via eslint's own dependency chain) — real but no reachable attack surface (only ever matches this repo's own source globs locally), fix requires a semver-major eslint bump, deliberately not forced per this stage's own "do not upgrade blindly" instruction, flagged for a dedicated future pass instead. 7 "possibly unused" packages flagged by an initial grep, each individually investigated and confirmed a false positive (build-tooling consumed indirectly, CSS `@import`, React SSR runtime requirement, or used in `tests/` outside the grep's `src/`-only scope) rather than reflexively removed. `npm outdated` shows healthy normal version drift, no abandoned/deprecated package found. No code change. |
| `30efbac` | YR-19 | Release-preflight self-test. New `scripts/release-preflight.mjs` (`npm run release:preflight`), consolidating the same ~8-step verification contract this session has manually run by hand every single stage into one real, executable command with a real non-zero exit code. Found and fixed 2 real bugs while building it: (1) a naive `eslint .` pass/fail check would have always failed given the documented 21/13 pre-existing lint baseline — fixed by comparing the actual count against the baseline instead; (2) the secret-scanner's PEM-private-key pattern (starting with `-`) was silently parsed as a `git grep` command-line flag instead of a search pattern, making the scan a permanent no-op — caught by actually running the script, fixed with an explicit `-e` flag. Verified each failure mode fires for real: ran with genuine uncommitted files (correctly failed), injected a real fake secret into a throwaway tracked file and confirmed detection with exact file/line before removing it, and confirmed the lint-baseline comparison passes correctly against the real current repo state. No migration, no test-suite change. `tsc`/`eslint scripts/release-preflight.mjs`/build clean, full `test:db` unaffected (1006/1006, no schema change), 141 migrations unchanged. |
| `b6df200` | YR-18 | Maintenance and degradation chaos audit. New `docs/MAINTENANCE_DEGRADATION_AUDIT.md`. Checked maintenance mode against direct RPC/API access per this stage's own instruction: confirmed enforcement is entirely at the Cloudflare Worker edge (`src/server.ts`), with zero database/RPC-layer awareness — a real finding, but traced to a deliberate, already-documented scope boundary (this app has exactly one real client, its own frontend; no public API exists to actually exploit the bypass), not an oversight. Flagged for revisiting if a public API is ever added, not built speculatively now (would be a large cross-cutting migration for a threat with no current real consumer). Confirmed "provider not configured" already handled (OAuth/fundraising), "job backlog" not applicable (no job system). Storage/DB outage injection documented as a genuine gap requiring a substantial new testing capability, not built here. No code change. |
| `b4dc230` | YR-17 | Operational read-model consistency — audit only, confirmed not applicable, no code change. New `docs/OPERATIONAL_READ_MODEL_AUDIT.md`. Grepped for `materialized view` (zero results — every view is a plain, always-fresh SQL view) and any stored denormalized count/aggregate column (none found — `listDriverWorkloads()`'s `activeJobCount` is computed live). Every named dashboard (support/moderation/transport/route/document-expiry/notification) queries its canonical table directly, live. No caching/read-model layer exists anywhere to audit for drift, matching the same architecture confirmed at XR-10/XR-11/XR-12/YR-4. |
| `c6ff881` | YR-16 | Error-contract consistency. New `docs/ERROR_CONTRACT_CONSISTENCY_AUDIT.md`. Found `getFriendlyErrorMessage()` (Stage BQ, already correct) was only wired into 1 of 88 customer-facing `toast.error(...)` call sites — 87 still passed raw `err.message` straight to the screen, exactly the raw-Postgres-leak risk it was built to prevent. Wired into 32 genuinely customer-facing route files (ops/admin dashboards correctly exempt per CLAUDE.md); 30 matched the exact existing idiom and were converted mechanically preserving every fallback message verbatim, 2 more fixed by hand. Deliberately left 3 auth-flow files unchanged (GoTrue Auth errors, never routed through Postgres, already safe strings). Confirmed conflict/forbidden/stale_write/rate_limited/reauthentication_required/feature_disabled all remain distinguishable — each is its own `P0001` with specific text, passed through unchanged by design. `tsc`/lint/build clean across all 32 files, full-repo lint baseline unchanged (21/13), full `test:db` unaffected (1006/1006, pure frontend change). |
| `88a6249` | YR-15 | Raw API bypass audit. New `docs/RAW_API_BYPASS_AUDIT.md`. **Found and closed a real gap**: `escalate_report_to_case()`'s duplicate-prevention only protected the RPC path — "moderators and admins manage all moderation cases" (a correct `for all` policy) meant a raw direct insert could still create a duplicate case for an already-escalated report. New migration `20260101013900_moderation_case_report_unique.sql`: real unique constraint on `moderation_cases (report_id)`, the strongest enforcement (works regardless of path); hardened `escalate_report_to_case()`'s own concurrent-insert handling to match `convert_application_to_reservation()`'s established precedent (Stage XR-9). Spot-checked every RPC this session added against the same bypass shape and confirmed the rest all follow the established, deliberate "trusted staff can always bypass their own RPC via raw update, and that's not a security issue since they already have full legitimate access" model — no deviation found. Re-swept this stage's own new migrations specifically for a *lower-trust*-actor bypass (the real bug class) and found none. New test in `tests/db/rehoming-report-atomic-rpcs.test.ts` (18 tests in file). 1006/1006 full suite (+4 from YR-14's 1002), verified on a fresh reset plus one more run without reset, `tsc`/lint/build/`db:preflight`/`db:contract-check` clean, 141 migrations, no duplicate prefixes. |
| `1891f8c` | YR-14 | Cross-tenant fuzz matrix. New `docs/CROSS_TENANT_FUZZ_MATRIX.md`, extended `tests/db/cross-tenant-fuzz.test.ts` (Stage CJE) with a new `organisation_members` case (an org's roster had no dedicated cross-tenant test before). Rather than force every named domain (support/moderation/welfare/messages) into this file's intentionally lightweight, purely-seeded-data shape (none of those tables have seeded rows, so covering them here would mean duplicating disposable-fixture setup for no added coverage), the doc is a real cross-reference matrix showing exactly where each domain's cross-tenant isolation is already proven — most already covered by dedicated tests elsewhere (support-cases.test.ts, welfare-cases.test.ts, access-control.test.ts, etc.), 2 confirmed not applicable (jobs — no such system; ownership — no such workflow, YR-4/YR-11). 1002/1002 tests (+1 from YR-13's 1001), verified on a fresh reset plus one more run without reset, `tsc`/lint/build/`db:preflight` clean, 140 migrations unchanged. |
| `74e17cd` | YR-13 | Public privacy regression suite. New `docs/PUBLIC_PRIVACY_REGRESSION_SUITE.md` and `tests/db/public-privacy-regression-suite.test.ts` (12 tests) — a single reusable anonymous-visitor sweep across animals, organisations, `private_addresses`, transport exact addresses, profile contact fields, report reporter identity, welfare-case internals, and moderation-case internals. **Investigated and confirmed deliberate, not a leak**: `animals.owner_profile_id` + `profiles.city`/`country`/`display_name`/`avatar_url` being anon-readable — two dedicated migrations exist specifically for this (private-rehoming listings need a public rough location the same way org listings show theirs), narrowly scoped, email/phone still excluded — caught before treating it as a bug, the same "confirm before fixing" discipline that already caught two wrong fixes earlier this session. `animals.microchip_number`'s public anon visibility investigated and left as a genuine product judgment call (this schema's own `missing_or_false_microchip`/`stolen_animal` report reasons suggest deliberate anti-fraud public-verification design) — flagged, not guessed at. No migration this stage (no schema change — every property tested was already correctly enforced). 1001/1001 tests (+12 from YR-12's 989), verified on a fresh reset plus one more run without reset, `tsc`/lint/build/`db:preflight` clean, 140 migrations unchanged. |
| `3880478` | YR-12 | Deletion and export consistency. New `docs/EXPORT_DELETION_CONSISTENCY.md`, superseding the stale equivalent table in `docs/PRIVACY_DATA_LIFECYCLE.md` (Stage O, written before account-deletion execution existed). Cross-referenced all 10 `exportMyData()` categories against `execute_account_deletion()`'s live definition: only `profile` is anonymised, every other category (transport requests, reservations, applications, posts, messages, notifications, etc.) is deliberately retained, matching the existing FK-safety design — documented explicitly per-category rather than left implicit. Checked "export after deletion reconstructing public identity" (no gap — export is always self-scoped via RLS, never a third-party path) and "respect legal holds" (already enforced upstream by `execute_account_deletion()` itself refusing to run while a hold exists, so export never encounters a half-deleted state). No code change — a genuine cross-reference/documentation deliverable. |
| `20cc79d` | YR-11 | Ownership-transfer finality audit — audit only, confirmed not applicable, no code change. New `docs/OWNERSHIP_TRANSFER_FINALITY_AUDIT.md`. Checked directly (not assumed): `animal_ownership_history` has no INSERT policy for anyone (Stage XR-6), `animals.owner_profile_id` is only ever set once at creation (grepped every query file), and `advance_transport_job_status()` never touches ownership fields at all — a completed handover/delivery has zero connection to any ownership-state change today. There is no real ownership-transfer feature anywhere in this app to audit finality/duplicate-prevention/concurrency properties of; building one now would be exactly the speculative infrastructure this session's standing discipline avoids. Documented what a future session should design before this becomes fixable. |
| `a97cbe0` | YR-10 | Transport operational timeline integrity. New `docs/TRANSPORT_TIMELINE_INTEGRITY_AUDIT.md`. Confirmed the driver-side transition graph (Stage CC) already correctly prevents delivery-before-pickup and impossible jumps. **Found and closed a real, previously-unfulfilled gap**: an earlier migration (Stage CC) explicitly promised an "explicit reason + audit" override path for ops reopening a terminal transport request, assigning it to "IR-8" — traced through the progress log and found IR-8 actually covered scheduling/capacity conflicts (already correct, unrelated topic), so this accountability half was simply never built. New migration `20260101013800_transport_terminal_reopen_reason.sql`: moving a request out of a terminal status (`completed`/`rejected`/`cancelled_by_customer`/`cancelled_by_operations`) now requires a real, non-blank internal note; every other transition remains completely unconstrained, preserving ops' deliberate override flexibility. The ops UI already has a real internal-note field for this — no frontend change needed. New tests in `tests/db/change-ops-request-status.test.ts` (7 new tests). 989/989 full suite (+7 from YR-9's 982), verified on a fresh reset plus one more run without reset, `tsc`/lint/build/`db:preflight`/`db:contract-check` clean, 140 migrations, no duplicate prefixes. |
| `2a378a3` | YR-9 | Driver and vehicle eligibility consistency. New `docs/DRIVER_VEHICLE_ELIGIBILITY_AUDIT.md`. Found a real gap: `assign_driver_to_job()` deliberately only checks a `drivers` row exists (ops make the final call by design), but `listDriverWorkloads()` never selected `internal_verification_status`/`document_expiry_date` at all — ops had zero visibility into whether a driver was unverified or document-expired while assigning them. Fixed by exposing both columns (no RLS change — already fully readable under the existing `for all` ops policy) and adding a real warning badge to `dashboard.operations.dispatch.tsx`, reusing the existing `documentExpiryWarning()` pure function. Confirmed `transport_requests.assigned_vehicle_id` is genuinely unwired (protected, FK-constrained, but zero code anywhere sets it) — left alone, matching this session's established "don't build speculative infrastructure for a workflow that doesn't exist" discipline. Confirmed "one consistent eligibility contract" already holds by construction (exactly one path, `assign_driver_to_job()`, can set `assigned_driver_id`). Attempted a live browser check; hit the same pre-existing hydration/sign-in issue documented in `docs/E2E_TESTING.md` (unrelated to this change, not fixed, disclosed honestly rather than skipped silently); worktree left clean. No migration this stage. `tsc`/lint/build clean, full suite unaffected (982/982, same as YR-8, no schema change). |
| `cfd8036` | YR-8 | Organisation trust-state consistency. New `docs/ORGANISATION_TRUST_STATE_AUDIT.md`. Confirmed already correct: public visibility for animals/litters/parent_dogs/welfare_cases/fundraising/achievements already all gate on `verification_status = 'approved'` (grep-confirmed, new direct test), and owner self-verification is already locked (Stage CJM). **Found and fixed a real gap**: `buyer_applications`' INSERT policy never checked the target animal's organisation at all — a buyer who already knew an animal's id (e.g. a stale `saved_animals` reference) could submit a brand-new application against a suspended organisation, bypassing the suspension. New migration `20260101013700_suspended_org_application_lock.sql` splits the policy so only INSERT gets the new check, leaving existing applications (made before suspension) fully visible/withdrawable. "Restoration bypassing reverification" checked and found not applicable — no separate re-verification pipeline exists to bypass. New `tests/db/organisation-trust-state-consistency.test.ts` (16 tests). 982/982 full suite (+16 from YR-7's 966), verified on a fresh reset plus one more run without reset, `tsc`/lint/build/`db:preflight`/`db:contract-check` clean, 139 migrations, no duplicate prefixes. |
| `5aba888` | YR-7 | Admin command catalogue and step-up map. New `docs/ADMIN_COMMAND_CATALOGUE.md` — full table of all 41 RPCs (gate, actor derivation, audit status, idempotency, step-up requirement). Building it found a real, demonstrated gap: cross-referencing the *live database's* actual function definitions (`pg_get_functiondef`, not migration file text alone — several functions are redefined across multiple migrations) found 9 staff-privileged RPCs with zero `audit_logs` trail at all, inconsistent with sibling RPCs of the same privilege tier. Four are this session's own new RPCs (`assign_driver_to_job`, `send_quotation`, `escalate_report_to_case`, `approve_rehoming_review`) — closed now rather than left to be rediscovered. New migration `20260101013600_admin_command_audit_coverage.sql` adds a real, correctly-attributed audit entry to all 9 (`assign_request_to_route`, `assign_driver_to_job`, `send_quotation`, `convert_application_to_reservation`, `approve_user_verification`, `mark_risk_signal_reviewed`, `claim_moderation_case`, `escalate_report_to_case`, `approve_rehoming_review`), with idempotent-retry paths deliberately not duplicating the entry. Also confirmed only 3 RPCs need step-up auth today (`execute_account_deletion`, `place_legal_hold`, `release_legal_hold`) and checked every other privileged command against that bar without finding a comparable gap. Tests extended across 7 files (109/109 passing in isolation). 966/966 full suite (+7 from YR-6's 959), verified on a fresh reset plus one more run without reset, `tsc`/lint/build/`db:preflight`/`db:contract-check` clean, 138 migrations, no duplicate prefixes. |
| `51e2f48` | YR-6 | Support-to-operations boundary audit. New `docs/SUPPORT_OPERATIONS_BOUNDARY_AUDIT.md`. Checked this app's real role model against the stage's premise: no separate "support" role exists — `is_ops_staff()` is the single gate for both support-case management and every operations RPC, a deliberate documented choice, so there's no lower-trust "support-only" actor that could accidentally cross into operations mutations. Confirmed `support_cases.related_entity_id`/`related_entity_type` (documented as "context only, never a capability") is genuinely inert by grep (zero references anywhere) and by a new direct test (a case referencing a foreign user's transport request grants no read access to it). Also proved the one staff/staff boundary that does genuinely exist — ops staff vs. moderator are independently checked gates, not a combined super-role — with a new test using the seeded `ops` persona (operations role only) correctly rejected by `claim_moderation_case()`. 2 new tests in `tests/db/support-cases.test.ts` (38 total, up from 36). No migration this stage. 965/965 tests (+6 from YR-5's 959), verified on a fresh reset plus one more run without reset, `tsc`/lint/build/`db:preflight` clean, 137 migrations unchanged. |
| `095db07` | YR-5 | Event replay safety. New `docs/EVENT_REPLAY_SAFETY_AUDIT.md` and `tests/db/event-replay-safety.test.ts`. No queued-event system exists (re-confirmed), so "replay" means a client retry, the same case Stage XR-9 already addressed per-RPC. New test proves the genuinely untested combination: RPC idempotency (XR-9) and notification dedup (CJR) had each only been proven in isolation, never together — a real network retry re-runs *both* calls (every real notification call site calls its RPC then unconditionally calls `notifyUserFromTemplate()`), and this proves that combination produces exactly one customer-visible notification, never two. The other 3 named concerns (template-drift immutability, unsupported-version safety, manual-replay authorization) were already covered by CJS/XR-13, or not applicable (no manual replay feature exists) — cross-referenced, not duplicated. No migration this stage. 959/959 tests (+5 from YR-4's 954), verified on a fresh reset plus one more run without reset, `tsc`/lint/build/`db:preflight` clean, 137 migrations unchanged. |
| `6eb3cb9` | YR-4 | Outbox consumer lease and recovery — audit only, not applicable, no code change. New `docs/OUTBOX_CONSUMER_LEASE_AUDIT.md`. Re-confirmed (fresh grep sweep) no job/worker/outbox/lease system exists anywhere, consistent with XR-10/XR-11/XR-12/YR-3's independent findings — every write is synchronous, request-scoped, nothing to lease or reclaim. Documented the two real, already-tested "concurrent actor" mechanisms that cover the only genuinely reachable version of this concern in this architecture (`claim_moderation_case()`/`claim_support_case()`'s atomic claims, and the `select ... for update` row-locking pattern used throughout this session's atomic RPCs) rather than building a speculative worker pool to have something to audit. |
| `ea9428a` | YR-3 | Notification locale and fallback hardening — audit only, not applicable, no code change. New `docs/NOTIFICATION_LOCALE_FALLBACK_AUDIT.md`. This app has real i18n infrastructure (`src/lib/i18n/`, EN/PL) but it's client-side-only (not SSR-aware, documented as a known gap in its own comment) and never wired into notifications, which are created server-side, synchronously — there is no server-reachable locale signal to capture even in principle today. Checked, not assumed: no template payload contains a locale field. "Unsupported template fails safely" and "retries preserve identity" were already proven by XR-13's/CJR's own tests (cross-referenced, not duplicated) — the DB layer never re-resolves a stored notification against the template registry at read time by construction. Matches this session's established honest handling of a stage whose named concern doesn't apply to the real architecture (same treatment as XR-10/XR-11/XR-12), rather than building speculative locale infrastructure to have something to change. |
| `071bba6` | YR-2 | Notification preference enforcement matrix. New `docs/NOTIFICATION_PREFERENCE_ENFORCEMENT_MATRIX.md` — ties each of the 4 real categories to optional/mandatory status and exactly where that's enforced (`get_notification_preference()` inside `create_notification_if_enabled()`, the only reachable insert path into `notifications`). Confirmed, not assumed: optional preferences are genuinely enforced server-side (Stage XR-2's grant lock already makes `get_notification_preference()` uncallable directly), and `security` is genuinely unconditional (hard-coded `true` before the stored row is even read). Checked "legal-hold, account, ownership-transfer and critical transport messages cannot be disabled" against the real YR-1 producer inventory and confirmed none of these currently produce a notification at all — nothing to make mandatory that doesn't exist yet; not fabricated here. **New test closes a real, previously-unproven property**: `notifications`' SELECT RLS is pure ownership with no join back to `notification_preferences`, meaning preference is a creation-time gate only, never a read-time filter — new test in `tests/db/notification-preferences.test.ts` proves this directly (create a notification, disable its category afterward, confirm the notification is completely unaffected: still exists, still correct, still normally mark-as-read-able), the exact "prevent preference changes from mutating historical queued events unexpectedly" property this stage's own definition names. No migration this stage. 954/954 tests (+6 from YR-1's 948), verified on a fresh reset plus one more run without reset, `tsc`/lint/build/`db:preflight` clean, 137 migrations unchanged. |
| `750e97e` | YR-1 | Notification producer inventory. New `docs/NOTIFICATION_PRODUCER_INVENTORY.md`. Inventoried all 5 real notification producers (this app has no email/push provider and no outbox/job system — confirmed repeatedly by earlier stages — so "notification" means exactly one thing: a synchronous `create_notification_if_enabled()` RPC call) — event source, template id, category, version, dedup key, mandatory/optional status, all in one table. **Found and fixed a real, reachable drift gap**: `category` was a second, independently-suppliable argument to `notifyUserFromTemplate()` alongside `templateId`, with nothing tying the two together — a future call site could type-check fine while sending an `adoption` template under a mismatched `category`, silently gating it on the wrong preference. Fixed at the type level: `category` now lives on the template definition itself (the single source of truth) and is no longer a caller argument at all — impossible to get wrong, not just tested against. `NotificationCategory` moved from `notifications.ts` to `notification-templates.ts` to avoid a circular import, re-exported from its old location. Investigated the `security` category (already correctly hard-coded as always-enabled in `get_notification_preference()`) and confirmed, not assumed, that it has zero real producers — checked the two most obvious candidates (legal-hold placement, account deletion) and found both are deliberately *not* good fits for a user notification, documented why rather than manufacturing one. New `tests/unit/` directory + `npm run test:unit` (pure-logic, no Supabase dependency, distinct from `tests/db`/`tests/e2e`) — 7/7 passing, the "drift coverage" this stage's own definition asked for. No migration this stage (pure TypeScript refactor of an already-tested pipeline). 948/948 `test:db` unaffected, verified on a fresh reset plus one more run without reset (one transient rate-limit-exhaustion flake from cumulative same-session runs, confirmed resolved by the fresh reset, not a regression), `tsc`/lint (21/13 baseline unchanged)/build/`db:preflight` clean, 137 migrations unchanged. |

## Next up

Stage YR-21 (schema ownership and naming consistency). Re-run the Stage A baseline checks (`git
status`, `db reset`, `test:db` ×2, `tsc`, `build`) before starting, per the "how to resume" section
below. **Note for whoever resumes**: candidate follow-up:
`docs/TRANSACTIONAL_WORKFLOW_BOUNDARIES_AUDIT.md` (Stage XR-7) still lists `createTransportRequest`
as the one remaining multi-write function not converted to a full atomic RPC, deliberately deferred
(reasoning in `docs/TRANSACTIONAL_WORKFLOW_BOUNDARIES_FOLLOWUP_1.md`) — not this session's own next
stage but worth picking up whenever convenient. `docs/E2E_TESTING.md` also documents a real,
unfixed SSR-hydration race on the sign-in/sign-up forms found 2026-07-27 (credentials briefly
appear in the URL query string on a fast click before React hydrates) — frontend work, out of this
session's backend-only mandate, flagged for whoever owns that surface next.
`src/lib/supabase/types.ts` is now real generated output (Stage IR-5), not a hand-written stub —
re-run `npm run db:types` after any future migration that adds/changes a table, column, or RPC
signature the app uses, the same as any other generated artifact. **Also note**: `eslint.config.js`
now excludes `src/lib/supabase/types.ts`/`src/routeTree.gen.ts` from lint (Stage IR-16) — the real
full-repo lint baseline is **21 errors / 13 warnings**, not the stale "38/13" figure repeated in
many earlier commit messages.

## Second supplemental queue: stages completed

| Commit | Stage | Summary |
|---|---|---|
| `64beacd` | BA | Background jobs/scheduler foundation audit — no reachable use yet (see table row above). |
| `8bdf62d` | BB | Document requirements/expiry engine audit — no fix needed (see table row above). |
| `d650b35` | BC | Organisation verification workflow. Found a real idempotency gap: `approve_user_verification()` had no guard against being called twice on the same verification. For breeder/organisation types (which create a real `organisations` row) this failed messily on a deterministic-slug unique-constraint violation rather than a clean business error, and there was no protection against a genuine concurrent double-approval race (two admins, or one double-click) — both transactions could read `status != 'approved'` before either committed. Fixed with `20260101009700_verification_approval_idempotency.sql`: `select ... for update` to serialize concurrent callers, plus an explicit status check before any side-effecting insert, matching the pattern already used in `execute_account_deletion()` (Stage AI) and `change_ops_request_status()` (Stage AD). New `tests/db/verification-approval-idempotency.test.ts` proves both the sequential-double-call case and a genuine concurrent race (`Promise.all`) resolve to exactly one organisation, never two — using disposable signed-up accounts throughout rather than the shared `breederPending`/`foundationPending` personas other test files read as a given pending state. 501/501 tests, verified on a fresh reset plus two more runs without reset (a third run for this concurrency-touching stage). |
| `d8fd63d` | BD | Driver onboarding/eligibility. Found the exact same bug class already fixed once this session (`20260101006100_owns_org_checks_active_role.sql`, "suspending the exact role that earned someone their organisation had no effect on their ability to keep managing it") — but never applied to drivers. Four independent policies/functions all checked only `drivers.profile_id = auth.uid()`, never `user_roles.status`: `is_my_driver_id()`, `is_assigned_driver_for_request()`, the `transport_documents` driver-read policy, and the Storage `transport-documents` driver-read policy. A suspended driver kept full access to their assigned transport requests, route stops, status history, and job documents. Fixed all four in one migration (`20260101009800_driver_id_checks_active_role.sql`), each adding `and public.has_role(auth.uid(), 'driver')`. New subtest in `tests/db/workflows.test.ts` (alongside the existing ops/org-owner suspension tests) proves suspending the driver role revokes their assigned-request access and restores correctly afterward. 502/502 tests, verified on a fresh reset plus one more run without reset. |
| `af9ac2c` | BE | Vehicle fleet/capacity/maintenance audit — no fix needed (see table row above). |
| `1377dd1` | BF | Route execution/stop state machines. `route_stops` had no uniqueness on `(route_id, stop_order)` — two stops on the same route could silently share the same order, making the route's sequence ambiguous. Fixed with a real unique constraint (`20260101009900_route_stops_order_uniqueness.sql`); `route_stops` is ops-only-write (structurally rules out the self-approval bug class, same as vehicles/routes), so this is a data-integrity safeguard, not a security fix. Deliberately did **not** build a full `routes.status` state-machine trigger here: ops needs real override ability (cancelling from any non-terminal state is legitimate), and this session already learned the hard way that a restrictive state machine on an ops-trusted table needs the "explicit reason + audit record" override path IR-8 is explicitly scoped to build — a partial, unaudited version now would conflict with that dedicated later stage rather than complement it. New `tests/db/route-stops-integrity.test.ts`. 507/507 tests, verified on a fresh reset plus one more run without reset. |
| `e4f9c10` | BG | Proof of pickup/delivery. `transport_status_history.evidence_url` existed since the original migration but was never wired to anything (zero references anywhere in `src/`) — a driver logging "delivered"/"handover_confirmed" produced only a bare status string, no photo evidence, despite the schema clearly being designed for it (the same "schema exists, never wired" shape as `messages.attachment_url` before Stage S). Also found `advanceJobStatus()` (the real function drivers use to progress their job) had the exact same non-atomic + forgeable-actor shape already fixed for `changeOpsRequestStatus()`/`assignRequestToRoute()` — two separate client-side writes with a client-supplied `driverProfileId` trusted as the actor. Fixed both together in `20260101010000_pickup_delivery_evidence.sql`: a new private `transport-evidence` Storage bucket (driver upload/read for their active job, requester read, ops manage-all) plus a new `advance_transport_job_status()` SECURITY DEFINER RPC doing the status update + history insert atomically with a server-stamped actor and an optional evidence object path. `src/lib/queries/driver.ts`'s `advanceJobStatus()` and its one call site (`dashboard.driver.index.tsx`) updated to match; the driver UI itself doesn't yet have a photo-attach control (the backend accepts an optional `evidencePhoto` File and is fully ready — building the actual capture UI is a real, separate, deliberately-deferred frontend task, documented rather than rushed). New `tests/db/pickup-delivery-evidence.test.ts`. 514/514 tests, verified on a fresh reset plus one more run without reset. |
| `08933f7` | BH | Incidents/animal-welfare events. Audited `transport_incidents` and found it already correctly built: `reported_by` is RLS-enforced (`with check (reported_by = auth.uid())`), not a client-trusted field — the forgeable-actor pattern found and fixed several times elsewhere this session doesn't apply here, it was already closed. Drivers have no UPDATE policy at all (can't edit their own report after filing — ops-only resolution, preserving report integrity), and `is_assigned_driver_for_request()` (already fixed in Stage BD to check active role) correctly gates who may report. What was missing: zero test coverage. New `tests/db/transport-incidents.test.ts` covers assigned-driver reporting, forged-actor rejection, unrelated-driver rejection, no-self-edit, ops resolution, and customer/requester exclusion from raw incident records. No code change — closing a real coverage gap on an already-correct design. 523/523 tests, verified on a fresh reset plus one more run without reset. |
| `ce1b646` | BI | Service areas/market availability audit — duplicates Stage AF, no fix needed (see table row above). |
| `50d0d5b` | BJ | Country/currency/timezone/unit normalisation. Found a real gap: `currency` is free-text with zero validation across 8 tables (`profiles.preferred_currency`, `animals`, `reservations`, `quotations`, `pricing_rules`, `fundraising_campaigns`, `fundraising_contributions`, `markets`), and some of these columns are writable by a party this session otherwise treats as "trusted but not fully trusted" (an org sets `animals.currency`/`reservations.currency` on their own rows) — the exact "row-level access doesn't validate the column's *value*" shape this session has repeatedly closed elsewhere. Grepped every currency literal in the schema/seed: only `'EUR'`/`'PLN'` are ever used. Added a `CHECK` constraint to all 8 columns (`20260101010100_currency_code_validation.sql`); the migration applying cleanly against existing seed data confirmed no other value was ever actually in use. Deliberately did **not** attempt a full country-normalisation overhaul: `country` stays free-text almost everywhere while only `markets.country_code` is ISO-constrained — a real inconsistency, but Stage BI's audit already confirmed nothing currently joins the two together, so fixing it now would be a large, risky migration for no current behavioural gain; documented here instead of rushed. New `tests/db/currency-validation.test.ts`. 528/528 tests, verified on a fresh reset plus one more run without reset. |
| `c0b0edf` | BK | Legal-document/consent versioning. The signup page already says "By creating an account, you agree to Havenpaw's Terms and Privacy" — but nothing recorded that a specific user actually consented to a specific version of either document. New `legal_document_versions` (admin-managed, publicly readable, at most one `is_current` row per document type via a partial unique index) and `user_consents` (append-only — no update/delete policy for ordinary users at all; INSERT requires the version to genuinely match a current row, not an arbitrary string) in `20260101010200_legal_consent_versioning.sql`. Wired into the real `signUp()` server action (`src/lib/auth/actions.ts`, not a frontend-owned file) as a best-effort step after account creation, matching the promise the signup page already makes, without inventing any new UI. Seeded the current terms/privacy/cookies versions honestly labelled `2026-07-24-draft`, matching that the underlying pages are still pending lawyer review — the versioning mechanism works the same regardless of draft-vs-final content. **Found and fixed a real bug while testing**: hit the `auto_expose_new_tables=false` GRANT gotcha a third time this session — `user_consents` was granted `select, insert` but not `update, delete`, so the admin "manage all" RLS policy was silently unreachable for those two operations via the Data API despite RLS itself allowing it; caught because the test's cleanup step now actually asserts the delete succeeded instead of ignoring its result. New `tests/db/legal-consent-versioning.test.ts`, including a full simulation of the exact sequence `signUp()` performs (this harness can't invoke the TanStack Start server function directly, but proves every DB operation it depends on). 537/537 tests, verified on a fresh reset plus two more runs without reset (a third run given the grant bug found mid-stage). |
| `c14a760` | BL | Support case management audit — no fix needed at the time (see table row above; note: a much more detailed spec for this stage arrived mid-session after this commit — see the BL-addendum row below, added out of order once that detail was available). |
| `a5ab979` | BM | Moderation assignment/reviewer workload. Found a real gap in the "investigate" UI action (`dashboard.admin.moderation.tsx`): a plain, unconditioned `UPDATE` setting `assigned_moderator_id` to a client-supplied value, with no guard against the case already being claimed by someone else. Two moderators clicking "investigate" on the same unassigned case at the same moment would race — whichever write landed last silently won, and any moderator could set `assigned_moderator_id` to a *different* moderator's id (the same forgeable-actor shape closed repeatedly this session). Fixed with a new `claim_moderation_case()` SECURITY DEFINER RPC (`20260101010300_moderation_case_claim_rpc.sql`): `select ... for update` to serialize concurrent claimers, a clear "already claimed by another moderator" error (idempotent if it's already you), and a server-stamped actor — matching `approve_user_verification()`'s pattern from Stage BC. Deliberately narrow: broader moderation actions (resolving, reassigning, adding notes) keep using the existing `is_moderator()` policy directly, unchanged. New `tests/db/moderation-case-claim.test.ts`, including a genuine `Promise.all` concurrency race proving exactly one of two simultaneous claims wins. 551/551 tests, verified on a fresh reset plus two more runs without reset (a third run for this concurrency-touching stage). |
| `ab25ee2` | BL-addendum | Support case management (real build). A much more detailed spec arrived mid-session, explicitly requesting this be built despite the earlier "no reachable consumer" audit conclusion (Stage AJ, re-confirmed at Stage BL) — building it now on that explicit instruction. New `support_cases`/`support_case_messages` tables (`20260101010400_support_cases.sql`) following patterns already proven elsewhere this session: server-controlled requester actor (RLS validates `requester_profile_id = auth.uid()`, matching the convention used throughout rather than an auto-fill trigger), the `is_internal` message split from Stage R (customer can never post or read internal notes), a customer-only "reopen a resolved/closed case" self-service transition (every other status/priority/assignment change is staff-only), and a `claim_support_case()` atomic-claim RPC identical in shape to `claim_moderation_case()` (Stage BM) — `select ... for update`, clear conflict error, server-stamped actor, and an `audit_logs` entry for every claim. No dedicated `'support'` platform role exists and none was invented; `is_ops_staff()` gates staff access, the closest already-real, already-trusted staff concept — inventing a new role with no staff-facing UI to use it would itself be speculative. New `tests/db/support-cases.test.ts` (25 tests) covers case creation/ownership, forged-actor rejection, staff-only-field rejection, internal-note privacy, unrelated-user denial, atomic claim + audit, and reopen-preserves-history. 576/576 tests, verified on a fresh reset plus one more run without reset. |
| `7251bf1` | BN | Explainable risk signals. New `risk_signals` table (`20260101010500_risk_signals.sql`): rule-based only (an enum of concrete signal types plus a plain-language `explanation` and `rule_version`, never an opaque ML score), deduplicated one row per `(subject_profile_id, signal_type)` via `record_risk_signal()`'s upsert (a repeat increments `occurrence_count`/`last_seen_at` instead of creating a new row), advisory-only by design — no code anywhere reads this table to automatically suspend, block, or otherwise punish an account, only `mark_risk_signal_reviewed()` lets ops staff mark a signal reviewed/false-positive. No INSERT/UPDATE policy exists for any client at all, staff-only SELECT gated by `is_ops_staff()`. **Found a real bug while testing, not just a coverage gap**: the first version tried recording a signal inside `enforce_rate_limit()` immediately before its rejection `raise exception` — proven broken by actually driving a real disposable account past a real limit and finding `rate_limit_events`/`risk_signals` never grew past what earlier *successful* calls had already committed. PostgREST runs each RPC call as one transaction; an uncaught `raise` rolls back the entire transaction, including anything inserted earlier in the same call — the signal-on-rejection write could never actually persist, dead code that looked functional. Redesigned around an `AFTER INSERT` trigger on `rate_limit_events` that fires once an actor's count of *committed* (accepted) uses of a rate-limited action crosses ~75% of that action's real configured `max_count` (values read directly from each `perform enforce_rate_limit(...)` call site in `20260101008200`/`20260101008300`) — a genuinely durable, real, explainable event source. `enforce_rate_limit()` itself is left byte-identical to its original definition. New `tests/db/risk-signals.test.ts` drives 15 genuinely successful `request_transport_amendment()` calls (not 21 rejected ones) against a disposable account with a real submitted transport request, proving the threshold crossing, non-duplication, staff-only read/review, non-staff rejection, and that the flagged account's other access is completely unaffected. 589/589 tests, verified on a fresh reset plus two more runs without reset. |
| `6e941e6` | BO | Duplicate detection. Two different kinds of duplicate, two different mechanisms. (1) `animals.microchip_number` had zero uniqueness — a real chip number is a physical identifier that can never legitimately belong to two different animal rows, the same "the column's value was never validated" gap this session has repeatedly closed elsewhere. Fixed with a case/whitespace-insensitive partial unique index (`20260101010700_duplicate_detection.sql`), a hard block since this case is genuinely unambiguous. (2) A duplicate *transport request* (same requester submitting the same request twice — a slow double-click, a retried network call that actually succeeded, someone testing the flow) is fuzzy, not unambiguous — a hard block risks rejecting two genuinely different requests that happen to share values. Wired into Stage BN's `risk_signals` instead: an `AFTER INSERT OR UPDATE OF status` trigger flags (never blocks) a same-requester, same-animal-name, same-route submission within 24 hours, gated on the transition to `status = 'submitted'` specifically (not every draft edit, which would be pure noise). Needed a second migration (`20260101010600`) purely to add the new `risk_signal_type` enum value in its own transaction before consuming it — the same safe split-migration pattern this codebase already established in Stage E/F (`20260101007700`/`800`), confirmed correct by a clean `db reset`. **Found a real test-repeatability bug while writing this, the same class of lesson as Stage BN**: `risk_signals` intentionally has no DELETE policy for any client (a signal is closed via `mark_risk_signal_reviewed()`, never deleted, so staff can't quietly erase evidence of a flagged pattern) — so a test against a *shared* seeded persona left a permanent signal row with no way to clean it up, corrupting the next run's "no signal yet" assertion (caught by actually re-running the suite a second time, not just once). Fixed by switching to a disposable signed-up account, matching Stage BN's own test. New `tests/db/duplicate-detection.test.ts`. 604/604 tests, verified on a fresh reset plus two more runs without reset. |
| `87a9df2` | BP | Search read models audit. Confirmed there is genuinely no server-side search of any kind anywhere in this codebase yet: grepped every query file and route for `ilike`/`.textSearch`/`to_tsvector` — zero matches. `listPublishedPuppies()`'s `filters` param (Stage W already found this) is 100% client-side JS filtering over an unpaginated full fetch; no other list function does even that much. Considered building a denormalized search read model (a materialized view or dedicated flat table joining animals/breeds/organisations/litters for fast faceted/full-text search, CQRS-style) and rejected it now: with zero real consumers of any search index today, and the actual "server-side marketplace search contract" already scoped as its own dedicated later stage (IR-2 in the integration-readiness queue), building read-model infrastructure ahead of that stage would be exactly the "speculative infrastructure with no reachable use" pattern this session has consistently avoided elsewhere (Stage BA/BB/BE, CLAUDE.md's own rule against designing for hypothetical future requirements). Separately verified the one real thing that *would* matter regardless of read-model existence — public listing visibility — is already correctly enforced at the RLS layer, not depending on any query-side filter: `"public reads published org-listed animals"` requires `is_published` **and** a live join proving the owning organisation is `verification_status = 'approved' and is_public`, so a suspended organisation's animals already cannot leak into any query (search-read-model-backed or not) regardless of what a future search feature's query looks like. No code change; correctly deferred to IR-2 rather than duplicated under a different stage name. |
| `30a9aae` | BQ | Error taxonomy. Confirmed a real, reachable gap directly named by CLAUDE.md's own UX principle: every RPC/trigger this session has written raises with the same generic `P0001` errcode (95/95 raise sites, confirmed by grep) but a deliberately plain-language message, while real Postgres constraint violations (unique/FK/check — e.g. Stage BO's own new `animals_microchip_number_unique`) carry technical text never meant for an end user; the frontend was passing errors straight through via `toast.error(err.message)` at 69+ call sites codebase-wide with zero translation. Rewriting all 95 raise sites to distinct custom SQLSTATEs would be large, risky migration churn for what's actually needed — the message text at those sites is already customer-safe. Instead: new `src/lib/errors.ts` (`getFriendlyErrorMessage()`) passes `P0001` messages through as-is and replaces recognised technical codes (`23505`/`23503`/`23514`/`42501`/`PGRST301`/`PGRST116`) with a generic, friendly equivalent, never risking a raw constraint name reaching the screen. Wired into the two real call sites in `_public.transport.request.tsx` — the one customer-facing file explicitly carved out of CLAUDE.md's frontend-file boundary for this session. The other 69+ call sites span explicitly frontend-owned dashboard/marketplace pages outside this session's scope, correctly left alone rather than a broad unrequested frontend sweep (the shared utility is now there for whoever does that pass); internal ops/admin dashboards correctly keep showing raw technical errors per CLAUDE.md's own exemption for those surfaces. No DB/migration change — `tsc`/lint/build clean, 604/604 DB tests unaffected (confirmed, not just assumed, since the change touches a route file). |
| `30fa378` | BR | API contracts / RPC grant hygiene. Audited all 30 RPCs granted execute to `authenticated` against this codebase's own established convention: an explicit `revoke all ... from public` before the grant, not relying solely on an internal `auth.uid()`/role check — Postgres grants EXECUTE to `PUBLIC` by default on every new function, and a later `grant ... to authenticated` layered on top never actually removes that default since GRANT is purely additive. Found four RPCs missing the explicit revoke that 25+ siblings already have (`20260101010800_rpc_grant_hygiene.sql`): `approve_user_verification` (admin-gated, the same shape as `claim_moderation_case`/`execute_account_deletion`, which already had this), `get_my_profile`, `start_application_conversation`, `start_transport_conversation`. Every one's internal check was already correct and unconditional — functionally safe either way, a missing defense-in-depth layer rather than an active vulnerability — but a real, demonstrated inconsistency in contract enforcement, not a hypothetical one. Also spot-checked the hand-maintained `src/lib/supabase/types.ts` stub against a real `supabase gen types typescript --local` output: every RPC actually called from `src/` is declared and no RPC name in the stub is stale (both cross-referenced directly, not assumed); full type-fidelity reconciliation (enum-typed params like `p_new_status` are currently widened to plain `string` in the stub, and the stub has no `Enums` section at all) is real but already its own dedicated later stage (IR-5) — not duplicated here. New `tests/db/rpc-grant-hygiene.test.ts` proves anon is now rejected at the grant layer for all four, and a real authenticated non-admin caller still reaches the same business-logic rejection as before (behavior unchanged for legitimate callers). 612/612 tests, verified on a fresh reset plus two more runs without reset. |
| `3bd87b5` | BS | Webhooks (disabled) audit. Confirmed the queue's own naming ("disabled") matches reality: grepped the whole repository (`src/`, `supabase/migrations/*.sql`, `docs/*.md`, `package.json`) for "webhook" case-insensitively — zero matches anywhere except this progress log's own record of the queue description. No incoming webhook receiver route, no outgoing webhook dispatch, no webhook-related table, config, or signature-verification helper exists. This is the same shape as Stage AA (payment abstraction, also disabled) and Stage AC (outbox/email, never built): there is no external integration today that would either push events into this system or need this system to push events out — no payment provider, no email provider, no third-party service of any kind is wired up yet. Building webhook-receiver/dispatch infrastructure now, with no real endpoint or consumer to protect or call, would be exactly the "speculative infrastructure with no reachable use" pattern this session has consistently avoided (Stage BA/BB/BE/BP). No code change; correctly stays unbuilt until a real external integration (a payment or email provider) is actually approved and configured, at which point that integration's own webhook contract (signature verification, replay protection, idempotent event handling) should be built alongside it, not speculatively ahead of it. |
| `f71858f` | BT | Bulk import audit. Grepped for any existing bulk-creation/CSV-upload/batch-insert path — none exists (`createLitter()` only creates the litter record itself; every individual puppy is still added one at a time through the ordinary `animals` insert path, and no admin/ops bulk-onboarding tool exists anywhere). Checked launch-readiness docs for a documented day-one bulk-onboarding need (e.g. migrating an existing kennel's spreadsheet catalog at launch) — none found; the only "bulk" hits in `docs/*.md` are unrelated coincidental matches ("bulk-harvesting" in the PII-lockdown writeup, "bulk-read" in the MVP test report). The one plausible real candidate — letting a breeder add several puppies for one litter in a single action instead of one form submission per puppy — has no existing UI trigger at all (no "bulk add" button, no CSV widget anywhere in `dashboard.breeder.*`/`dashboard.foundation.*`), so a backend bulk-insert RPC built now would have zero real consumer and be dead code the moment it's written; building the missing UI itself is outside this session's backend/frontend-file-boundary scope. Same shape as Stages BA/BB/BE/BP/BS. No code change; candidate for whoever next builds the litter/puppy-management UI, not for this session. |
| `bddb9cd` | BU | Archival. `rate_limit_events` (Stage J) has grown unbounded since day one — every accepted rate-limited action inserts a permanent row and nothing ever removed one. Found the fix already half-designed: the original Stage J migration's own comment explicitly proposed opportunistic pruning ("Old rows are pure noise once their own window has passed... cheap enough to prune opportunistically rather than needing a scheduled job") and even added `rate_limit_events_created_at_idx` to support it, but the actual delete was never written — the same "schema/index designed for X, X never wired up" shape as a few other gaps closed earlier this session. No background job/scheduler exists (Stage BA confirmed no other reachable need for one), so `enforce_rate_limit()` (`20260101010900_rate_limit_events_archival.sql`) now deletes the calling actor's own stale rows for the same `action_key` before counting — a free byproduct of a read it already has to do, never new infrastructure, and it can never remove a row still inside its own window. New `tests/db/rate-limit-events-archival.test.ts` proves it via a very short custom window passed directly to the (genuinely arbitrary-args-callable) RPC plus a short real wait, since this suite has no service-role/direct-SQL path to backdate rows with. 617/617 tests, verified on a fresh reset plus two more runs without reset. |
| `0c8f957` | BV | Storage cleanup audit. Investigated the real orphan-file risk (a metadata row deleted while its Storage object survives, or vice versa) across all 5 buckets (`kennel-media`, `transport-documents`, `transport-evidence`, `message-attachments`, `welfare-case-documents`) rather than assuming one exists. Found no reachable path today: grepped every `.delete()` call in `src/lib/queries/*.ts` against every Storage-object-referencing table (`transport_documents`, `welfare_case_documents`, `animal_images`, `messages.attachment_url`) — zero matches, nothing in the real app ever deletes one of these rows at all yet. Every real upload flow (`submitDocument()`, driver evidence upload, message attachments, welfare documents) already does correct best-effort Storage cleanup on its own insert-failure path (`.remove([objectPath])` if the DB insert errors) — the "clean up on the way out" pattern already proven elsewhere this session. The one genuinely reachable edge case — `kennel-media`'s real `storage.objects for delete` policy lets an org owner delete a photo file directly via the Storage SDK, which no app code calls today (no "remove this photo" UI button exists anywhere), so a sophisticated user manually calling it would delete the file while `animal_images.image_url`/`organisations.logo_url`/`cover_image_url` still pointed at it — requires deliberate direct Storage-API use, not an ordinary UI action, and building a cleanup trigger for a delete path nothing in the app actually exercises would be speculative. No code change; candidate for whoever builds the first real "delete this photo/document" UI feature, at which point the deletion action itself should clean up both sides atomically from the start rather than needing a trigger retrofitted after. |
| `0622282` | BW | Health checks. Unlike Stages BA/BB/BE/BP/BS/BT/BV, this one found a real, standard gap with an obvious always-relevant consumer: no `/health` (or any liveness/readiness) endpoint existed anywhere, needed by Cloudflare's own health checks, an external uptime monitor, or a deploy-verification step — none of which can use TanStack Start's RPC-style `createServerFn` protocol, only a plain GET. Added directly to `src/server.ts` (the actual Worker `fetch()` entry point, already customized for error handling/security headers) rather than a TanStack Start file-route, since this Start version has no file-based server-route API (confirmed against the installed package's exports). Does a real DB probe, not just "the process responded" — a `head: true` count against `legal_document_versions` (Stage BK, small, always-populated, public) — returning `200 {status:"ok"}` or `503 {status:"degraded"}`, always `cache-control: no-store`. Verified for real against a running dev server: `GET /health` returns 200 with the DB check genuinely succeeding, `POST /health` correctly falls through to the normal SSR 404, ordinary pages render unaffected. Could not additionally verify against the real `wrangler dev`/Cloudflare Worker build target — wrangler in this sandbox rejects the environment's current `compatibility_date` as unsupported, a pre-existing, unrelated environment limitation. `tsc`/lint/build clean, 617/617 DB tests unaffected (no migration this stage). |
| `b316b88` | BX | Service level objectives. No production deployment or real traffic exists, so no numeric SLO (an uptime %, a latency target) can be honestly justified — a fabricated number would be worse than none, the same discipline already applied to `docs/BACKUP_AND_DISASTER_RECOVERY.md`/`docs/PRODUCTION_SETUP.md`. New `docs/SERVICE_LEVEL_OBJECTIVES.md` instead catalogs what's already measurable once real monitoring exists, using only infrastructure already built this session rather than proposing anything new: `/health` (Stage BW) for availability, `rate_limit_events` (Stage J/BU) for volume, `risk_signals` (Stage BN) for abuse-rate, `audit_logs` (Stage AE) for ops-action timing, and the backup/DR doc's own open items for data-durability. Defines SLI categories (availability, core-journey latency, error rate explicitly excluding expected `P0001` business rejections, data durability) and lists concretely what's needed before any target becomes real: a production deployment, real traffic, and an actual observability tool. Docs-only, no code change. |
| `bdf198b` | BY | Incident runbooks. New `docs/INCIDENT_RUNBOOKS.md`, five concrete scenarios built entirely from mechanisms already verified this session (not requiring real production traffic to write, unlike Stage BX's SLOs): `/health` reporting degraded (Stage BW), a specific RPC failing for all callers (the same `npx supabase db query` diagnostic pattern used throughout this session), legitimate users hitting rate limits or `risk_signals` false positives (Stage J/BN/BU — including the honest gap that there's no manual "reset this user's limit" RPC today, only the window itself elapsing via Stage BU's pruning), suspected account compromise (role suspension via `user_roles.status`, non-forgeable `audit_logs`, `execute_account_deletion()`'s unresolved-obligation checks), and a suspected private-data/document leak (RLS-first triage before assuming a wider breach, signed-URL expiry, bucket-public checks). The bad-migration case is deliberately not duplicated — already covered in `docs/BACKUP_AND_DISASTER_RECOVERY.md`'s "Incident response" section. Docs-only, no code change. |
| `42d4a20` | BZ | Maintenance mode. No mechanism existed to take the app down for a planned migration/deploy without a code change and redeploy. New `app_maintenance_mode` (`20260101011000_maintenance_mode.sql`): a single always-exactly-one-row table (`id` pinned to `true` via a `check`), publicly readable, admin-only write, `enabled_by`/`enabled_at` server-stamped on *every* update that leaves `enabled = true` (not only the transition) — closing a forgeable-actor gap a naive "only stamp on transition" trigger would have left open on a same-value update; disabling always preserves who last turned it on from `old`, never client-supplied. Enforced directly in `src/server.ts` — the real Worker `fetch()` entry point, same layer as Stage BW's `/health` — so a broken app/SSR state can never block the maintenance page itself; cached 15s per Worker isolate so normal traffic doesn't add a DB round-trip to every request. `/health` is always exempt and now also reports `maintenance:true/false`, distinguishing "intentionally down" from "actually broken." New `src/lib/queries/maintenance.ts` (matching the existing `markets.ts` admin-toggle pattern) with a real toggle wired into `dashboard.admin.settings.tsx` (admin-only, not excluded by CLAUDE.md's frontend boundary). Verified end-to-end against a real running dev server, not just `tsc`/tests: enabling via direct SQL made the homepage return the real maintenance page (503, custom message) while `/health` stayed live and correctly reported `maintenance:true`; disabling and waiting past the 15s cache TTL restored normal service, confirmed by re-checking after the wait rather than assuming the cache logic works. 625/625 tests, verified on a fresh reset plus two more runs without reset. |
| `c3321f1` | CA | Migration preflight command. Turned Stage AL's one-time manual audit into a real, reusable `npm run db:preflight` command instead of a snapshot that goes stale the moment the next migration is added — a fast, offline, static scan over `supabase/migrations/*.sql`, wired into CI's fast no-Docker job. Checks four concrete bug classes this session actually hit: (1) GRANT-vs-RLS gap — a table with RLS + policies but no matching GRANT *anywhere* in the migration history (checked globally, not per-file, since grants commonly live in a different/later file — a naive same-file-only first draft produced 44 false positives against this schema's own already-correctly-granted tables before being fixed); this exact class was found and fixed FIVE separate times this session, always caught late by a real "permission denied." (2) `NOT NULL` column added with no `DEFAULT`. (3) An enum value added via `ALTER TYPE` and used elsewhere in the *same* file — Postgres can't use a newly-added enum value in the same transaction it was added in (Stage BN hit this for real). (4) A bare `drop table`/`drop column`. Verified against the real, current 112-file migration set (clean, as expected) and unit-tested each check individually against synthetic bad SQL to confirm the detection logic actually fires, not just that the real migrations happen to pass. `tsc`/lint/build clean, 625/625 DB tests unaffected (no migration this stage). |
| `b22f6f6` | CB | Database invariant catalogue. New `docs/DATABASE_INVARIANTS.md`, complementary to `docs/DOMAIN_MODEL.md` (which describes what tables *mean*; this describes what's *guaranteed to always be true*, and exactly where). Catalogues real, currently-enforced invariants by category — uniqueness ("at most one X"), server-stamped actors (every non-forgeable actor column this session locked down, with its enforcement mechanism and migration reference), state locks ("once X, Y can never change again"), role/suspension behavior, referential/business-value constraints (currency, simulated-only fundraising, self-pruning rate limits), deletion/lifecycle rules, and visibility guarantees. Every entry cites the real migration file it comes from — nothing aspirational. Docs-only, no code change. |
| `c55ff99` | CC | Driver status state machine + chaos tests. Closed the gap Stage M/BF both found and deliberately deferred to this dedicated stage: `prevent_non_staff_operational_field_changes()` (Stage L) exempted the assigned driver entirely, so a raw-API UPDATE could set `status` to *any* enum value (skip straight to `completed`, move backwards, or set an ops-only hold state) and reassign `assigned_route_id`/`assigned_vehicle_id`/`assigned_driver_id`/`compliance_review_result`/`visibility` — none reachable through the real driver UI, real via a raw API call (`20260101011100_driver_status_state_machine.sql`). The legal transition graph isn't invented — it matches exactly what the app already treats as canonical in two places (`driverStatusSteps`, the UI's own linear sequence, and the full-journey scenario test, which additionally exercises the `rest_or_care_stop` detour the UI array skips). `veterinary_hold`/`compliance_hold` deliberately stay unreachable by a driver — no real code path sets either from that side yet. Ops staff remain completely unconstrained (Stage BF's own reasoning: the override-with-reason path is IR-8's scope). New `tests/db/driver-status-state-machine.test.ts`: every illegal jump rejected, the new field lock, the legal detour both directions, and two `Promise.all` chaos passes firing many invalid concurrent transitions from the same state — proving none sneaks through regardless of ordering. **Fixed a real regression this surfaced**: `tests/db/pickup-delivery-evidence.test.ts`'s fixture jumped `driver_assigned` straight to `delivered` in one call, only ever possible because this exact gap existed — updated to advance through the real intermediate steps first. 635/635 tests, verified on a fresh reset plus two more runs without reset (a third given the chaos tests' concurrency). |
| `f2e4490` | CD | Permission inventory. New `docs/PERMISSION_INVENTORY.md` — a role-first, practical "what can this kind of user actually do" reference grounded in real RLS/RPCs, complementing Stage CB's invariant catalogue. Covers `anon`, `customer`, `buyer`, `animal_owner`, `breeder`, `foundation_member`/`shelter_member`, `operations`, `driver`, `moderator`, `admin`, and organisation sub-roles, each entry citing the real enforcement mechanism (a specific RPC, RLS policy, or Stage CC's state machine) rather than restating every policy verbatim. Deliberately not a full table×role matrix or exhaustive automated coverage — that's IR-6's dedicated later scope. Docs-only, no code change. |
| `8743f91` | CE | Data-access consolidation. Surveyed every route file calling Supabase directly (24 files) for repeated query shapes and found real, exact duplication: two pairs of route files independently reimplemented the "get my organisation" query byte-for-byte (`dashboard.breeder.reservations.tsx`/`dashboard.breeder.transport.tsx` identical; `dashboard.breeder.tsx`/`dashboard.foundation.tsx` near-identical variants) — while `src/lib/queries/breeder.ts`/`foundation.ts` already had canonical `getMyKennel()`/`getMyKennelProfile()`/`getMyFoundation()`/`getMyFoundationProfile()` functions doing the exact same thing, unused by these four files. Also found `dashboard.breeder.settings.tsx`/`dashboard.foundation.settings.tsx` duplicated both the `get_my_profile()` RPC read and the phone-update mutation byte-for-byte with no shared function at all — added `getMyProfile()`/`updateMyPhone()` to `src/lib/queries/profile.ts` (clearly separated from that file's existing public-profile functions, which deliberately never return private fields). No behavior change — every replacement returns a superset or exact match of what was inline before. `tsc`/lint/build clean, 635/635 DB tests unaffected (no migration this stage), and a real smoke test against a running dev server confirms all six changed routes still resolve correctly (307 to sign-in, no 500s). |
| `f18668b` | CF | Tech-debt register. New `docs/TECH_DEBT_REGISTER.md` consolidates every real, currently-open item this session found and deliberately deferred — the "Known open items" section plus several per-stage deferred notes never promoted there (Stage BT's bulk-add UI gap, Stage BV's delete-photo/document feature gap) — organized by kind rather than chronology: missing features (nothing to fix, something to build on real need), known-incomplete hardening (a real fix exists but was deliberately narrower than the whole problem), and scale/UX limitations. Each item cites the stage that found it and what should trigger picking it up. Docs-only, no code change. |
| `8c98caa` | CG | Final backend PR review — second supplemental queue (BA–CF). A genuine review pass over the whole queue (61 commits, 58 files, ~4580 insertions), not a re-statement of per-stage summaries. Re-ran the full verification chain fresh: `db reset` + `test:db` ×3 (635/635 every run), `npm run db:preflight` against the real 113-file migration set, full-repo `eslint` (38/13, matching the documented baseline exactly), `tsc`, `build`, route-tree consistency. Additionally checked things only visible looking at the whole range together: no function redefined twice within this range in a way that could silently contradict an earlier stage's fix (`enforce_rate_limit()`/`prevent_non_staff_operational_field_changes()` each legitimately redefined once — read both final bodies directly to confirm), no secrets anywhere in the diff, nothing pushed, frozen frontend branches confirmed untouched. New `docs/BACKEND_PR_REVIEW_SECOND_SUPPLEMENTAL_QUEUE.md`. Verdict: clean, no blocking issues. |
| `def9b72` | CH | Second release-candidate report — closes the second supplemental queue (Stages BA–CH, 34 stages total). Verification re-run fresh: 113 migrations no duplicate prefixes, `npm run db:preflight` clean, fresh `db reset`, 635/635 tests on three consecutive runs, `tsc` clean, `build` clean, full-repo lint at the same 38/13 baseline with no new regressions across the whole range, nothing pushed, frozen frontend branches untouched. Test suite grew 576 → 635 (+59) from 14 new test files. Categorizes all 34 stages: 17 real code/schema fixes, 9 audits confirming an already-correct design or a genuinely unbuilt feature correctly deferred, 1 test-coverage-only stage, 1 new CI/tooling command (`db:preflight`), and 6 documentation deliverables. New `docs/BACKEND_RELEASE_CANDIDATE_REPORT_2.md`. **This closes the second supplemental queue.** Continuing directly into the third/fourth supplemental queue (Stage CJA onward, as far as content was actually received) and then IR-1 through IR-18, per standing instruction. |

## Integration-readiness queue (IR-1 through IR-18): stages completed

| Commit | Stage | Summary |
|---|---|---|
| `cb1ee9f` | IR-18 | Integration-ready handoff — closes the whole IR-1 through IR-18 queue. New `docs/INTEGRATION_READY_HANDOFF.md`: full fresh re-verification (clean reset, 816/816 on 3 consecutive runs, `tsc`/lint/build clean, RLS/grant/search_path/migration-prefix checks all zero-finding) plus a stage-by-stage summary table of what all 18 IR stages actually did — 11 real code/schema/config fixes (IR-1, IR-2, IR-5, IR-6, IR-7, IR-9, IR-10, IR-11, IR-12, IR-13, IR-16), 4 confirmed-already-correct audits (IR-3, IR-4, IR-8, IR-17), 3 documentation-only deliverables (IR-14, IR-15, IR-18). Records known blockers (no production Supabase project, no email provider, frontend integration itself not done, 21/13 lint debt) and the explicit next action: begin the XR-1 through XR-24 queue at XR-1, per its own stated precedence. No code, migration, or test change this stage. Worktree clean. |
| `453599d` | IR-17 | Final backend PR review. New `docs/BACKEND_FINAL_PR_REVIEW.md` — a targeted, evidence-based review across security/races/idempotency/audit/Storage/errors/frontend-conflict (not a literal re-read of 217+ commits, impractical at that volume; a genuine targeted pass instead). Re-audited all 5 Storage buckets for the tamper-after-decision bug class IR-11 closed for `welfare-case-documents` — `transport-documents`/`transport-evidence` already correctly insert-only for their non-ops uploader (no gap, stricter than welfare-case-documents was pre-IR-11), `message-attachments`/`kennel-media` have no equivalent "decision" concept to protect. Re-ran IR-13's `SECURITY DEFINER` grant/search_path audit against every migration added since — still clean. Spot-checked this session's own newest write paths (IR-10's notification dedup reuse, IR-12's advisory-only batched read, IR-11's MVCC-safe status-gated lock) for races/idempotency — none found. Confirmed no new state-changing action this session added skipped an existing audit-trail path. Confirmed no new silent-error-swallowing was introduced. Frontend conflict not re-litigated (already IR-14/IR-15's dedicated subject). No new issue found beyond what IR-10 through IR-16 already fixed in real time — a genuine "reviewed, confirmed correct" outcome, the same honest shape as IR-3/IR-4/IR-8. No code, migration, or test change. |
| `f4295d6` | IR-16 | Release manifest. New `docs/BACKEND_RELEASE_MANIFEST.md` — every figure generated by actually running the command next to it against the current commit, not carried over from memory. **Found and fixed a real gap while gathering the lint-baseline figure**: this session's repeatedly-repeated "38-error/13-warning" full-repo lint baseline (first claimed at Stage K, re-asserted unverified in commit messages ever since) was stale — a true, literal `npx eslint .` run for this manifest reported **4499** errors, 4478 of them from a single file: `src/lib/supabase/types.ts`, the real `supabase gen types` output that replaced a hand-written stub at Stage IR-5, which `eslint.config.js` never excluded from linting despite being entirely externally-generated (comparing generator output against a prettier config it was never written to match). Every later stage's "lint changed files only" checks correctly never re-triggered this — it was only ever visible to a genuinely unscoped full-repo run, which nothing had actually re-done since IR-5. Fixed by adding `src/lib/supabase/types.ts` (and, defensively, `src/routeTree.gen.ts`, which currently passes but is generated by a separate external tool the same way) to `eslint.config.js`'s `ignores`, the same "never hand-edit/format-police a generated file" principle CLAUDE.md already states for the route tree. Real, now-verified baseline going forward: **21 errors / 13 warnings**, all pre-existing, all in real authored files, all previously-known low-priority debt. Also corrected two small inaccuracies caught while re-verifying `git diff` details for the manifest, in Stage IR-14's already-committed conflict map: `src/lib/i18n/index.tsx`/`locales/*.json` are not new (they exist at the merge-base and are already actively imported by `main` today — the frozen branch substantially extends them, not introduces them), and `__root.tsx`'s frozen-branch diff is SEO meta-copy only, not i18n-context wiring as an earlier draft guessed. `tsc`/build re-verified clean; 816/816 tests unaffected (config-only change, re-run once as a sanity check, not the full 3x contract since nothing runtime/security changed). |
| `8c34eea` | IR-15 | Integration runbook. New `docs/FRONTEND_INTEGRATION_RUNBOOK.md`, a concrete plan (not executed) for merging the frozen `ux-marketplace-frontend-pass` branch into `main`, built directly on IR-14's real findings rather than a generic checklist: isolate the work in a dedicated `git worktree add` + new branch (never `main` directly, never the frozen worktree); recommends one real `git merge --no-commit` over 52 individual cherry-picks given IR-14 found only 3 real conflicts (documents the ordered-cherry-pick alternative too, since it was explicitly asked for, with the concrete `git log --reverse` command for it); per-conflict resolution guidance naming the exact functions/files from IR-14's map, including the quotation-expiry-gate re-port that conflict needs; a regeneration step for `routeTree.gen.ts` and a `types.ts`/`tsc` drift-budget step tied to IR-14's Stage-IR-5-predates-freeze finding; an ordered test/build gate list (`db reset`, `test:db` ×2, the frozen branch's new `test:unit`, `tsc`, `lint` against the documented baseline, `build`, then commit); a browser-QA checklist scoped to the actual pages IR-14 flagged as at-risk (not a full 75-file sweep); and a non-destructive rollback (`git worktree remove` + `git branch -D`, `main`/frozen branch untouched at every step before the final commit). No worktree created, no merge attempted, no writer stopped — plan only, per the stage's own definition. No code, migration, or test change. `tsc`/build re-verified clean (nothing touched). |
| `75596f3` | IR-14 | Frontend integration conflict map. Read-only comparison of `main` against the frozen `ux-marketplace-frontend-pass` branch — new `docs/FRONTEND_INTEGRATION_CONFLICT_MAP.md`. Never entered/checked out the frozen worktree; used `git diff`/`git merge-tree --write-tree` (a real, in-memory-only hypothetical merge, not a guess from diff stats)/`git cat-file`/`git merge-file` against a scratch directory. Found exactly 3 real content conflicts among 75 changed files, all reconciled with concrete evidence, not assumption: (1) `src/lib/queries/marketplace.ts` — both sessions independently fixed the identical litter/breeder N+1 pattern with differently-shaped batching helpers, and the frozen branch adds real `mapOrgToFoundation()`/`orgAvailableAdoptionCounts()` with zero equivalent on `main`; (2) `src/lib/queries/buyer-activity.ts` — downstream of #1, frozen branch's foundation/breeder-following split is a strict improvement to adopt; (3) `src/routes/dashboard.buyer.quotations.tsx` — the one conflict with a real regression risk if resolved carelessly: the frozen branch's i18n rewrite of this page has **zero quotation-expiry awareness**, always rendering a clickable Accept button the RLS layer (Stage IR-9) would reject — recommends re-porting the `isExpired` gate into the frozen branch's newer version, not picking either side wholesale. Also documented: 6 other both-sides-touched files that auto-merge cleanly (`package.json`, `profile.ts`, `routeTree.gen.ts` — flagged to regenerate post-merge regardless, never trust a merged generated file — plus 3 route files); confirmed zero filename overlap in `src/components/` between the two sessions; and a real, currently-invisible risk the conflict count misses entirely: the frozen branch never touches `src/lib/supabase/types.ts`, so it still assumes the pre-Stage-IR-5 hand-written stub shape, meaning integration will likely surface new TypeScript errors (the same class Stage IR-5 already fixed 37 of on `main`'s own side) that a clean `git merge` wouldn't warn about. No code, migration, or test change — a genuine read-only mapping stage. `tsc`/build re-verified clean (nothing touched). |
| `7b9a5f4` | IR-13 | Migration rehearsal. Full clean-slate rehearsal (empty DB → all 128 migrations → seed → RLS/grant/search_path/Storage/types audits → `tsc`/build/tests) — new `docs/MIGRATION_REHEARSAL_REPORT.md` has the full checklist. Auditing every `SECURITY DEFINER` function's grants (not just `search_path`, already clean) found a real, previously undiscovered information-disclosure gap: `has_role(p_user_id uuid, p_role platform_role)` — unlike every other role-check helper in this schema, which only ever answers about the calling user — was never explicitly revoked from `PUBLIC`, so any caller (including anonymous, via PostgREST's auto-exposed `/rpc/has_role`) could probe an arbitrary real profile id and learn whether that specific person holds any given platform role, a role-membership enumeration oracle useful for targeting staff accounts. **Caught a real mistake in my own first-draft fix before committing it**: assumed every call site routes through a `SECURITY DEFINER` wrapper (safe from a bare revoke) and that `has_role()` was never called directly from an RLS policy — running the full test suite immediately proved that wrong: two policies (`transport_documents` and `storage.objects`, both from `20260101009800_driver_id_checks_active_role.sql`) call `has_role()` directly inline. Real fix in `20260101012600_has_role_execute_lock.sql`: added `is_active_driver()`, a no-arg `SECURITY DEFINER` wrapper matching `is_admin()`/`is_moderator()`'s shape, and pointed both policies at it instead; `has_role()` itself stays fully revoked from `PUBLIC`. New `tests/db/has-role-execute-lock.test.ts` proves anon and ordinary authenticated callers are both now blocked from probing `has_role` directly, while the internal wrapper chain (`is_admin()`, `is_active_driver()`) still works correctly. `src/lib/supabase/types.ts` regenerated (only diff: the new `is_active_driver` RPC — no undetected drift from any earlier stage). 816/816 tests, verified on a fresh reset plus two more runs without reset (security-grant/RLS-policy-changing stage), `tsc`/build clean, 128 migrations, no duplicate prefixes. |
| `e005868` | IR-12 | Representative volume and performance. Auditing `src/lib/queries/*.ts` for N+1 patterns (loops issuing a query per item, distinct from the many already-fine `Promise.all` calls batching a small fixed number of independent named queries) found a real one in `src/lib/queries/matching.ts`: `suggestRoutesForRequest()` calls `computeMatch()` once per candidate route, and `computeMatch()` used to query `route_assignments` once per route (a `.eq("route_id", route.id)` head-count) to compute remaining capacity — parallelised via `Promise.all`, but still one DB round trip per candidate route every time an ops user selects an unmatched request on `dashboard.operations.matching.tsx`, scaling linearly with how many routes are in planning/confirmed. Fixed by replacing the per-route query with `fetchActiveAssignmentCounts()`, one `.in("route_id", routeIds)` query for every candidate route at once, grouped into a `Map` client-side (PostgREST has no GROUP BY) — `computeMatch()` now takes the pre-computed count as a parameter instead of querying itself, identical math (`max(0, max_capacity - activeAssignmentCount)`), zero behavior change. No migration (query-shape-only change, no schema/RLS touched). `matching.ts` can't be imported directly by `tests/db` (it pulls in the browser Supabase client via the `@/` alias, unresolvable under plain `node --test` — confirmed no other `tests/db` file actually imports a real query module for this reason, despite several files' header comments referencing one), so the new `tests/db/matching-capacity-batch.test.ts` instead proves the real thing that changed directly against the database: the new batched-by-route query returns exactly the same active-only counts (cancelled assignments correctly excluded) as the old per-route query did, for multiple real routes at once. Reformatted the whole file with `prettier --write` while already touching it, since `eslint` surfaces the file's pre-existing (not newly introduced — confirmed identical against `HEAD`) formatting debt as soon as any change lands in it; no logic changed beyond the capacity fix. 808/808 tests (+4 from IR-11's 804), verified on a fresh reset plus two more runs without reset, `tsc`/lint/build clean, no new migration. |
| `673c796` | IR-11 | Storage and signed URL security. Inventoried all 5 private/public Storage buckets (`kennel-media` public, `transport-documents`/`transport-evidence`/`message-attachments`/`welfare-case-documents` private) for the "org can tamper with evidence after ops already acted on it" bug class Stage AP closed for `transport_documents`. Found the same gap, previously unclosed, for welfare cases: `welfare_cases` itself already locks organisation-side edits to a fixed editable window (draft/submitted/information_required), but both `welfare_case_documents` (the metadata table) and the `welfare-case-documents` Storage bucket used a single unrestricted `for all` policy with no such window — an org member could insert, overwrite, or delete supporting evidence at any time, including after ops accepted/declined the case based on that exact evidence. Not reachable through the real UI (`uploadWelfareCaseDocument()` only ever inserts), but real via a raw API/Storage call, the same severity tier as every other self-approval-class fix this session has made. Fixed with `20260101012500_welfare_case_document_lock.sql`: split each `for all` policy (on both the table and `storage.objects`) into SELECT (always allowed — viewing was never the risk) and INSERT/UPDATE/DELETE (restricted to the same editable window `welfare_cases`' own update policy already uses); ops keeps its separate unrestricted `for all` policy on both layers, unchanged. `claim_moderation_case()`'s row-lock and the notification dedup layer (both examined for "stale locks"/"concurrent workers") were already thoroughly soak-tested at Stage IR-10 and needed no further work — `claim_moderation_case()` deliberately has no lease/TTL since the broader moderator `for all` policy already gives a permanent admin override. New `tests/db/welfare-case-document-lock.test.ts` proves the full boundary: uploads/edits succeed while editable, view/signed-URL access stays open forever, writes (table insert/update/delete, Storage upsert/delete) are blocked once locked, and ops retains full access throughout. 804/804 tests (+14 from IR-10's 790), verified on a fresh reset plus two more runs without reset (RLS/Storage-policy-changing stage, per the standing third-run rule), `tsc`/lint/build clean, no duplicate migration prefixes (127 total), no new SECURITY DEFINER functions to audit (policy-only migration). |
| `4ee25d0` | IR-10 | Outbox and notification soak. No background job/worker/outbox system exists anywhere in this app (repeatedly confirmed: Stage BA, Stage AC/CJO for email, Stage BU for `rate_limit_events` archival) — the real analog for "retries, dedup, stale locks, concurrent workers" is the notification dedup/preference layer (CJR/CJS) and `claim_moderation_case()`'s row-lock claim, both already thoroughly soak-tested (10-way concurrent race, replay-preserves-original-text, mandatory `security` category, muted-category suppression). `claim_moderation_case()` has no lease/TTL by design — not a bug, since the broader `is_moderator() for all` policy already gives any moderator/admin a permanent override, so there's no real "stale lock" to recover from. Auditing every real notification producer for completeness (the concrete, reachable form of "soak" this app actually has) found one genuinely missing entirely: `review_moderation_appeal()` (Stage G) never notified the appellant of the outcome — upheld or overturned — leaving them with no way to learn their appeal was resolved short of manually revisiting their case page, unlike the other 3 producers (rehoming decision, application status change, moderation decision) which all notify their affected user. Closed by adding a 4th template (`moderation_appeal_decision` in `src/lib/notification-templates.ts`) and `notifyAppellantOfAppealDecision()` (`src/lib/queries/moderation.ts`), wired into `dashboard.admin.moderation.tsx`'s existing `reviewAppeal` mutation as a second call after the RPC succeeds — the same established "client makes a second call after the write succeeds" shape every other producer already uses, not a new pattern. No migration needed (reuses `create_notification_if_enabled()` as-is). New tests: a template-purity test (`notification-template-versioning.test.ts`) and a full regression in `moderation-appeals.test.ts` proving the real end-to-end shape — `review_moderation_appeal()` itself still creates no row, the follow-up call creates exactly one for the appellant, a retry doesn't duplicate it, and muting the `moderation` category suppresses it. 790/790 tests (+8 from IR-9's 782), verified on three consecutive full runs with no reset between (retry/dedup-touching stage, per the standing third-run rule), `tsc`/lint/build clean. |
| `5cc520f` | IR-9 | Document expiry jobs. Auditing every `expiry_date`-bearing column across the schema found `transport_documents`/`vehicles.insurance_expiry_date`/`drivers` already have a real, deliberate, already-wired client-computed expiry warning (`documentExpiryWarning()` in `transport.ts`, `expiryWarnings()` in `fleet.ts`, wired into 3 real ops pages) — the same "no background job exists anywhere in this app, compute staleness on read" pattern this session confirmed at Stage BA. `quotations.expiry_date` was the one genuine, unaddressed gap: "requesters accept or reject their own quotation" RLS only ever checked the target status was accepted/rejected — nothing stopped a customer accepting a quotation whose `expiry_date` had already passed, server-side or in the UI (which only ever showed the raw date as plain text, "Valid until ...", even once it was in the past). **Deliberately distinguished from Stage IR-8's scheduling conflicts** (a genuinely advisory-only design this session just re-learned to respect) — there is no existing product decision anywhere saying customers should be able to accept stale price commitments, so this is closed as a real fix, not reverted. Extended the RLS policy (`20260101012400_quotation_expiry_enforcement.sql`): rejecting an expired quotation stays allowed (declining a stale offer is always harmless), only the transition *to* accepted is blocked once `expiry_date` has passed; a null `expiry_date` is unaffected. Reused `documentExpiryWarning()` directly (no new duplicate function) in `dashboard.buyer.quotations.tsx` to show a clear "This quote expired on ..." message and hide the Accept button once expired — verified against a live dev server, zero console errors. New `tests/db/quotation-expiry-enforcement.test.ts` proves the reject-still-allowed/accept-blocked/null-unaffected matrix. 782/782 tests (+13 from IR-7's 769 — IR-8 made no code change), verified on a fresh reset plus one more run without reset. |
| `92ec206` | IR-8 | Scheduling and capacity conflicts audit — already correct, no fix needed (a real, caught-before-committing false start). Initial read of only `supabase/migrations/*.sql` found `assign_request_to_route()` never checks `routes.max_capacity`, and no DB constraint stops the same `driver_id`/`vehicle_id` being assigned to two routes on the same `departure_date` — drafted and applied a migration (unique partial indexes + an RPC capacity check) and a new test file on that basis. Running the full suite surfaced real regressions in the *existing* `tests/db/calendar-scheduling.test.ts`, whose own header comment cites "Stage B" and explicitly tests that double-booking rows **must** be insertable (`"scheduling conflict data: driver/vehicle double-booking is genuinely queryable"`). Reading `src/lib/queries/calendar.ts` in full (not just migrations) revealed a real, comprehensive, already-built, already-wired `getSchedulingConflicts()` (live in `dashboard.operations.calendar.tsx`) that deliberately treats every one of these exact situations — driver double-booking, vehicle double-booking, a request assigned to multiple routes at once, a closed request still holding a route, capacity overrun — as **advisory conflicts surfaced to ops staff for manual resolution**, not conditions to hard-block at the database/RPC layer. This is a real, deliberate, well-reasoned product design (ops needs the ability to temporarily over-assign while re-planning a route, then resolve the flagged conflict), and my draft fix would have been a direct regression against it. Reverted both the migration and the test file cleanly before ever committing (`git status` confirms a clean worktree) — the real lesson: search the actual TypeScript query layer for "conflict"/"capacity"/"scheduling" logic, not just migration files, before concluding a gap exists. No code change — a genuine "already correct by deliberate design" outcome, caught by this session's own testing discipline before it ever reached the repository. |
| `66acbbb` | IR-7 | Role/suspension matrix. The existing suspension test (`tests/db/workflows.test.ts`, extended repeatedly since Stage 6100) already covered ops, driver, kennel-owning breeder, foundation-owning member, shelter-owning member (ad-hoc fixture), non-owner member, and admin — but every test in this whole session exercising `is_moderator()`-gated access (reports/moderation_cases/appeals) does so as `admin`, which also always satisfies `is_moderator()` via its own `or is_admin()` clause. That means the *moderator* half of that OR — and specifically whether suspending a `moderator` role actually revokes moderation-case access — had zero coverage; confirmed no seeded user anywhere in `supabase/seed.sql` even has the `moderator` role. `has_role()` (checking `status = 'active'`) is the same shared helper already proven correct for every other role in this exact file, so this was expected to already work — and it does, a genuine coverage gap, not a bug, the same "already correct, just untested" shape as Stage BH's transport_incidents finding. New ad-hoc-fixture subtest (matching the file's own established shelter_member/foundation_member pattern exactly) grants a disposable moderator role to `breederPending`, creates a real `moderation_cases` row, proves active management works, suspends the role, proves it's blocked. 769/769 tests (+5 from IR-6's 764), verified on a fresh reset plus one more run without reset (no migration this stage, pure coverage addition). |
| `e8170ba` | IR-6 | Complete product scenarios. Writing a genuine end-to-end test for "buyer applies → breeder approves → reserved" (the exact pipeline `docs/PRODUCT_VISION.md`'s own priority hierarchy names — "applications/reservations/adoption/handover") surfaced a real, significant gap, not just a missing test: `reservations` (`20260101001100_reservations.sql`) has a complete, already-correct RLS contract ("org owners create reservations from approved applications," including the exact "must come from an approved application for this animal" check), but zero code path anywhere could ever actually create one — `reservations.ts` only ever had read functions, and the one real application-review UI (`dashboard.breeder.applications.tsx`) only offered approve/reject, with `applicationStatusLabels`' own `converted_to_reservation: "Reserved"` entry dead code nothing could ever reach. The exact "schema/RLS built, never wired" shape this session has repeatedly found (Stage BG's evidence_url, Stage S's message attachments), this time for the single most product-critical step in the whole commercial hierarchy. Closed with a new `convert_application_to_reservation()` RPC (`20260101012300_convert_application_to_reservation.sql`) doing the reservation insert, the application status flip, and the animal's public availability update atomically in one transaction — the same non-atomic-multi-write risk this session has closed for every other multi-table write (`advance_transport_job_status`, `assign_request_to_route`, `claim_moderation_case`); rejects a non-approved application, an unrelated organisation, and a duplicate conversion attempt, all server-side. Wired into a minimal, real "Mark as reserved" action on the existing approved-application detail sheet — verified against a live dev server (real screenshot: the button renders correctly on a real approved application). New `tests/db/application-to-reservation-scenario.test.ts` proves the full journey end-to-end in one place, not just the RPC in isolation. 764/764 tests (+12 from IR-5's 752), verified on a fresh reset plus two more runs without reset. |
| `293648d` | IR-5 | Generated type reconciliation. Confirmed `npm run db:types` genuinely works in this sandbox now (previously uncertain for a long stretch of this project's history, correctly documented as such — but that constraint stopped applying long before anyone corrected the note) and ran it for real: `src/lib/supabase/types.ts` goes from a 1760-line hand-written stub (deliberately widened to plain `string` for every enum column/RPC arg, a known tracked gap since Stage BR/CD) to 6165 lines of complete, accurate, generated types with a real `Enums` section covering all 75 real Postgres enum types — the stub is retired for good, not just patched. Surfaced 37 real (not hypothetical) type errors across 15 files, each a place the app silently relied on a widened `string` where the database actually enforces a specific enum value, or passed `null` where a real generated RPC arg wants `undefined`. Fixed all 37: array literals passed to `.in()` needed `as const`; several function params tightened from `string` to a real enum type (new `src/lib/supabase/enums.ts` holds reusable aliases like `TransportStatus`, kept in a separate hand-written file specifically so it survives future `db:types` regenerations); `null`→`undefined` at RPC call sites; two view-sourced nullable columns that are never actually null in practice handled with explicit filters/fallbacks instead of widening the consuming type; three UI components with a live `<Select>`/form field cast at the one real dynamic-user-input boundary (the actual option values are always valid, TS just can't statically prove a runtime string equals a literal union member). Every touched file is legitimately in scope — none is a frontend-owned file this session avoids. `tsc`/lint/build clean, 752/752 DB tests unaffected (no migration), and verified against a live dev server across every touched page (marketplace, litters, planned routes, transport request, and — signed in as ops — requests list, quotations, dispatch, request detail) with zero new console errors. Also corrected two long-stale claims in `docs/DECISIONS.md` ("this sandbox can't run Docker," "nothing has run against a live database yet") that had been wrong for the vast majority of this session without anyone catching it, and closed two `docs/TECH_DEBT_REGISTER.md` items (enum fidelity, marketplace search) this session's own work (IR-2, IR-5) had already resolved. |
| `e72d961` | IR-4 | Frozen frontend compatibility check — read-only, no gap found. `git diff main...ux-marketplace-frontend-pass -- src/lib/queries/ src/lib/supabase/types.ts src/routeTree.gen.ts package.json` (never `git checkout`, per the standing frontend-freeze rule) shows the frozen branch touches `buyer-activity.ts`, `community.ts`, `marketplace.ts`, `profile.ts`, `routeTree.gen.ts`, `package.json`. Extracted every real `.from(...)`/`.rpc(...)` call in the frontend's own versions of those files: `saved_animals`, `follows`, `posts`, `reactions`, `comments`, `profiles`, `organisations` — every one of these tables (and `posts` specifically, whose schema hasn't been touched by any migration since its original creation, `20260101001900_community.sql`) is unchanged and stable in the current backend. **Directly verified the one highest-risk overlap**: the frozen branch's own `listPublishedPuppies(filters?: { breed?: string; country?: string })` has the *exact* signature IR-2 extended — since every field IR-2 added (`sex`, `priceMin`, `priceMax`, `page`, `pageSize`) is optional and the return type is unchanged, the frozen branch's own calls remain fully type- and behavior-compatible with today's backend, confirmed by reading the frozen branch's actual source, not assumed from the general "additive change" reasoning alone. `package.json`'s 2-line diff (frontend adds `test:unit`/`i18n:check` scripts) inserts at a different point in the file than this session's own additions (`db:preflight`, `config:check`) — non-overlapping, low merge-conflict risk, though an actual test-merge to confirm git's exact auto-merge behavior would itself be a merge action, correctly not performed here. No code change — a genuine, verified "still compatible" outcome; full frontend integration/merge itself stays explicitly out of this session's scope. |
| `b74265d` | IR-3 | Public read-model privacy audit — revalidation, no new gap found. Substantially overlaps Stage CJB's own column-by-column audit of all 7 public views (`public_transport_requests`, `public_routes`, `public_fundraising_contributions`, `public_fundraising_totals`, `public_transport_rating`, `driver_transport_job_view`, `my_moderation_case_view`) — rather than repeating that full column-level pass, verified what's changed since via `git log`/`git diff` directly: (1) zero `alter table ... add column` statements touching any anon-selectable table (`animals`, `organisations`, `breeds`, `litters`, `parent_dogs`, `posts`, `comments`, `reactions`, `groups`, `markets`, `pricing_rules`, `legal_document_versions`, `legal_requirements`, `transport_operator_authorisations`, `fundraising_campaigns`, `rehoming_reviews`, `app_maintenance_mode`, and more) across every migration since CJB's own commit; (2) zero `create or replace view`/`create view` touching any of the 7 public views at all in that same range — the exact column projections CJB verified column-by-column are still byte-identical today. Every table newly created since CJB (`legal_holds`, `support_cases`, `support_case_messages`, and the new `notifications.dedup_key`/`template_version` columns from CJR/CJS) has zero `anon` grant and appears in no public view, so none of this session's newer work introduced a new public exposure surface. Also checked IR-2's own new `countPublishedPuppies()`/server-side filters specifically: a `head: true` count query returns no row data regardless of embed shape, and PostgREST's `!inner` embed syntax only changes join type (inner vs left), never bypasses the underlying RLS policy still filtering which rows are visible — no new leak path from that stage either. No code change — a genuine, verified "still correct" outcome. |
| `737de44` | IR-2 | Server-side marketplace search contract. Explicitly deferred to this dedicated stage by Stage BP's own earlier audit ("already scoped as its own dedicated later stage (IR-2)... building read-model infrastructure ahead of that stage would be speculative"). `listPublishedPuppies()` fetched every published row unconditionally and filtered breed/country in memory (Stage BP's own finding) — extended with genuine server-side filtering (breed, country, sex, price range) and optional `page`/`pageSize` via `.range()`, plus a new `countPublishedPuppies()` for the paired total count a future paginated search UI would need (kept as a separate lightweight query, matching the existing `countAnimalsByStatus`/`mapLitterRows` split already in this file, rather than changing the main function's return shape). Fully additive: every real call site (`_public.index.tsx`, `_public.find-your-dog.tsx`, `_public.find-a-dog.tsx`) calls this with no arguments and gets back the same plain array as before — verified against a live dev server with Playwright, not just `tsc`, since this touches a function 3 real pages depend on (all 3 rendered real puppy data, zero console errors). **Found and fixed a real correctness bug in my own first draft before committing it**: filtering a joined column (`breeds.name`, `organisations.country`) with a plain `.eq()` only excludes non-matching parent rows when that relation is marked `!inner` in the select string — without it, PostgREST silently returns every row with the embedded object simply `null` instead of narrowing the result set at all (confirmed empirically against the live local instance: an `.eq("breeds.name", "X")` filter without `!inner` returned all 6 published puppies, not just matches). Fixed by adding `!inner` only to the specific relation actually being filtered (never unconditionally — `breed_id`/`organization_id` are both nullable, so an unconditional `!inner` would silently drop any listing with no breed set from an otherwise-unfiltered query). New `tests/db/marketplace-search-contract.test.ts` proves the `!inner` requirement directly (a filter without it demonstrably returns every row unfiltered), server-side price/sex filtering, and that `.range()`-based pagination is stable and gapless across pages (run 4 times total to rule out ordering flakiness). 752/752 tests (+9 from IR-1's 743), verified on a fresh reset plus two more runs without reset. |
| `b3a7efd` | IR-1 | Backend/frontend contract snapshot. New `docs/BACKEND_API_CONTRACT_SNAPSHOT.md` — exactly what the Data API exposes today, queried directly from the live instance's `information_schema`/`pg_proc` (not transcribed from migration files or memory, the same standard this session applies everywhere): every table's real SELECT/INSERT/UPDATE/DELETE grants to `anon`/`authenticated`, every RPC's real signature (35 RPCs, all `authenticated`-only, none callable by `anon`), every public view. Deliberately narrower than `docs/PERMISSION_INVENTORY.md` (role-first) — this is the literal verb-level Data API surface PostgREST evaluates RLS against, not a role-capability narrative. Explicitly not duplicating XR-17's later "public contract drift scanner" stage — this is the snapshot that tool would later diff against, not the diffing tool itself. **Found and fixed a real gap while cross-referencing this snapshot's RPC list against the hand-written `src/lib/supabase/types.ts` stub** (via `comm` against two real name lists, not eyeballed): `require_recent_auth()`/`last_auth_at()` (Stage CJN) were granted to `authenticated` but never added to the stub — neither is called from `src/` app code directly (both are used internally by other SECURITY DEFINER functions), so not a live bug, but real, demonstrated staleness against the actual grant surface, matching the existing convention of stubbing every granted RPC (`enforce_rate_limit` was already stubbed on the same basis despite no direct app-code caller either). `tsc`/lint/build clean, 743/743 DB tests unaffected (no migration this stage). |

## Post-XR-24 follow-up work

The assigned XR-1 through XR-24 queue closed at XR-24 (`164164e`/`3157358`). Work continuing after
that point picks up the "known open items" XR-24's own closeout report carried forward — starting
with the highest-priority one, XR-7's documented atomic-RPC-conversion backlog.

| Commit | What | Summary |
|---|---|---|
| `cfd33ca` | Transactional workflow boundaries follow-up 1 | Converted the top 3 of XR-7's 6 documented candidates (`respondToQuotation`, `sendQuotation`, `assignDriverToJob`) into atomic, actor-stamped, idempotent-retry `SECURITY DEFINER` RPCs (`20260101013400_quotation_dispatch_atomic_rpcs.sql`). Full detail, including a real previously-invisible customer-acceptance bug found and fixed via the new tests, in `docs/TRANSACTIONAL_WORKFLOW_BOUNDARIES_FOLLOWUP_1.md`. New `tests/db/quotation-dispatch-atomic-rpcs.test.ts` (29 tests). 934/934 full suite (+29), verified on a fresh reset plus one more run without reset. `tsc`/lint/build/`db:preflight` clean, `db:contract-check` baseline regenerated (70 tables, 39 RPCs, +3) and re-verified clean, 136 migrations, no duplicate prefixes. `db:schema-drift` could not run this session — shadow-database provisioning failed under this sandbox's memory pressure (confirmed via `free -h`, not a code issue); re-run in a future session with more headroom. `createTransportRequest`, `approveRehomingReview`, `escalateReportToCase` remain open, per XR-7's own priority order. |
| `34d3768` | Transactional workflow boundaries follow-up 2 | Converted the remaining 2 good-fit candidates (`approveRehomingReview`, `escalateReportToCase`) into atomic RPCs (`20260101013500_rehoming_report_atomic_rpcs.sql`). `escalate_report_to_case()` also closes a real duplicate-case risk that had zero DB-level protection (`moderation_cases.report_id` has no unique constraint) via the same idempotent-retry pattern as elsewhere this session. Full detail in `docs/TRANSACTIONAL_WORKFLOW_BOUNDARIES_FOLLOWUP_2.md`. New `tests/db/rehoming-report-atomic-rpcs.test.ts` (14 tests). 948/948 full suite (+14), verified on a fresh reset plus one more run without reset. `tsc`/lint/build/`db:preflight` clean, `db:contract-check` baseline regenerated (70 tables, 41 RPCs, +2) and re-verified clean, 137 migrations, no duplicate prefixes. `db:schema-drift` still could not run — same shadow-database provisioning failure reproduced even with more free memory available this time, confirming it's a sandbox-specific Docker issue independent of memory pressure, not a code issue. Only `createTransportRequest` remains open from XR-7's original 6, deliberately deferred (reasoning in follow-up 1). |

## XR queue (XR-1 through XR-24): stages completed

| Commit | Stage | Summary |
|---|---|---|
| `164164e` | XR-24 | Six-hour closeout report — the final stage of the XR queue. New `docs/XR_QUEUE_CLOSEOUT_REPORT.md`. Every figure re-verified fresh at this exact commit immediately before writing it (fresh reset, 2 consecutive `test:db` runs, `tsc`, full-repo `eslint`, `build`, `db:preflight`, `db:contract-check`, `db:schema-drift`), not carried over from memory — 905/905 tests, 21/13 lint baseline confirmed to match exactly, no contract or schema drift, clean `git status`. Confirmed the frozen frontend worktree remains untouched at its own last commit (`727d551`), working tree clean. Summarised (not duplicated) what XR-1 through XR-24 did, consolidated the 3 known-open-items already documented by XR-7/XR-17/XR-21 into one place, and confirmed every standing rule (no frontend worktree touch, no push/deploy, no weakened test/RLS, no rewritten migration, no hand-edited generated file, no invented stage, no restarted/duplicated work) was honoured throughout. No migration, no app/schema code change. |
| `a706464` | XR-23 | Final adversarial matrix. New `docs/ADVERSARIAL_MATRIX.md`, tests added to `tests/db/organisation-team.test.ts`. By this stage 63 test files already give most of the schema's 34 public RPCs real adversarial coverage — rather than re-describing that as a fresh "matrix," cross-referenced every RPC in the XR-18 contract baseline against every `tests/db/*.ts` call site and found exactly 2 with zero test coverage at all: `change_org_member_role` and `decline_org_invitation` (a third, `last_auth_at()`, checked and ruled out — `security invoker`, no parameters, no other-user surface to attack). Both are real, live, currently-wired; reading their definitions found the underlying logic already correct, but `change_org_member_role`'s genuine self-escalation-prevention logic (an administrator cannot promote themselves or a peer to owner/administrator) had never actually been exercised — a regression there would have gone completely unnoticed. New tests prove: a non-manager has zero role-change capability; an administrator cannot escalate themselves to owner or promote a volunteer straight to administrator, but can move someone between two non-privileged tiers; only the true owner can touch an administrator-tier row; a wrong-email user cannot decline someone else's invitation (leaves it untouched); a bogus token is rejected; the correct invitee can decline once, producing a real audit-log entry, and not twice. Found and fixed a real bug in the new test itself while running it: querying `audit_logs` as the org owner instead of admin, correctly blocked by RLS (admin/ops-only reads) — fixed to match the pattern used everywhere else in the suite. No migration this stage. 905/905 tests (+14 from XR-22's 891), verified on a fresh reset plus one more run without reset, `tsc`/lint/build/`db:preflight` clean, 135 migrations unchanged, no duplicate prefixes. |
| `cb07d46` | XR-22 | Backend integration manifest. New `docs/BACKEND_INTEGRATION_MANIFEST.md` — the current, regenerated "what's true right now" reference. Earlier point-in-time closeout docs (`BACKEND_RELEASE_MANIFEST.md`, `INTEGRATION_READY_HANDOFF.md`, `BACKEND_RELEASE_CANDIDATE_REPORT.md`/`_2.md`) are each a real, valuable snapshot of a completed queue, but by this stage every one cites stale figures (the earliest: 98 migrations/490 tests; this repo is now 135/891) — none of them was ever meant to stay current, so rather than adding a fifth stale-on-arrival snapshot, this consolidates three things nothing else had in one place: fresh numbers regenerated at this exact commit, a table of every `db:*` verification tool built this session with what each one actually checks and whether it needs a live database, and a compiled "known open items" backlog pulling together the real, still-open gaps from XR-7 (6 functions needing atomic-RPC conversion), XR-17 (marketplace filter dead code), and XR-21 (deliberately-unindexed FK columns) so a frontend integrator doesn't have to re-read every individual audit doc to find the backlog. Documentation only, no code/schema change. 891/891 tests (no reset needed, nothing schema-affecting changed), `tsc`/build clean, 135 migrations unchanged. |
| `b4f53c2` | XR-21 | Query performance budget audit. New `scripts/query-performance-report.mjs` (`npm run db:perf-report`) and `docs/QUERY_PERFORMANCE_BUDGET_AUDIT.md`. Stages N and W each explicitly deferred indexing ~130 unindexed foreign-key columns their own audits found, "left for a future pass once real usage data (pg_stat_statements or equivalent) can identify genuinely hot ones" — this is that future pass. `pg_stat_statements` was already installed and enabled in the local Postgres image, just never used; new script resets/reports its counters as a repeatable tool. Ran it against a real workload (a full `test:db` run, 891 tests): every marketplace/listing query relevant to the deferred columns executed in well under 1ms, confirming with evidence (not a guess) that Stage N/W's existing targeted indexes already cover every actually-hot query — a real negative result, not a skipped check. Investigated and ruled out one apparent outlier (an unfiltered `rate_limit_events` smoke-check query showing 52ms mean over only 2 calls) via direct `EXPLAIN ANALYZE`: real execution time 0.08ms, the number was cold-path/connection overhead on a code path hit only twice in 891 tests, not a real query cost — documented rather than silently dismissed since a future skim of the report could otherwise misread it. Also fixed a real bug found while live-testing the new tool itself: some `pg_stat_statements.query` values contain embedded newlines, breaking the naive one-line-per-record output parser; fixed with `regexp_replace` flattening at the SQL level. No migration, no app/schema code changed this stage. 891/891 tests, verified on a fresh reset plus one more run without reset, `tsc`/lint/build/`db:preflight` clean, 135 migrations unchanged, no duplicate prefixes. |
| `6f5c8d3` | XR-20 | Schema/migration drift detection. New `scripts/schema-drift-check.mjs` (`npm run db:schema-drift`) and `docs/SCHEMA_DRIFT_DETECTION.md`. Neither existing check covered this: `migration-preflight.mjs` is a static text scan with no live database, and `contract-drift-check.mjs` (XR-18) only covers the grant/RPC surface — nothing caught a live database that had drifted out of band from the committed migration files (a manual `ALTER TABLE`/`psql` change with no corresponding migration). `supabase db diff --local` already does the real comparison (shadow DB built from migrations, diffed against the real local DB) but exits 0 regardless of whether it finds drift, so a thin wrapper parses its JSON output and turns a nonempty diff into a real failing exit code with the drift SQL printed. **Verified the detector actually detects drift, not just that it runs**: live-tested by creating a real table directly via `psql` (bypassing migrations entirely), confirming the wrapper reported the exact reconciling SQL and exited 1, then dropping it and confirming clean again. No migration this stage. 891/891 tests, verified on a fresh reset plus one more run without reset, `tsc`/lint/build/`db:preflight` clean, 135 migrations unchanged, no duplicate prefixes. |
| `8f8c4b4` | XR-19 | Fixture determinism. New `docs/FIXTURE_DETERMINISM_AUDIT.md`. Re-applied Stage AM's own `createTestTransportRequest()` precedent (a `Date.now()`-only suffix is theoretically collision-prone against Node's clock resolution) to the one place it was never re-applied: disposable `auth.signUp()` test emails, where a collision is more severe than elsewhere in the suite — it fails the whole test outright with "email already registered" rather than producing an informative assertion failure. New `uniqueTestEmail(prefix)` helper in `tests/db/helpers.ts`; migrated the 7 call sites that built an email from `Date.now()` alone (`deletion-blocker-graph.test.ts`, `duplicate-detection.test.ts`, `account-deletion-execution.test.ts` ×2, `risk-signals.test.ts`, `legal-holds.test.ts`, `recent-auth-step-up.test.ts`, `legal-consent-versioning.test.ts`); left 2 files alone that already had a random component. **Deliberate scope boundary, documented rather than silently left**: ~19 other `Date.now()`-only uniqueness suffixes exist (request numbers, dedup keys, storage paths) but were left untouched — a collision there is an ordinary, informative test failure, not an infrastructure-level abort, so fixing them would be speculative robustness with no observed gap. No migration this stage (pure test-infrastructure change). 891/891 tests, verified on a fresh reset plus one more run without reset, `tsc`/lint/build/`db:preflight` clean, 135 migrations unchanged, no duplicate prefixes. |
| `81d0d6a` | XR-18 | Public contract drift scanner. New `scripts/contract-drift-check.mjs` (`npm run db:contract-check`) and `docs/PUBLIC_CONTRACT_DRIFT_SCANNER.md`. IR-1's own snapshot doc was already demonstrably stale (dated against a 124-migration state; this repo is now at 135) — confirmed the real, reachable problem this stage names rather than assuming it. New script queries the live database for the same two contracts IR-1 documents (table grants, RPC signatures) and diffs against a committed, machine-readable baseline (`docs/backend-api-contract-baseline.json`), failing loudly on any undocumented drift; `--write` regenerates it after a deliberate change, matching the ESLint-baseline pattern from IR-16. **Verified the detector actually detects drift, not just that it runs**: live-tested by revoking a real grant, confirming the scanner reported it precisely, then restoring and confirming clean. Initial baseline (70 tables, 36 RPCs) generated fresh after a clean reset. Corrected a stale citation in IR-1's own doc (mislabeled the future scanner as "XR-17's" scope; XR-17 was cursor stability). Nullability drift deliberately not covered by the automated check — not exposed through the grant/RPC surface this scanner queries, and `src/lib/supabase/types.ts` (Stage IR-5) already tracks it for free via regeneration; a second tracker would duplicate it. No migration this stage. 891/891 tests, verified on two consecutive runs (hit transient container-restart timing flakiness on the first attempt after a fresh reset — auth/storage services briefly 502'd right after restart, confirmed resolved by waiting and re-running, not a real regression), `tsc`/lint/build clean, 135 migrations unchanged. |
| `6f49c54` | XR-17 | Cursor stability. New `docs/CURSOR_STABILITY_AUDIT.md`. Found and fixed a real gap in `listPublishedPuppies()` (the one real server-side-paginated query, Stage IR-2): `created_at` alone is not a stable sort key, since Postgres evaluates `now()` once per statement, not once per row — several puppies added via one multi-row INSERT (a real, reachable pattern, e.g. a litter added at once) share the exact same timestamp, giving `.range()`-based pagination no guaranteed ordering among the tied rows across separate page requests. Fixed by adding `id` as a secondary, stable sort key (`created_at desc, id asc`), no visible change for the common distinct-timestamp case. New test in `tests/db/marketplace-search-contract.test.ts` proves it against a genuine tie (two animals inserted in one statement, confirmed to share the literal same `created_at`), not a hypothetical one. Checked bounded page size (no reachable attack surface yet — nothing currently calls the paginated path with real arguments) and "malformed cursor rejection" (not applicable — this app uses plain offset pagination, not an opaque cursor token, so there's no token to forge) and confirmed neither needs a fix now. No migration this stage. 891/891 tests (+4 from XR-16's 887), verified on two consecutive runs (no reset needed, no schema change), `tsc`/lint/build clean, 135 migrations unchanged. |
| `d2d5d62` | XR-16 | Legal-hold propagation. New `docs/LEGAL_HOLD_PROPAGATION_AUDIT.md` — a revalidation pass over Stage CJH per this stage's own explicit precedence, not a duplicate build. Confirmed already correct: deletion/anonymisation blocking, never-hard-deleted hold rows, admin-only gating, server-stamped actors. **Found one real gap, checked literally against this stage's own "restrict and audit hold transitions" wording**: `place_legal_hold()`/`release_legal_hold()` never wrote a single `audit_logs` entry, unlike every other comparably consequential admin action in this schema — a legal hold overrides someone's own deletion rights, tied to litigation/investigation, yet left no trace in the shared ledger ops/admin actually review together. `20260101013300_legal_hold_audit_trail.sql`: both RPCs now insert a real, correctly-attributed `audit_logs` entry. New tests in `tests/db/legal-holds.test.ts` prove both transitions land as separate, real entries. Checked "archival"/"Storage cleanup" propagation and confirmed no real gap — no delete-a-document feature exists anywhere yet to propagate a check into (matching Stage BV's earlier finding, still true). 887/887 tests (+3 from XR-15's 884), verified on a fresh reset plus one more run without reset, `tsc`/lint/build/`db:preflight` clean, 135 migrations, no duplicate prefixes. |
| `c707b8a` | XR-15 | Anonymisation consistency. New `docs/ANONYMISATION_CONSISTENCY_AUDIT.md`. `execute_account_deletion()` (Stage AI) and `get_account_deletion_blockers()` (its dry-run, Stage CJI) were each already thoroughly tested in isolation but never proven consistent with each other on the same account. New test in `tests/db/account-deletion-execution.test.ts` proves it end-to-end on one real throwaway account: the dry-run reports a real blocker and real execution also refuses, consistently; the blocker resolves, the dry-run reports zero, and real execution then genuinely succeeds. Also proves "preservation of other users and history" directly rather than by construction alone: a real public post the account authored survives anonymisation completely intact (FK, content, existence all untouched) while the author's own identity is genuinely anonymised. Confirmed (not assumed) the frontend already gracefully degrades a since-anonymised author's null `display_name` at every real display site. No migration this stage. 884/884 tests (+6 from XR-14's 878), verified on a fresh reset plus one more run without reset, `tsc`/lint/build clean, 134 migrations unchanged. |
| `ce38f62` | XR-14 | Export object lifecycle — audit only, not applicable, no new gap. New `docs/EXPORT_OBJECT_LIFECYCLE_AUDIT.md`. `exportMyData()` is the only real export feature in this app — confirmed by reading the full function it performs a set of RLS-scoped DB reads and returns the assembled JSON directly to the caller in the same request, with no Storage bucket, generated file, signed URL, or async flow anywhere. Every concern this stage names (object ownership, signed access, expiry, duplicate files) describes protecting a generated file that outlives its request — there is none here. Cross-referenced (not re-audited) the export's own already-verified real boundary (RLS-scoped queries, no cross-tenant leak, `tests/db/data-export.test.ts`). Documented what a future async/Storage-backed export would need if one is ever built. No code, migration, or test change. |
| `9cc84f7` | XR-13 | Template revalidation. New `docs/TEMPLATE_REVALIDATION_AUDIT.md`. Revalidated CJS's core safety claim directly instead of trusting the code comment: confirmed by grep that zero code anywhere re-resolves a stored notification against `notificationTemplates` at read time (`listMyNotifications()` only ever selects the stored `title`/`body`). New test in `tests/db/notification-template-versioning.test.ts` proves it empirically — a notification with `template_version: 9999` and a `notification_type` confirmed absent from `notificationTemplates` (via `hasOwnProperty`, not assumed) creates and reads back cleanly, the concrete "current and unknown template versions" compatibility test this stage names. No duplicate implementation — nothing in the CJS migration or `notification-templates.ts` changed. 878/878 tests (+5 from XR-12's 873), verified on a fresh reset plus one more run (hit the same known transient rate-limit flakiness from cumulative session runs on one attempt, confirmed not a real regression via reset), `tsc`/lint/build clean, no migration this stage (134 unchanged). |
| `3a8a456` | XR-12 | Outbox payload versioning — audit only, no new gap. New `docs/OUTBOX_PAYLOAD_VERSIONING_AUDIT.md`. The one real outbox-shaped producer with a real reader (`notifications`) already has exactly this — Stage CJS's `template_version`, deterministic pure-function rendering, text stored permanently at creation (never re-resolved at read time), stable ids. Not rebuilt (that's Stage XR-13's dedicated revalidation scope). Checked the other candidate, `audit_logs.before`/`after` jsonb payloads, and confirmed no real versioning need: zero code anywhere (including the one real audit-log viewer) ever programmatically reads/parses them — write-only forensic snapshots for direct DB inspection, not a machine-consumed event stream with a reader that could break on shape drift. No other outbox/event-payload system exists (no email/webhook/external integration). No code, migration, or test change. |
| `bf22a96` | XR-11 | Backpressure and bounded workers — audit only, not applicable, no new gap. New `docs/BACKPRESSURE_BOUNDED_WORKERS_AUDIT.md`. Same real constraint as XR-10: no background worker pool exists to protect. Checked the closest real human-actor analog (`claim_moderation_case()`/`claim_support_case()`) for "stale locks" specifically — already directly investigated at Stage IR-11 and re-confirmed at Stage XR-17: deliberately no lease/TTL, since the broader staff "manage all cases" policy already gives a permanent override, confirmed neither claim RPC has been redefined since. Documented what a real worker pool would need (bounded concurrent claims, `SELECT ... FOR UPDATE SKIP LOCKED`, a real lease/TTL, backpressure) if one is ever built, without speculatively designing it now. No code, migration, or test change. |
| `f9ad727` | XR-10 | Poison-job handling — audit only, not applicable, no new gap. New `docs/POISON_JOB_HANDLING_AUDIT.md`. Re-confirmed (not assumed) no background job/worker/queue system exists anywhere in this codebase — the same fact independently verified at Stage BA, BU, IR-9, and IR-10; re-grepped for `pg_cron`/scheduler/worker-queue patterns this stage specifically, every hit is a comment referencing the same already-established fact. Poison-job handling (terminal failure after bounded retries, one job not blocking others) only applies to a real job/worker system, which doesn't exist and has no demonstrated need to. Cross-referenced the closest real analogs already built this session (XR-8's terminal-state guards, XR-9's idempotent retry, Stage J's rate limiting) rather than re-explaining them, and documented what a real job system would need to include if one is ever approved, without speculatively designing it now. No code, migration, or test change. |
| `edf131b` | XR-9 | Idempotency key registry. New `docs/IDEMPOTENCY_REGISTRY_AUDIT.md`. Confirmed a new generic actor/tenant/operation-scoped key table has no currently-demonstrated need beyond the several well-fitted, narrower mechanisms this schema already has (`buyer_applications_active_unique`, notification `dedup_key`, reservation/conversation unique indexes, claim RPCs' "idempotent if already you" handling) — building one would be the same speculative infrastructure this session has repeatedly declined elsewhere. Found and fixed 2 real gaps in `convert_application_to_reservation()`/`convert_welfare_case_to_transport_draft()`: both already correctly prevented a second real conversion, but did it by raising an exception on retry instead of returning the original success — a client retrying after a dropped response saw an error even though the operation had actually already succeeded. `20260101013200_conversion_rpc_idempotent_retry.sql`: a retry with the same effective payload now returns the original result; `convert_application_to_reservation()` additionally rejects a retry whose terms genuinely differ as a real conflict (proven directly — the pre-existing test's own retry call, which omits price/currency, already exercises this path). **Writing a genuinely-concurrent `Promise.all` test surfaced a second real gap the first fix didn't cover**: two callers could both pass the "already converted" check before either commits, then race at the `INSERT` — `reservations_application_id_key`'s real unique constraint means only one ever succeeds, but the loser used to see a raw, confusing Postgres error instead of the same idempotent success; closed with an `exception when unique_violation` handler matching `create_notification_if_enabled()`'s own race-resolution pattern, verified across 3 consecutive fresh-reset runs given the inherent timing-dependence of proving a race is closed. New tests in `tests/db/application-to-reservation-scenario.test.ts` (idempotent-retry, changed-payload-rejection, 10-way concurrent) and `tests/db/welfare-cases.test.ts` (idempotent-retry). 873/873 tests (+6 from XR-8's 867), verified on a fresh reset plus two more runs without reset, `tsc`/lint/build/`db:preflight` clean, 134 migrations, no duplicate prefixes. |
| `de8c33d` | XR-8 | Optimistic concurrency/stale-write protection. New `docs/OPTIMISTIC_CONCURRENCY_AUDIT.md`. Audited every RPC deciding a shared case's outcome for a `select ... for update` lock plus a terminal-state guard, the pattern `claim_moderation_case()`/`claim_support_case()`/`review_moderation_appeal()` already established. Two candidates investigated and found already correctly handled: quotation pricing edits (no reachable UI path exists at all), and moderation-case resolution by a non-assignee moderator (**caught before attempting a fix** — looked like a gap until re-reading `20260101010300_moderation_case_claim_rpc.sql`'s own comment showed it's a deliberate, already-considered design decision, the same "read full prior context first" lesson as Stage XR-6's `reports` near-miss). Found and fixed one real gap: `review_welfare_case()` did a plain unlocked update with no read of the current row first — two ops staff (or one, two tabs) concurrently deciding the same case would silently race with no serialization, and a case already `converted_to_transport`/`closed` could be silently re-reviewed and reset, contradicting the real `transport_requests` row it already spawned. `20260101013100_welfare_case_review_concurrency_lock.sql`: adds the row lock plus a terminal-state guard (reconsideration between the 3 non-terminal decision states stays fully allowed), and incidentally fixes the audit-log entry's always-`null` `before` value to the real prior status. New tests in `tests/db/welfare-cases.test.ts` prove the terminal-state rejection, legitimate reconsideration, and 10 genuinely concurrent (`Promise.all`) review calls all serializing to one consistent final state. 867/867 tests (+6 from XR-7's 861), verified on a fresh reset plus two more runs without reset (hit the same known transient rate-limit-exhaustion flakiness from cumulative session test runs on one attempt, confirmed not a real regression via a fresh reset), `tsc`/lint/build/`db:preflight` clean, 133 migrations, no duplicate prefixes. |
| `3e4ae1f` | XR-7 | Transactional workflow boundaries. New `docs/TRANSACTIONAL_WORKFLOW_BOUNDARIES_AUDIT.md`. Audited `src/lib/queries/*.ts` for exported functions doing 2+ sequential client-side writes with no single atomic RPC boundary — found 6 real, currently-wired candidates (`respondToQuotation`, `sendQuotation`, `createTransportRequest`, `assignDriverToJob`, `approveRehomingReview`, `escalateReportToCase`), 4 of them also trusting a client-supplied actor id with no server check. Rather than converting all 6 into bespoke RPCs (a much larger scope), found a single, smaller, higher-leverage fix closing the security-relevant half for every current *and future* direct-insert path at once: `transport_status_history.changed_by` was never actually locked to the real caller — a genuine straggler Stage CJD's own actor-attribution audit missed when it fixed the identical shape for `quotations.created_by`/`legal_requirements.created_by`. Added `stamp_changed_by_actor()` (`20260101013000_transport_status_history_actor_lock.sql`), the same unconditional trigger shape as the existing `stamp_created_by_actor()` — confirmed safe against every real caller (all 7 existing test-file insert sites, both already-atomic sibling RPCs) before applying, a true no-op for every legitimate path. Closes 3 of the 4 forgeable-actor rows found (`respondToQuotation`/`sendQuotation`/`assignDriverToJob` all insert into this table) plus half of the 4th. New test in `tests/db/actor-attribution-stragglers.test.ts` proves a forged `changed_by` is silently overridden. Full atomicity (the partial-write risk) for all 6 functions, and 2 silently-swallowed errors found while reading `respondToQuotation()`, deliberately left open and documented as a dedicated follow-up rather than a rushed addition — matching Stage IR-8's own precedent for scoping down rather than rushing. 861/861 tests (+4 from XR-6's 857), verified on a fresh reset plus two more runs without reset (trigger-changing stage), `tsc`/lint/build/`db:preflight` clean, 132 migrations, no duplicate prefixes. |
| `281f0e4` | XR-6 | Immutable history/evidence preservation. New `docs/HISTORY_EVIDENCE_IMMUTABILITY_AUDIT.md`. Audited every status/ownership/audit/document/moderation/support history-shaped table for a real UPDATE/DELETE policy (`transport_status_history`/`audit_logs` already confirmed append-only). Found and fixed one real gap: `animal_ownership_history`'s only policy for admins was `for all` despite the table's own comment describing it as provenance/history data — split to admin-SELECT-only, no INSERT for anyone (zero real writer exists anywhere in `src/`, Stage Y's original finding still holds). `20260101012900_history_evidence_immutability.sql`. **Caught a real mistake in an earlier draft before committing it**: the draft also planned to remove `reports`' raw DELETE capability (a superficially identical `for all` shape) based only on the migration comment explaining the `dismissReport()` app-code fix — re-reading `tests/db/reports-soft-dismissal.test.ts`'s own header comment showed Stage CJG had already explicitly considered and deliberately kept DELETE ("a report could legitimately need real removal... e.g. defamatory content"), a real, tested design decision, not an oversight; reverted that half of the change before finalizing the migration. Also explicitly declined to touch `moderation_cases`/`moderation_appeals`/`messages`/`support_cases`/`support_case_messages` (same `for all` shape, but current-state lifecycle records with plausible legitimate staff-delete needs and no documented prior intent like `reports` has) — flagged as the honest confidence boundary of this audit rather than a broad speculative rewrite. New `tests/db/animal-ownership-history-immutability.test.ts` (5 tests) proves no role can insert a row via the Data API at all (the strongest form of immutability, since nothing can ever be created to later update/delete) and that admin SELECT still works. 857/857 tests (+5 from XR-5's 852), verified on a fresh reset plus two more runs without reset (RLS-changing stage), `tsc`/lint/build/`db:preflight` clean, 131 migrations, no duplicate prefixes. |
| `1a49991` | XR-5 | Signed URL permission-loss safety. New `docs/SIGNED_URL_PERMISSION_LOSS.md`. Every real signed-URL call site uses a 300-second TTL, generated fresh on demand (never cached/persisted). Proved the real, honest residual risk empirically rather than assuming it: a driver's already-issued signed URL (created before a role suspension) still returns 200 on a raw `fetch()` after the suspension, while a *new* `createSignedUrl()` call from the same suspended driver is correctly rejected by RLS — Storage only re-checks RLS when minting a new token, never on each download of one already issued. Also confirmed "cancellation" is intended behaviour, not a gap: a customer keeps document-read access to their own cancelled transport request by design (no status condition in that policy at all), proven via a fresh signed-URL request succeeding post-cancellation. Documented which of the 5 named scenarios have a real, reachable mechanism in this schema: suspension and cancellation (tested), transfer (not materially distinct from suspension — same live-recheck-at-request-time shape, not duplicated), removal and dispute (confirmed via grep — no reachable feature exists for either, matching Stage CJQ's own earlier finding for "removal"). New `tests/db/signed-url-permission-loss.test.ts` (11 tests). Hit the same transient rate-limit-exhaustion flakiness this session's own Stage AP already documented (from the sheer cumulative volume of `test:db` runs this session) on the first run attempt — confirmed not a real regression via a fresh reset plus two more clean runs, 852/852 (+11 from XR-4's 841). No migration this stage (130 unchanged), `tsc`/lint/build clean. |
| `be9685f` | XR-4 | Storage path canonicalisation — audit only, no new gap found. New `docs/STORAGE_PATH_CANONICALISATION_AUDIT.md`. Verified every one of the 5 buckets' RLS policies uses the exact same `(storage.foldername(name))[1]::uuid` + domain-specific membership predicate shape (grepped every occurrence — zero exceptions). Classic path traversal doesn't apply: `storage.objects.name` is an opaque string, never a resolved filesystem path, so a `../`-containing name would just produce a confusingly-named object still fully scoped to its real, literal first segment, not a cross-tenant write — and no real upload path in `src/` ever accepts a raw user-supplied path anyway. Uploader identity is never encoded in the Storage path at all in this schema's design (always tracked server-side in the metadata table row instead) — "canonical uploader path" doesn't apply as a concept here. Re-verified the one real historical bug in this class (Stage BF's `drivers.name`/`storage.objects.name` column-shadowing fix, `20260101006300`) stays fixed: every policy joining a table with a real `name` column (`drivers`) already fully qualifies `storage.objects.name`; the two tables still using the bare, unqualified `name` (`welfare_cases`, `conversations`) have no `name` column at all, confirmed via `information_schema.columns`, so no shadowing risk exists for them either. No code, migration, or test change — everything this stage's own definition named was already either structurally impossible or already fixed/tested. |
| `43ba753` | XR-3 | Grant/Data API exposure audit. New `docs/GRANT_DATA_API_AUDIT.md` — a live cross-reference of every real `anon`/`authenticated` table grant (`information_schema.role_table_grants`) against `pg_policies`, finding grants broader than any RLS policy actually uses (the mirror image of `db:preflight` check 1, which catches the opposite: a policy with no grant). Found and documented a real limitation of the method itself first: 7 of 15 raw hits are views, which never have their own `pg_policies` row (a real gap for any future automated version of this check, not built this stage — would need real view-definition parsing). Of the 2 real table-level findings: `rehoming_reviews` grants `anon` SELECT with no anon-scoped policy — confirmed necessary and already documented (`20260101003000_rehoming_reviews_anon_grant.sql`: `animals`' own public-listing policy references it in a subquery, and Postgres requires table-level privilege on every table a query plan touches, even inside another table's RLS subquery); `audit_logs` grants `authenticated` UPDATE/DELETE as part of a deliberate 25-table blanket grant (`20260101002900_table_grants.sql`) with zero UPDATE/DELETE policy for anyone — confirmed inert, matching `docs/DATABASE_INVARIANTS.md`'s own "audit_logs are append-only for everyone" claim. Neither needed a fix (RLS already fully closes both). New `tests/db/grant-data-api-audit.test.ts` proves both directly against the live database — the `audit_logs` test specifically as the `admin` persona, the single most-privileged non-superuser role, the strongest possible proof of the append-only claim — acting as the "drift test" this stage's own definition asks for. 841/841 tests (+9 from XR-2's 832), verified on 3 consecutive runs (no migration this stage, so no reset needed between them), `tsc`/lint/build clean, no schema change. |
| `75684cd` | XR-2 | SECURITY DEFINER audit. Extended `scripts/migration-preflight.mjs` (Stage CA) with a 5th automated check: any `SECURITY DEFINER` function whose *latest* definition (correctly tracking `create or replace` redefinitions across files, not just its first appearance) has no pinned `search_path` — turning the manual `psql` audit run by hand at Stages IR-13/IR-17/IR-1(indirectly) into a real, repeatable, offline `npm run db:preflight` check. **Found the script itself had zero test coverage** despite its own Stage CA commit message claiming otherwise — added `tests/db/migration-preflight.test.ts` (9 tests) covering all 5 checks against synthetic SQL, including a real bug caught while writing it: check 4's "safe drop" exemption checks for `if not exists` (which doesn't grammatically apply to DROP), never `if exists` — inert in practice (this schema never uses that phrasing) but a genuine, if harmless, latent defect in a check nobody had tested before. Also found and fixed a second real "minimal grants" instance of the exact shape Stage IR-13 closed for `has_role()`: `get_notification_preference(p_profile_id, p_category)` — the only other function in the schema accepting an arbitrary *other* user's id — was granted directly to `authenticated` with zero direct app-code caller (confirmed by grep) and no internal check of its own; any logged-in user could probe an arbitrary real profile id and learn whether that person muted a specific notification category. Lower severity than a role-membership probe, same reachable-via-raw-API-call shape. Fixed with `20260101012800_notification_preference_execute_lock.sql` (`revoke execute ... from authenticated`) — safe by the same mechanism already proven for `has_role()`: its one real caller, `create_notification_if_enabled()`, is itself `SECURITY DEFINER` and executes it under its own owner privileges regardless of the invoking role's grants. Updated the 2 existing direct-RPC-call tests in `tests/db/notification-preferences.test.ts` to prove the new rejection instead, while the actual preference-logic behavior (default-enabled, security-always-true) stays proven through `create_notification_if_enabled()`'s own already-present observable-outcome tests, unchanged. 832/832 tests (+9 from XR-1's 823), verified on a fresh reset plus two more runs without reset (grant/security-changing stage), `tsc`/lint/build clean, 130 migrations, no duplicate prefixes, `db:preflight`'s own new check confirms zero unpinned `SECURITY DEFINER` functions remain. |
| `fdd1bfc` | XR-1 | Protected-field mutation matrix. New `docs/PROTECTED_FIELD_MATRIX.md` — a real audit of every `prevent_*`/`*_lock` trigger in `supabase/migrations/` (cross-referenced against each migration file, not guessed), consolidated into one table: 14 tables/buckets with a documented protected-field lock, plus 5 confirmed to structurally need none (single-writer tables where the column-lock bug class can't occur). **Found and fixed one real, previously-uncovered gap while building it**: `support_cases`' "requesters may only reopen their own resolved or closed case" policy's `with check` only ever verified the new `status`, never that every other column stayed equal to `OLD` — a requester's UPDATE could smuggle in a `priority` bump, a `category`/`subject` change, or self-assign `assigned_staff_id` alongside a legitimate reopen, despite this table's own header comment explicitly saying all of those are staff-only. Not reachable through the real UI (`reopenSupportCase`-style call sites only ever send `{status:'reopened'}`), real via a raw API call, the exact same severity tier as every other lock trigger this session has added. Fixed with `20260101012700_support_case_reopen_field_lock.sql`: `prevent_requester_writes_to_staff_controlled_support_fields()`, the same allowlist-diff shape as `prevent_buyer_writes_to_org_controlled_fields()` (only `status`/`updated_at` stay requester-mutable). New tests in `tests/db/support-cases.test.ts` prove: a reopen bundled with a priority bump is rejected and doesn't partially apply, a reopen bundled with self-assignment is rejected, a plain reopen still works, and ops staff retain full unrestricted access throughout. 823/823 tests (+7 from IR-18's 816), verified on a fresh reset plus two more runs without reset (RLS/trigger-changing stage), `tsc`/lint/build clean, 129 migrations, no duplicate prefixes, `SECURITY DEFINER` search_path audit still zero-finding. |

## Third/fourth supplemental queue: stages completed

| Commit | Stage | Summary |
|---|---|---|
| `4634e32` | CJA | Authorization matrix. A systematic, table-centric sweep distinct from Stage CD's role-first permission inventory: (1) grepped every RLS policy for `using (true)`/`with check (true)` on a non-SELECT operation (the classic "accidentally grants unrestricted write access" red flag) — found 6 `using (true)` policies, all genuinely `for select` on legitimately public data (profiles' public columns, breeds, legal document versions, maintenance-mode status, group membership lists), zero `with check (true)` anywhere. (2) Cross-referenced every `alter table ... enable row level security` against every `create policy` — zero tables have RLS enabled with no policy at all (which would fully lock the table, even for admins, via the Data API). (3) Checked every table for an admin/staff override policy and manually verified each apparent gap (a quick grep heuristic false-flagged `reports`/`moderation_cases` as missing one — they use `is_moderator()`, not `is_admin()`/`is_ops_staff()`, confirmed by reading the actual policy text) — the real, remaining gaps (`follows`, `reactions`, `saved_posts`, `notification_preferences`) are uniformly low-stakes, purely personal, user-owned preference/social-graph data where no admin override is a reasonable, intentional design choice, not a bug; nothing security- or moderation-relevant lacks one. No code change — a genuine, verified "no gap found" outcome. |
| `3593f16` | CJB | Public/private projection layer. Read all 7 public views in the schema column-by-column (`public_transport_requests`, `public_routes`, `public_fundraising_contributions`, `public_fundraising_totals`, `public_transport_rating`, `driver_transport_job_view`, `my_moderation_case_view`) and confirmed every one correctly hand-picks a safe column list, never leaking an exact address, phone number, document field, supporter/reviewer identity, or internal-only moderation note. `driver_transport_job_view` and `my_moderation_case_view` are `security_invoker`/per-caller-scoped views (correctly relying on the base table's row-level RLS, only narrowing columns), while the fully-public ones (`public_transport_requests`, `public_routes`, the two fundraising views, `public_transport_rating`) are deliberately definer-style since their base tables have no anon-facing RLS policy at all — the view itself is the entire security boundary for those. Complements Stage O's earlier table-by-table privacy audit with a view-by-view column-projection lens instead. No code change — a genuine, verified "no leak found" outcome. |
| `29660b5` | CJC | Data classification catalogue. New `docs/DATA_CLASSIFICATION.md` — a simple four-tier scheme (Public, Internal, Restricted, Confidential/staff-only) covering everything sensitivity-relevant in this schema, personal and non-personal alike. Deliberately broader and shallower than `docs/PRIVACY_DATA_LIFECYCLE.md` (Stage O), which stays the authoritative, detailed GDPR-focused source for personal data — cross-referenced, not duplicated. Covers marketplace/community public data, contact details/exact addresses (Restricted), risk signals/internal moderation notes/audit logs/rate-limit telemetry (Confidential — never shown to the subject even though they can see the parent row, verified for `reports` specifically that the reported party has no read access to the row at all, not just the reporter identity), and business/pricing configuration. Docs-only, no code change. |
| `838acce` | CJD | Server-controlled actor attribution audit. Systematically re-grepped every `*_by`/`*_profile_id` actor-attribution column across the whole schema (13 distinct column names, ~20 tables) against Stage CB's already-documented list and found two real stragglers, both still client-insertable with no trigger tying them to the real caller: `quotations.created_by` (the real, demonstrated gap — `createQuotation()` inserted a client-supplied value directly, called from a real, live UI, `dashboard.operations.quotations.tsx`) and `legal_requirements.created_by` (no real call site today, closed at the same time for consistency at near-zero cost). New `stamp_created_by_actor()` trigger (`20260101011200_actor_attribution_stragglers.sql`) forces `created_by := auth.uid()` on insert for both tables. `createQuotation()` no longer accepts or sends a `createdBy` param at all — matching the pattern already used elsewhere: once an actor is fully server-stamped, drop the now-forgeable client param entirely. New `tests/db/actor-attribution-stragglers.test.ts`. 641/641 tests, verified on a fresh reset plus two more runs without reset. |
| `03cb13f` | CJE | Cross-tenant fuzz testing. New `tests/db/cross-tenant-fuzz.test.ts` — a single, consolidated, data-driven sweep across the major domain tables (`transport_requests` with 3 different owners, `buyer_applications`, `transport_status_history`, `animals` write-side, `saved_animals`) rather than the many individual, scattered cross-tenant checks already spread across feature files, so a future regression in any of them shows up in one place. Reuses existing seeded fixtures rather than creating new ones; each case includes a positive-control check (the real owner must still see their own row) so a "blocked" result can't be vacuously true from a nonexistent row. Found and fixed a real test bug while writing this: the seed data already has `buyer -> animalMaja` saved, so inserting a fresh `saved_animals` row for that exact pair hit the table's own unique constraint — switched to reading the existing seeded row. Also hit and resolved the same well-documented cumulative rate-limit exhaustion this session has hit before, confirmed not a real regression via a fresh reset. 650/650 tests, verified on a fresh reset plus two more runs without reset. |
| `044f507` | CJF | Immutable lifecycle history. `transport_status_history` is meant to be the permanent record of what actually happened during a delivery — both existing write policies were `for all`, not `for insert` ("ops staff manage all status history", "assigned drivers view and log status on their own requests"), so both ops/admin and the assigned driver could UPDATE or DELETE an existing historical row, not just append new ones. Grepped every real call site in `src/lib/queries/*.ts`: every single one is either `.insert(` or `.select(` — zero UPDATE/DELETE anywhere in the real app — so narrowing this (`20260101011300_immutable_status_history.sql`, splitting both `for all` policies into separate `select`/`insert` policies with the same predicates, then revoking update/delete at the table-grant level too) closes a real, reachable-via-raw-API gap with zero effect on any real code path. Matches `audit_logs`' own existing precedent exactly — no update/delete policy for anyone, not even admin, since Stage AE. New `tests/db/immutable-status-history.test.ts`. 658/658 tests, verified on a fresh reset plus two more runs without reset. |
| `2ff2b06` | CJG | Soft-deletion/archival semantics. Audited every table for a consistent soft-delete/archival pattern: `profiles` already has real soft-deletion (Stage AI), organisations use suspend/restore (Stage I), animals use status transitions, and every real `.delete()` call elsewhere targets genuinely ephemeral records with no audit value. One real inconsistency found: `dismissReport()` hard-deleted a `reports` row — permanently destroying evidence for detecting a real abuse pattern (repeated reports from one reporter), the exact signal `risk_signals.multiple_independent_reports` (Stage BN) was deliberately left unwired for exactly this future need. `reports` had no status column at all. New `report_status` enum (`20260101011400_reports_soft_dismissal.sql`); `dismissReport()` now updates status instead of deleting, `escalateReportToCase()` also marks the source report escalated, `listReports()` filters to `status='open'` so the admin queue's observable contents stay identical to before. Ops/admin's raw DELETE capability is deliberately left intact at the RLS layer (a rare real removal need is still legitimate) — only the normal dismiss workflow is now non-destructive by default. New `tests/db/reports-soft-dismissal.test.ts`. 663/663 tests, verified on a fresh reset plus two more runs without reset; smoke-tested the admin reports page against a running dev server. |
| `584be04` | CJH | Legal-hold mechanism. `execute_account_deletion()` (Stage AI) already refuses while a real business obligation is unresolved — a legal hold is the same shape of blocker for a different reason (litigation/investigation/regulatory need to preserve a specific account's data). No UI exists to place one yet — explicitly requested by name in this queue regardless, the same "build the backend mechanism on explicit instruction" precedent as Stage BL-addendum's support cases. New `legal_holds` table (`20260101011500_legal_holds.sql`), admin-only (`is_admin()`, narrower than the usual `is_ops_staff()` since this overrides someone's deletion rights entirely); `place_legal_hold()`/`release_legal_hold()` server-stamp the real actor; released holds are never deleted, only marked `released_at`/`released_by`, matching Stage CJG's own just-established discipline. `execute_account_deletion()` extended (not rewritten) with one more blocker check — carefully diffed line-by-line against the original migration to confirm the rest of the function body (the `processed_by` stamp, the closing `audit_logs` insert) carried over exactly unchanged. New `tests/db/legal-holds.test.ts`. 674/674 tests, verified on a fresh reset plus two more runs without reset. |
| `b1e1b59` | CJI | Deletion-blocker graph. `execute_account_deletion()` only ever reports the first blocker it finds via an `elsif` chain — an admin discovers blockers one at a time, by repeatedly attempting and re-reading the error after fixing each one. New `get_account_deletion_blockers(profile_id)` RPC (`20260101011600_deletion_blocker_graph.sql`) reuses the exact same conditions but returns every one that currently applies at once — read-only, admin-only. Wired into `dashboard.admin.users.tsx`: each pending deletion request now shows its current blockers inline (new `getAccountDeletionBlockers()` in `src/lib/queries/privacy.ts`, new `DeletionBlockers` subcomponent) — a real, already-existing admin page was the natural place to wire this in immediately rather than deferring the UI. New `tests/db/deletion-blocker-graph.test.ts` proves admin-only access and that multiple real blockers (a transport request + a legal hold) are reported together, not just the first. 679/679 tests, verified on a fresh reset plus two more runs without reset. |
| `f7389ca` | CJJ | Versioned export manifest. `exportMyData()` (Stage AH) had no self-describing metadata about itself — a downloaded export file today is indistinguishable from one downloaded after the function's shape changes later. Wrapped the return value in `{ manifest, data }`: `manifest` carries `schema_version` (bumps only on a breaking shape change, never just for an added category), `generated_at`, `subject_profile_id`, and the real category list. Confirmed this is a real, already-wired UI feature, not a schema-only stub — called from `account-privacy-card.tsx`'s "Download my data" button (`dashboard.buyer.profile.tsx`), which passes the result straight to a generic `downloadJson(data: unknown, ...)` with no field-specific access, so the change is fully backward-compatible for that call site. No DB/migration change — `tsc`/lint/build clean, 679/679 DB tests unaffected, smoke-tested the buyer profile page against a running dev server. |
| `3a8f062` | CJK | Support-access justification audit. Checked whether staff access to a customer's data through the support-case system (Stage BL-addendum, the one place this session built dedicated support tooling) carries a real justification trail. Found it already does: `claim_support_case()` is atomic and audited (`audit_logs`, actor server-stamped, before/after state), and every `support_cases` row already carries an explicit `related_entity_type`/`related_entity_id` context field ("never trusted for authorisation by itself; it's context for staff, not a capability" — Stage BL-addendum's own words) that a reviewer can join against the audit log's `target_id` to answer "why did this staff member access this case." Considered denormalising the related-entity context directly into the `audit_logs` row and rejected it — the information is already one join away via `target_id`, and duplicating it would be redundant storage for a completeness gain nobody has asked for. Considered building a general "staff must justify every read of sensitive data" system across the whole app and rejected it as clearly disproportionate: `is_ops_staff()`-gated "manage all" access on operational tables (transport requests, quotations) is core to how ops actually does its job — a mandatory reason prompt on every view would be disruptive to a real, high-volume, legitimate workflow with no demonstrated abuse this session found evidence of. No code change — a genuine "already adequate for the one real support-access surface that exists" outcome, not a manufactured fix. |
| `f71c0e5` | CJL | Impersonation-policy decision. Confirmed no staff "sign in as user" impersonation mechanism exists anywhere in this codebase. Recorded a deliberate policy decision in `docs/DECISIONS.md` (the existing home for product/architectural decisions) rather than building the feature: staff impersonating a real user is a materially larger trust concession than the operational `is_ops_staff()` access ops already has, and shouldn't be added as a side effect of a support-tooling stage with no demonstrated need. Documents what a future implementation would minimally require if a real need ever arises (visible-to-user notification, a second confirmation for any mutating action while impersonating, time-boxed sessions, a real `audit_logs` entry per impersonated request) without building any of it now. Docs-only, no code change. |
| `c987200` | CJS | Notification and email template versioning. No email provider exists in this app (Stage AC/CJO's own audits confirmed this repeatedly — nothing to send to), so building a marketing template editor (explicitly out of scope per this stage's own instruction) would be pure speculation; the real, honest scope is the 4 notification types that already exist (Stage H). New `src/lib/notification-templates.ts`: a pure, versioned `render()` function per type (`rehoming_approved`, `rehoming_rejected`, `application_status_change`, `moderation_decision`), replacing ad-hoc inline title/body string construction that was previously repeated at each of the 3 non-trivial call sites (`rehoming.ts` ×2, `applications.ts`, `moderation.ts`) — `notifyUserFromTemplate()` is the new path all 3 now use, `notifyUser()` itself stays available as the low-level primitive. New `notifications.template_version` column (`20260101012200_notification_template_versioning.sql`), stamped as a pure audit trail of which version of the render logic produced the stored text — critically, the rendered text itself is still persisted permanently at creation and never re-rendered at read time (unchanged from Stage H's own original design), so an "unsupported version" can never surface as a rendering failure to a reader; the database enforces the one thing it honestly can, rejecting a non-positive version outright (`check (template_version is null or template_version >= 1)`). A real generic-TypeScript bug found and fixed while wiring this: `Parameters<(typeof notificationTemplates)[T]["render"]>[0]` for a generic `T extends NotificationTemplateId` produced an unsatisfiable *intersection* of every template's payload type instead of narrowing to one — fixed with an explicit mapped type (`NotificationTemplatePayloads`) instead. New `tests/db/notification-template-versioning.test.ts` proves render() is a pure function (same payload in, byte-identical output out), the version is stored/validated correctly, and — tying directly into CJR's own dedup work — a "replay" sharing a dedup key with a *different* title/body and a bumped version is silently ignored, the original row's original wording and version surviving untouched. Locale capture was deliberately left out: this app has no i18n/locale system at all, and `src/lib/i18n/**` is an explicit CLAUDE.md frontend-owned area this session avoids broad edits to — inventing a locale field with no real consumer would be speculative infrastructure, the same pattern this session has consistently avoided (Stage BA/BB/BE/BP/BS/BT/BV). 743/743 tests (+9 from CJR's 734), verified on a fresh reset plus two more runs without reset. **This closes every received CJ-series stage (CJA through CJS) — CJT onward and all of CKA–CKZ were never transmitted with real content and are correctly not invented, per standing instruction. Continuing directly into IR-1 (integration-readiness queue) next, per standing precedence.** |
| `ae09219` | CJR | Notification deduplication. `create_notification_if_enabled()` (Stage H) is the one real producer every `notifyUser()` call routes through (confirmed by grep: exactly one `insert into public.notifications` anywhere in this schema), but had no dedup concept. All 4 real call sites are invoked directly from an unguarded client action — e.g. `approveRehomingReview()` does a plain `.update()` with no `admin_status = 'pending'` guard — so a double-click, a retried network call, or two admins acting on the same review at once each independently call `notifyUser()` again, a real reachable duplication path, not hypothetical. Added an optional `p_dedup_key` parameter (`20260101012100_notification_deduplication.sql`), enforced by a partial unique index (`profile_id, dedup_key) where dedup_key is not null`), resolved atomically via `insert ... on conflict do nothing` plus a fallback lookup — genuinely safe under concurrent callers racing the same key, not just sequential retries (proven by a real 10-way `Promise.all` race test, not just a sequential retry test). Since adding a new trailing parameter changes the function's signature (6 args → 7), `create or replace` alone would have left the old 6-arg version behind as a stale, reachable duplicate — explicitly `drop function if exists` first. Wired real dedup keys into all 3 non-trivial call sites: `rehoming.ts` (keyed on review id + decision), `applications.ts` (keyed on application id + the specific status reached, so a later genuinely different transition still notifies — "distinct events remain distinct"), `moderation.ts` (keyed on case id). `NULL` dedup_key (the unchanged default) is never deduped, so any future caller that omits it keeps today's behaviour exactly. Updated the hand-written `src/lib/supabase/types.ts` stub (`notifications.dedup_key`, the RPC's new param) to stay in sync. 734/734 tests (+12 from CJQ's 722), verified on a fresh reset plus two more runs without reset (a third for the concurrency test). |
| `cedaa9d` | CJQ | Messaging abuse controls. Audited `conversations`/`messages` (Stage R/S) and `support_cases`/`support_case_messages` (Stage BL-addendum/CJP) against this stage's checklist — most items already correct and not duplicated: sender attribution is RLS-enforced on both tables (`sender_profile_id = auth.uid()`, never client-trusted), internal-note spoofing was closed at Stage R/the support-case build, membership is a live RLS join re-evaluated on every request (not a cached grant), and send-flooding is now rate-limited on both tables (Stage J/CJP). "Removed-member continued access" has no reachable path — grepped every query file, there is no "leave a conversation"/"remove a participant" feature anywhere yet. "Suspended-role access" doesn't apply by design: messaging is conversation-membership-based, not role-gated (unlike Stage BD's driver/org checks) — a suspended platform role shouldn't, and doesn't, cut off someone's personal messages, the same distinction established at Stage 20260101006100. Two real gaps closed in `20260101012000_messaging_abuse_controls.sql`: (1) neither `messages.body` nor `support_case_messages.body` had any length bound — a raw API call could insert an arbitrarily large body (the longest real body anywhere in seed/tests is 73 characters; added a 10,000-character `CHECK`, enormously generous for genuine use). (2) `messages.attachment_url` had no constraint tying it to the message's own `conversation_id` — the one real call site (`sendMessage()`) always constructs the path itself right before upload, so not UI-reachable, but a raw API call could set it to a path under a *different* conversation's folder. Storage's own RLS (`is_conversation_participant()` on the folder-derived UUID, Stage S) already independently re-validates participancy in the *referenced* conversation at signed-URL time, so nothing could actually leak — but the DB row itself could still carry a lie about which conversation an attachment belongs to; a new `CHECK` (`attachment_url like conversation_id || '/%'`) closes it structurally, not just practically. New `tests/db/messaging-abuse-controls.test.ts`, using two genuinely different real conversations (an application conversation and a fresh disposable transport conversation) to prove the substitution is rejected, not just a same-conversation edge case. 722/722 tests (+15 from CJQ's own file over CJP's 707), verified on a fresh reset plus two more runs without reset. |
| `2e76063` | CJP | Rate-limit configuration. Audited every real customer-reachable creation path against Stage J's `enforce_rate_limit()` (already wired to `report_submission`, `message_send`, `welfare_case_submission`, `application_submission`, `transport_draft_creation`, `transport_amendment_request`, `org_invitation`) and found two genuinely unlimited paths: `support_cases`/`support_case_messages` (Stage BL-addendum, built after Stage J so never checked against it) had no rate limit of any kind — spammable directly via the Data API, no RPC required. Fixed with two new `BEFORE INSERT` triggers (`20260101011900_support_case_rate_limits.sql`) reusing `enforce_rate_limit()` exactly as-is (server-derived actor, deterministic window, stable error, self-pruning via Stage BU) — same shape as the 4 existing triggers, 60/hour for case creation, 30/minute for messages (matching `message_send`'s own precedent). Everything else on this stage's checklist is already covered, not duplicated: signup/sign-in rate-limited by GoTrue itself (`sign_in_sign_ups = 30`/5min/IP, `supabase/config.toml` — a real external limit, correctly left alone); appeals need no limit at all since `moderation_appeals`' own `unique (moderation_case_id)` constraint already makes a second appeal per case structurally impossible, a stronger guarantee than any throttle; quotations are staff-created only, not customer-reachable; Storage-object uploads are a genuinely different enforcement surface (documented as a real, separate future item in `docs/TECH_DEBT_REGISTER.md` rather than conflated here). **Found and fixed a real bug while testing, not just a coverage gap**: `support_cases` was granted `select, insert, update` but never `delete`, despite "ops staff manage all support cases" being a `for all` RLS policy that includes delete — the same GRANT-vs-RLS gap class this session has now hit 7 separate times. This was silently masked: `tests/db/support-cases.test.ts`'s own 4 existing cleanup steps called `admin.from("support_cases").delete(...)` without ever asserting the result, so the failure was invisible until this stage's own cleanup step explicitly asserted it and caught it for real. Fixed the grant (`grant delete on public.support_cases to authenticated`) and all 4 silent-cleanup call sites in the existing test file. New `tests/db/support-case-rate-limits.test.ts`, using disposable throwaway accounts throughout (never the shared `customer`/`admin` personas, so this stage's own bucket-filling can never exhaust another test file's quota for the same action_key) and filling the bucket via direct `enforce_rate_limit()` RPC calls rather than dozens of real inserts. 707/707 tests (+10 from CJO's 697), verified on a fresh reset plus two more runs without reset. |
| `bb9589d` | CJO | Secrets and configuration audit. Checked env files, config loaders, service-role usage, OAuth provider config, and Storage bucket visibility. All already correct: `.env` is gitignored and untracked (only `.env.example`, with genuinely empty placeholders, is committed); grepped the whole repo for `service_role`/`SERVICE_ROLE` — zero references anywhere in `src/`, only a comment in `supabase/config.toml`; both `[auth.external.google]`/`[auth.external.facebook]` default to `enabled = false` in `supabase/config.toml`, with the sign-in page already surfacing a clear "isn't configured on this server yet" message (`_public.signin.tsx`) rather than a fake success state; all 4 document/evidence-carrying Storage buckets (`transport-documents`, `transport-evidence`, `message-attachments`, `welfare-case-documents`) are private, only `kennel-media` (public marketplace photos) is intentionally public; both Supabase client constructors (`browser.ts`/`server.ts`) already throw a clear, secret-free error naming the missing variable if required config is absent, at first construction. The one real gap: no standalone way to check config *before* actually running the app. New `npm run config:check` (`scripts/config-check.mjs`) — reports which required/optional variables are set without ever printing a value, warns if `VITE_SUPABASE_URL` doesn't look like a local instance (127.0.0.1/localhost) since no production Supabase project is configured for this app at all (CLAUDE.md), and warns on a half-configured OAuth provider (client id set without its secret or vice versa). Deliberately **not** wired into CI: both existing CI jobs intentionally never set `VITE_`-prefixed vars (the build job's own comment already explains `vite build` doesn't need them locally), so gating on them would break a currently-green pipeline for a non-issue — documented as a local/dev command, not silently claimed as CI-enforced. `tsc`/lint/build clean, 697/697 DB tests unaffected (no migration this stage, confirmed by one sanity run rather than assumed since `package.json` changed). |
| `0d09749` | CJN | Reauthentication ("step-up") hooks. Confirmed directly against a real signed-in session in this local instance (not assumed) that Supabase GoTrue's JWT carries a genuine `amr` (Authentication Methods Reference) claim recording actual authentication *events* with their own timestamps — unlike `iat`/`exp`, `amr` timestamps are never advanced by a silent token refresh, so they honestly answer "when did this session last actually authenticate," making a real, provider-neutral step-up check possible without inventing an identity provider. New `require_recent_auth(p_operation, p_max_age default 15 minutes)` (`20260101011800_recent_auth_step_up.sql`), fail-closed (raises a stable `reauthentication_required` P0001 message if unauthenticated or stale), server-enforced with no client-supplied bypass (reads only `auth.jwt()`, never a request parameter). Deliberately narrow scope: wired into exactly the three rarest, most destructive, already-admin-only RPCs this session has built that this stage explicitly names — `execute_account_deletion()` (Stage AI/CJH, irreversible), `place_legal_hold()`/`release_legal_hold()` (Stage CJH, legal-sensitive) — each carefully diffed line-by-line against its prior definition to confirm only one `perform require_recent_auth(...)` line was added, placed *after* the existing `is_admin()` check so a non-admin still gets the original authorization error, never a reauth-error side-channel confirming the operation exists. Deliberately NOT wired into higher-volume routine admin actions (`approve_user_verification()`, the CJM verification/owner/featured trigger, moderation/support case claims) — no reauthentication UI exists anywhere in this app yet (no "re-enter your password" step), so hard-gating a frequently-used workflow behind an unrefreshable freshness window would be a real, demonstrated regression, not a security improvement; documented as the honest current scope rather than silently narrowed. New `tests/db/recent-auth-step-up.test.ts`: valid/expired/missing markers (expired forced deterministically via a `0 seconds` window rather than a real wait; missing-marker via anon, correctly caught at the grant layer before the function body's own fail-closed branch, confirmed as defense-in-depth not a gap), authorization-before-reauthentication ordering, and a genuinely fresh admin session succeeding at all three wired RPCs. 697/697 tests (+9 from CJM's 688), verified on a fresh reset plus two more runs without reset. |
| `413099f` | CJM | Admin command log + a real self-approval fix. Investigating why admin-only organisation actions (verification status, featuring) had no audit trail surfaced a more serious finding: `organisations.verification_status` was never added to `prevent_org_owner_transfer_by_non_admin()`'s protected-field set (Stage E/I locked `owner_user_id`/`is_featured`, but not this column) — "owners update their own organisation" is a plain row-level policy with no column restriction, so an org owner could self-approve (or self-suspend) their own organisation's verification via a raw UPDATE, completely bypassing `approve_user_verification()` (Stage BC's admin-gated, idempotency-hardened RPC this is meant to go through) — the same self-approval bug class this session has closed repeatedly elsewhere (`rehoming_reviews`, `buyer_applications`). Closed both findings together in `20260101011700_organisation_verification_lock_and_admin_audit.sql`: `verification_status` joins the protected set, and every real admin change to any of the three protected fields (`owner_user_id`, `is_featured`, `verification_status`) now writes a real `audit_logs` entry atomically in the same trigger, actor server-stamped from `auth.uid()`, never a separate client-side write that could be forgotten or fail silently. **Found and fixed a real test-design bug while writing `tests/db/organisation-verification-lock-and-audit.test.ts`**: the first draft used `breederPending` (whose own `breeder` role is still `status='pending'`) as the actor attempting self-approval — but `owns_org()`'s RLS `using` clause requires an *active* role for the org's type, so RLS itself would silently filter the raw UPDATE to zero matched rows (`data: [], error: null`) before the trigger ever ran, making the test pass for the wrong reason and proving nothing about the new lock. Fixed by using `breeder1` (an owner with an already-active role) for a disposable pending org instead, so the UPDATE genuinely reaches the trigger and the assertion exercises the real P0001 rejection (`assert.ok(attempt.error, ...)`, not the `isBlocked()` RLS-style helper, which doesn't recognize a trigger-raised P0001 as a rejection). Also fixed an absolute-zero audit-count assertion that would have gone flaky against `orgWolnaDolina`, a shared seeded org `tests/db/admin-placeholders.test.ts` also legitimately admin-toggles in the same non-reset suite run — switched to a before/after delta. 688/688 tests (+9 from CJI's 679), verified on a fresh reset plus two more runs without reset (a third given this is a security-trigger/audit stage). `tsc`/full-file lint/build/`db:preflight` clean; confirmed the function's `set search_path = public` stayed pinned through the rewrite. |

## First supplemental queue: stages completed

| Commit | Stage | Summary |
|---|---|---|
| `6f8717a` | R | Messaging/conversation security. Found and closed a real gap: "participants send messages in their conversations" (`20260101002000_messaging.sql`) checked `sender_profile_id`/conversation membership but never restricted `is_internal` — an ordinary participant's INSERT could set `is_internal = true` on their own message, spoofing what's meant to be a trusted ops-only annotation channel (`is_internal` is the actual visibility gate: the SELECT policy filters `not is_internal`). Not reachable through the real UI today (`chat-thread.tsx`'s only `sendMessage()` call site never passes `isInternal`, and no route yet renders internal-note content), only via a raw API call. Fixed with `20260101008600_messages_internal_flag_lock.sql` (`is_internal = false` added to the participant WITH CHECK; ops staff unaffected, they write through a separate broad policy). Also closed a real coverage gap: no prior test touched `messages`/`conversations`/`conversation_participants` at all. New `tests/db/messaging-security.test.ts` covers the lock, normal message round-trip, internal-note visibility (ops only), and cross-tenant isolation (an unrelated third party has zero read/write access). 405/405 tests, verified on a fresh reset plus one more run without reset. |
| `3a2ef1d` | S | Message attachments (see table row above for full description). |
| `72c2865` | T | Listing lifecycle self-approval bugs (see table row above for full description). |
| `2ac548a` | U | Multi-species audit — no fix needed (see table row above for full description). |
| `6b1745d` | V | Taxonomy audit — no fix needed (see table row above for full description). |
| `0c31009` | W | Search/discovery indexing (see table row above for full description). |
| `3902a60` | X | Application/handover state machine audit — no fix needed (see table row above for full description). |
| `b157d8a` | Y | Handover/ownership transfer audit — real gap documented, not built (see table row above for full description). |
| `34de6ed` | Z | Quotation/pricing test-coverage gap (see table row above for full description). |
| `f3e636e` | AA | Fundraising outcome-status self-declaration gap (see table row above for full description). |
| `74b4655` | AB | Fundraising compliance gating audit — no fix needed (see table row above for full description). |
| `fe17e95` | AC | Outbox/email delivery reliability audit — nothing to harden, documented (see table row above for full description). |
| `8b89243` | AD | Atomic transport status changes, server-stamped actor (see table row above for full description). |
| `7c18d3e` | AE | Audit-log actor lock, atomic route assignment (see table row above for full description). |
| `ff74ea0` | AF | Feature flags/market configuration audit — no fix needed (see table row above for full description). |
| `3552611` | AG | Concurrency races closed: double-sell reservations, duplicate conversations (see table row above). |
| `bfa0ec6` | AH | User data export audit + extension (see table row above for full description). |
| `0711f35` | AI | Real account deletion execution (see table row above for full description). |
| `0f72b7f` | AJ | Support/diagnostic tooling audit — deferred to Stage BL (see table row above). |
| `ac3f54c` | AK | Backup/DR documentation (see table row above for full description). |
| `fe6e329` | AL | Migration quality re-audit (see table row above for full description). |
| `bcccb0b` | AM | Test factory for deterministic transport-request fixtures (see table row above). |
| `7ed2ff6` | AN | Bounded load/volume readiness (see table row above for full description). |
| `8d2ff9c` | AO | Document review/substitution lock (see table row above for full description). |
| `dfe6aeb` | AP | Release-candidate verification + report. Re-ran the full verification chain fresh for this report (not carried over from earlier stages' output): 98 migrations, no duplicate prefixes, fresh `db reset` clean, 490/490 tests on two consecutive clean runs, `tsc` clean, `build` clean, route-tree consistent, full-repo lint at the same 38-error/13-warning baseline first documented at Stage K (no new regressions across the whole R–AP range). New `docs/BACKEND_RELEASE_CANDIDATE_REPORT.md` closes out the first supplemental queue with a summary of all 18 stages (10 real fixes, 6 confirmed-correct audits, 2 correctly-deferred unbuilt features) and the known-open-items list carried into the second supplemental queue. Test suite grew 395 → 490 (+95) across R–AP. This closes the first supplemental queue (Stages R–AP); continuing directly into the second supplemental queue (Stage BA onward) per standing instruction. | Security red-team pass. Found a real, serious gap: `transport_documents`' requester policy is `FOR ALL` with zero column restriction beyond row ownership — a requester could self-set `status='accepted'` and forge `reviewed_by`/`reviewed_at` (faking an ops review that never happened), and more seriously, **substitute the underlying file after a real ops approval** — swap `file_url` to point at a different Storage object while `status`/`reviewed_by`/`reviewed_at` still showed the original ops approval, making a never-reviewed replacement document (passport scan, health certificate, transport authorisation) appear ops-approved. Fixed with `20260101009600_transport_document_review_lock.sql`: a requester keeps the legitimate ability to upload new documents and replace a not-yet-accepted one, but can never set/forge review fields, and an accepted document is completely locked from requester-side changes. Also closed a smaller, related gap found while writing the trigger: `reviewDocument()` (ops-side) trusted a client-supplied `reviewedBy` — one ops account could credit a different ops profile as the reviewer; now always server-stamped. New `tests/db/document-review-integrity.test.ts` proves both, plus the legitimate not-yet-accepted replacement path stays allowed. Hit one transient, unrelated failure while verifying repeatability — a `transport_draft_creation` rate-limit exhaustion from the sheer cumulative volume of `test:db` runs across this unusually long session (Stage J's thresholds are calibrated for a normal session, not 50+ consecutive non-reset runs) — confirmed not a real regression via a fresh reset plus two more clean runs. 490/490 tests. | Bounded load/volume readiness. Audited every ops/admin/moderation list query (`listOpsTransportRequests()`, `listReports()`, `listModerationCases()`, etc.) for the "returns the whole table as data grows" risk — none call `.limit()` explicitly, but confirmed they're not actually unbounded: `supabase/config.toml`'s `api.max_rows = 1000` caps every PostgREST response platform-wide regardless of app code, and every one of these queries is already ordered `created_at desc`, so the cap degrades gracefully. Documented in `docs/PRODUCTION_SETUP.md` §3b, including the real, actionable production gap this surfaces: `max_rows` is a local `config.toml` setting that does **not** automatically carry over to a hosted production project — must be independently verified/configured via the Supabase dashboard before launch. No code change; a genuine "already protected, but by platform config that needs re-verifying in production" finding, not a code gap. | Test factories/deterministic fixtures. Found 5 test files (`workflows`, `security-regressions`, `transport-lifecycle-scenarios`, `concurrency-hardening`, `data-export`) had converged on the same ~10-line "insert a fresh Poland→Germany transport_requests row" block, each with its own slightly different (and in one case `Date.now()`-only, theoretically collision-prone in a fast run) `request_number` scheme. Added `createTestTransportRequest()` to `tests/db/helpers.ts` — deterministic, collision-free `request_number` (timestamp + random suffix), throws on failure like `as()` already does rather than returning a `{data,error}` tuple. Refactored the two files added this session (`concurrency-hardening.test.ts`, `data-export.test.ts`) to use it, proving the factory in real use; deliberately left the three older, already-established files untouched rather than churning already-correct, already-reviewed tests for a stylistic-only gain. 478/478 tests, verified on a fresh reset plus one more run without reset. | Migration quality/upgrade path. Re-audited all 96 migration files for the three concrete unsafe patterns that actually matter for a future production push: `add column ... not null` without a `default` (zero found — every one pairs with a default, the safe pattern), `alter type ... add value` used elsewhere in the same transaction (zero found — Postgres would reject this outright, confirmed both instances are purely additive), and genuinely destructive drops (zero — every `drop policy`/`drop trigger`/`drop function` is immediately followed by a `create` in the same file, never a data-destroying drop). Also documented a real, previously-undisclosed limitation: `CREATE INDEX CONCURRENTLY` cannot be used in this repo's transactional migration flow, so the plain `CREATE INDEX` statements added in Stages N/W would lock their target table for the duration against a production database with real rows (not a concern for the current empty-database day-one push, but a real future constraint). Updated `docs/PRODUCTION_SETUP.md` §3 with all of the above and corrected its stale migration count (54 → 96). No code change. | Backup/disaster-recovery documentation. New `docs/BACKUP_AND_DISASTER_RECOVERY.md`: documents the two independent recovery paths (schema replay from migrations vs. real data restore, which migrations can never provide), the local recovery commands actually verified this session (`db reset`, `db dump`, `db query`), the concrete checklist for whoever picks a production Supabase plan tier (PITR vs. daily-only, RPO/RTO, Storage-bucket coverage — none decided yet, correctly not guessed at), and the fix-forward incident-response convention for a bad migration. Explicitly does not claim backups are active (no production project exists) or invent an RPO/RTO/retention number. No code change — a documentation-only stage matching its own name. | Support/diagnostic tooling. Confirmed no dedicated support-ticket/case system exists anywhere (`support_case`/`support_ticket`/`diagnostic` all grep to zero) — `reports`/`moderation_cases` (built in earlier sessions) already serve as the real, working "user flags a problem, staff triages it" mechanism today, and a formal, separate support-case system with staff-workload/access-justification semantics is explicitly its own dedicated later stage (Stage BL, "support case management," in the second supplemental queue). Building a second, competing concept now would duplicate that stage rather than close a reachable gap. No code change; deferred to BL by design, not oversight. | Deletion/anonymisation execution. Closed the real, repeatedly-flagged gap first documented in Stage O: `markDeletionRequestProcessed("processed")` used to only flip the request's own status flag, never actually touching the account. New `execute_account_deletion()` admin-only RPC (`20260101009500_account_deletion_execution.sql`) does real anonymisation (never a hard delete — Stage L's audit already established audit-trail FK columns have no `ON DELETE` action, so a hard delete would fail outright for any account with history): clears `display_name`/`first_name`/`last_name`/`email`/`phone`/`avatar_url`/`city`/`country`, sets new `profiles.is_deleted`/`deleted_at` columns, and refuses while a real unresolved obligation exists (active transport request, active reservation, active application, or un-transferred organisation ownership) — every check a real, reachable table this schema already has, nothing speculative. `dashboard.admin.users.tsx`'s stale "still needs Supabase Studio" copy corrected to describe what the button now actually does. New `tests/db/account-deletion-execution.test.ts`: the successful-anonymisation path is tested against a throwaway signed-up account (`anon().auth.signUp()`, email confirmation disabled locally), never against any of the ~10 shared seeded personas this whole suite reuses — anonymising a shared persona would corrupt it for every later test file in the same run, the same lesson as Stage J's rate-limit exhaustion bug. Blocker tests use existing personas safely since they only prove rejection. 478/478 tests, verified on a fresh reset plus one more run without reset. | User data export. Audited `exportMyData()` (`src/lib/queries/privacy.ts`) against the exclusion requirements (must never include reporter identity, internal moderation notes, another user's private data, unrelated org data) — confirmed clean by construction: every query is filtered by the caller's own id column, and reports/moderation_cases/internal_notes/organisation-owned tables are never selected at all. Added two real, missing-but-safe completeness categories (sent messages excluding internal notes, received notifications) since they're unambiguously the user's own data and trivially RLS-safe. New `tests/db/data-export.test.ts` proves the export's own query shape returns real data and that a forged `userId` argument (simulating a client bug) still can't pull another user's data. While writing the cross-tenant test, hit a false positive from accumulated test-fixture pollution (an earlier stage's persona had legitimately accumulated dozens of "named recipient" `transport_parties` rows across many non-reset `test:db` runs this session) — not a real bug, confirmed by direct inspection of `transport_parties` and the `is_named_transport_party()` policy; fixed by isolating the test to a freshly-created fixture instead of relying on global persona state. 469/469 tests, verified on a fresh reset plus one more run without reset. | Idempotency/concurrency audit. Walked through the concrete concurrent-actor scenarios named for this stage and found three real races, closed in `20260101009400_concurrency_hardening.sql`: (1) `reservations` had no uniqueness on `animal_id` — two different *approved* applications for the same animal (an org can approve more than one buyer's application for a popular puppy; nothing prevented that) could both become active reservations, a real double-sell race. Closed with a partial unique index (`where status <> 'cancelled'`). (2) `start_transport_conversation()` did a classic check-then-insert with no protection — two concurrent callers (e.g. the requester and ops both opening the thread for the first time) could each create a duplicate conversation for the same transport request. Closed with a partial unique index (one conversation per transport request). (3) `start_application_conversation()` has the same check-then-insert shape, but its natural key is (animal, buyer) — not expressible as a plain unique index since multiple buyers legitimately each get their own conversation about the same animal. Closed with a transaction-scoped `pg_advisory_xact_lock` keyed on the (animal, buyer) pair. New `tests/db/concurrency-hardening.test.ts` fires genuinely parallel requests via `Promise.all` (not sequential calls) and proves each race resolves safely: exactly one reservation wins, both conversation calls converge on the same id. 461/461 tests, verified on a fresh reset plus two more runs without reset (a third run for this concurrency-touching stage, per the standing testing discipline). | Feature flags/market configuration. Confirmed `FUNDRAISING_ENABLED` (`src/lib/fundraising-flag.ts`) is the only real feature flag in the codebase (grepped every `import.meta.env.VITE_*` usage) and it was already fully audited in Stage AB. Audited `markets` (RLS trivially correct — single admin writer, same shape as taxonomy) and confirmed its per-market state columns (`marketplace_state`, `adoption_state`, `transport_*_state`, `fundraising_state`, `enabled`) are read *only* by `src/lib/queries/markets.ts` (the admin settings display page) — nothing in the real marketplace/transport/adoption flow checks a market's state before allowing an action, not even `enabled`. Purely a forward-looking admin roadmap tracker today, not a security boundary, the same "schema exists, no real enforcement built yet" shape as Stages U/V. Building enforcement now (blocking actions from an under-ready market) would be inventing new product behaviour with no existing reachable trigger for it. No code change. | Audit-log quality. Found the general-case gap Stage AD's fix was one instance of: "ops staff and admins write audit logs" checked `is_ops_staff()` but never restricted `actor_profile_id` — any ops/admin account could credit a *different* profile as an audit entry's actor, project-wide. Locked it at the RLS layer (`actor_profile_id = auth.uid()`) in `20260101009300_audit_logs_actor_lock_and_route_assignment_rpc.sql`; verified every existing SQL-side `audit_logs` insert already used `auth.uid()` correctly (440/440 pre-existing tests still passed unchanged after the lock). Same migration also fixed `assignRequestToRoute()` (`src/lib/queries/routes.ts`): two non-atomic client-side writes (insert `route_assignments`, update `transport_requests.assigned_route_id`) with the second one's error never checked — a failure there silently left the two tables inconsistent — plus a client-supplied `assigned_by`. Folded into a new `assign_request_to_route()` SECURITY DEFINER RPC, matching Stage AD's pattern; both call sites (`dashboard.operations.matching.tsx`, `dashboard.operations.routes.$id.tsx`) updated. New `tests/db/audit-log-and-route-assignment-integrity.test.ts`. 450/450 tests, verified on a fresh reset plus one more run without reset. | Transactional domain events. Found `changeOpsRequestStatus()` (`src/lib/queries/operations.ts`) did 3 separate client-side network round-trips (update `transport_requests`, insert `transport_status_history`, insert `audit_logs`, the last one's error not even checked) — not atomic, and both the history and audit rows trusted a client-supplied `actorId` instead of the real caller, the same forgeable-actor shape already closed for `notifications.actor_profile_id` in an earlier session. Fixed with a new `change_ops_request_status()` SECURITY DEFINER RPC (`20260101009200_change_ops_request_status_rpc.sql`) doing all three writes in one Postgres transaction with `auth.uid()` always the real actor; `changeOpsRequestStatus()` and its one real call site (`dashboard.operations.requests.$id.tsx`) updated to match, dropping the now-removed `actorId` param. New `tests/db/change-ops-request-status.test.ts`: atomic write proven end-to-end, non-ops and driver callers rejected, actor is always the real caller, a nonexistent request id errors rather than silently no-oping. 440/440 tests, verified on a fresh reset plus one more run without reset. | Outbox/email delivery reliability. Confirmed there is no outbound email delivery mechanism anywhere in this codebase to harden — no email provider SDK in `package.json` (checked for resend/nodemailer/sendgrid), and `email` only ever appears as a stored data field (`organisation_invitations.invited_email`, `profiles.email`, `transport_parties.external_email`), never actually sent. The organisation invitation flow (Stage E) works entirely via an in-app token link (`_public.invitations.$token.tsx`), not email. Building a real outbox/retry/queue system now would mean inventing a provider integration from scratch, which needs real credentials this session is explicitly barred from using ("do not use production credentials") — there is nothing reachable to harden, only a larger, genuinely-unbuilt feature to defer, the same shape as Stage Y's ownership-transfer finding. Documented rather than built. No code change. | Fundraising compliance gating. Audited every route that touches fundraising for the `FUNDRAISING_ENABLED` flag (`src/lib/fundraising-flag.ts`): every real content route (`dashboard.foundation.fundraising.{index,$id}.tsx`, `_public.fundraising.{index,$id}.tsx`, `dashboard.admin.fundraising.tsx`) checks it and shows an honest "not yet available" state; the two routes with no check (`dashboard.foundation.fundraising.tsx`, `_public.fundraising.tsx`) are pure layout/`<Outlet />` wrappers with no content of their own, correctly not needing one. Confirmed the real compliance boundary — "never claim real money moved" — is enforced at the RLS layer independent of the frontend flag: `fundraising_contributions`' INSERT policy forces `is_simulated = true` regardless of what a raw API call requests, so even a complete frontend-flag bypass could never insert a real (non-simulated) payment record. Also checked `markets.fundraising_state`: purely an admin-facing readiness display badge (`dashboard.admin.settings.tsx`), not consumed by any gating logic — correctly informational-only, not a security boundary. No code change; the one real gap in this feature area was already found and fixed in Stage AA. | Disabled payment-provider abstraction. Audited fundraising (the closest thing this schema has to a payment-adjacent feature, correctly disabled behind `src/lib/fundraising-flag.ts` pending a real provider) and found a real integrity gap: an org could set its own campaign's status to `target_reached`/`partially_funded` — but per `docs/FUNDRAISING_POLICY.md`, these are meant to be *outcomes* of real fundraising activity ("active → (target_reached \| partially_funded \| expired)"), not org-declarable states, and both labels are shown publicly with no correlation to the campaign's real computed total. An organisation could publicly claim "Target reached ✓" on a campaign that collected nothing. Fixed with `20260101009100_fundraising_outcome_status_lock.sql`, moving both to the same admin-only side of the line the policy already drew for approved/suspended/refund_review/completed. New test in `tests/db/fundraising.test.ts` proves the org is blocked from both while admin retains access. 435/435 tests, verified on a fresh reset plus one more run without reset. | Quotation/pricing hardening. Audited `pricing_rules` and quotation creation and confirmed the design already correct: `pricing_rules` is ops-only-write, and the client-side estimate calculator (`src/lib/queries/pricing.ts`) only ever reads active rules to compute a display-only range — never trusted as the charged amount, which ops always sets manually and directly on `quotations` (already column-locked in Stage L). What was genuinely missing: zero test coverage existed for either boundary. New `tests/db/pricing-and-quotation-security.test.ts`: active pricing rules are public, inactive ones are hidden from anon/ordinary users, only ops can write them; and — a gap Stage L's field-lock test didn't cover — a customer cannot INSERT a new quotation at all (not just restricted on update). No code change, closing a real coverage gap rather than manufacturing a fix for an already-correct design. 429/429 tests, verified on a fresh reset plus one more run without reset. | Handover/ownership transfer. Audited `animal_ownership_history` (`20260101000900_animals.sql`): has RLS (owner/org/admin SELECT, admin-only write) but zero consumers anywhere in `src/` — confirmed by grep, same as `handover_protocols` before it. Went one level deeper than the Stage S precedent before concluding: checked whether any real code path ever actually changes `animals.owner_profile_id`/`organization_id` after creation (reservation completion, adoption finalisation, rehoming completion) — none does. Unlike Stage S (attachments), where the underlying action being extended — sending a message — already existed and just needed a file parameter, here the underlying action itself (transferring an animal's real ownership record on a sale/adoption/rehoming) was never built at all: no RPC, no "mark as transferred" UI action, nothing. Building only a history-recording trigger on top of that would watch for a column change that no code path ever produces — permanently dead code, not a fix. Correctly deferred as a real, documented product gap (a missing business action, not a bug in existing reachable code) rather than invented as new unrequested feature scope. No code change. | Application/handover state machines. Audited both halves. Application side: confirmed org-side `buyer_applications.status` transitions are intentionally unrestricted (the org is the trusted decision-maker for its own applications by design) and safe — verified `reservations`' INSERT policy already requires `buyer_applications.status = 'approved'` **and** `owns_org()` before a reservation can be created, so the org can't be tricked by anything client-controlled; the buyer side was already correctly locked in Stage F/T. Handover side: `handover_protocols`/`compliance_reviews` are single-writer (ops-only, `for all`), structurally ruling out the self-approval bug class the same way Stage V's taxonomy tables did. The one real, related concern — no state-machine enforcement on which `transport_requests.status` values an assigned driver can set (found Stage M) — remains correctly deferred to its own dedicated later stage ("route/stop state machines" / "state-machine/chaos tests" in the third supplemental queue), not attempted here under a differently-scoped stage name; a partial/rushed version now risks exactly the "blocked the whole workflow" failure this session already hit once before with a first-draft lock trigger. No code change. | Search/discovery. Audited the marketplace browse query path in `src/lib/queries/marketplace.ts` for the same missing-index class Stage N found: `is_published` (animals, litters) and `is_public`/`org_type`/`verification_status` (organisations) gate nearly every public marketplace query and none were indexed — not a guess, the single most-executed query pattern in the whole public marketplace, same justification tier as Stage N's indexes. Added 3 partial indexes (`20260101009000_public_listing_query_indexes.sql`). Also found and deliberately did **not** fix (documented instead): `listPublishedPuppies()`'s `filters?: { breed, country }` parameter fetches the full result set and filters in JS afterwards instead of pushing into the query, and its only real caller (`_public.find-a-dog.tsx`, frontend-owned, outside this session's scope) never actually passes those filters — 100% client-side search over an unpaginated full fetch. Not fixed here: the caller doesn't use the filters today (wiring them server-side would be dead code with no observable effect), and adding a `LIMIT` without corresponding pagination UI would silently drop listings with no way for a user to see the rest — that UI work belongs to whoever owns marketplace UX next. No new tests needed (index-only change, doesn't affect query results). 422/422 tests, verified on a fresh reset plus one more run without reset. | Taxonomy. Audited `breeds` (`20260101000600_breeds.sql`) and `product_service_categories` (`20260101005700_product_service_categories.sql`, itself deliberately schema-only/disabled per its own header, matching the multi-species pattern from Stage U) — the full extent of "taxonomy" in this schema; no separate tags/labels table exists. Both are single-writer (admin-only INSERT/UPDATE/DELETE, no other role has any write policy at all), which structurally rules out the self-approval/column-lock bug class this session has repeatedly found elsewhere — there is no second, less-trusted writer whose column access could need restricting. No code change; a correct, verified "nothing to fix" outcome. | Multi-species. Audited the existing `species` table/`species_id` columns (`20260101005300_species.sql`, built in a prior session, predating this session's Stage B baseline) rather than assuming a gap existed just because the stage name suggested one. Confirmed: the foundation is deliberately schema-only ("Species-aware forms and per-species field/document/eligibility rules are explicitly NOT built in this pass" — the migration's own words), and `grep -rl "species_id\|speciesId" src/` returns zero matches — no query or route anywhere sets or reads it. Considered two theoretical gaps (a listing could reference a disabled/not-yet-activated species; an animal's `breed_id` and `species_id` could point to different species) and rejected fixing either: neither is reachable through any real UI (nothing sets `species_id` at all), neither has any real consequence today (no query filters or displays by species, and no non-dog breed rows exist in the schema at all yet, making the breed/species mismatch currently impossible with real data), and CLAUDE.md's engineering rules explicitly say not to add validation for scenarios that can't happen or design for hypothetical future requirements. No code change — the correct, honest outcome of this stage's audit, matching Stage O's precedent for "audited, found already appropriately scoped." | Listing lifecycle. Found and closed two real self-approval bugs, the same bug class as the Stage L quotation fix (an INSERT/UPDATE policy checks row ownership but not the initial value of a status/decision column): (1) `rehoming_reviews`' INSERT policy never restricted `admin_status` — an owner could insert their own review already `admin_status='approved'`, and since `animals.is_published` is fully owner-controlled, this made a private-rehoming listing publicly visible without ever going through real admin review, a direct violation of CLAUDE.md rule #5 ("Private rehoming requires a separate moderated workflow"). Fixed with `20260101008800_rehoming_review_self_approval_lock.sql`. (2) `buyer_applications`' write-lock trigger (`prevent_buyer_writes_to_org_controlled_fields`, Stage F) only ever fired on UPDATE — a buyer's INSERT could set `status='approved'` directly, bypassing organisation review entirely. Fixed with `20260101008900_buyer_application_insert_status_lock.sql`, extending the same trigger to INSERT (same fix shape as Stage G's `set_moderation_case_appeal_deadline`). Neither reachable through the real UI, both real via a raw API call. Found and fixed a real regression while doing this: `tests/db/fundraising.test.ts`'s `buildEligibleFixture()` and one other fixture used the exact same self-approval shortcut to set up test data — updated both to submit as the buyer then approve as admin, a realistic path. New `tests/db/listing-lifecycle-security.test.ts`. 422/422 tests, verified on a fresh reset plus one more run without reset. | Attachments. `messages.attachment_url` existed on the schema since the original messaging migration but was completely unwired — no Storage bucket, no RLS, no upload UI (confirmed by grep: zero references anywhere in `src/` before this stage). Added the missing backend layer following the established private-bucket-plus-signed-URL pattern (`transport-documents`/`welfare-case-documents`): new `message-attachments` bucket, RLS scoped via the existing `is_conversation_participant()` helper so an attachment is exactly as protected as its conversation (`20260101008700_message_attachments_storage.sql`). Wired a real, minimal UI in the shared `src/components/chat-thread.tsx` (attach-a-file button, upload on send, signed-URL-on-demand rendering — never a bare link) since it was genuinely unbuilt, not just unwired at the DB layer; `chat-thread.tsx` is a shared component outside the frontend-worktree's excluded-file list. `tests/db/message-attachments.test.ts`: upload + signed URL for the uploader and the other participant, ops access, and two negative cases (an unrelated user can neither read nor upload into someone else's conversation folder). 413/413 tests, verified on a fresh reset plus one more run without reset. |

**First supplemental queue**: R (messaging/conversation security), S (attachments), T (listing
lifecycle), U (multi-species), V (taxonomy), W (search/discovery), X (application/handover state
machines), Y (handover/ownership transfer), Z (quotation/pricing), AA (payment abstraction,
disabled), AB (fundraising gating), AC (outbox/email jobs), AD (domain events), AE (audit quality),
AF (feature flags), AG (idempotency/concurrency audit), AH (data export), AI (deletion/
anonymisation execution), AJ (support tooling), AK (backup/DR docs), AL (migration quality), AM
(test factories), AN (load readiness), AO (security red-team pass), AP (release-candidate report).

**Second supplemental queue**: BA–CI (see above) plus a final post-CI adversarial review pass.

## Known open items carried forward

- `driver_transport_job_view` and the new timeline queries don't yet expose the multi-animal list
  (`transport_request_animals`) — a driver/timeline viewer still only ever sees the primary/first
  animal snapshot on multi-animal requests. Documented as a known non-goal in
  `docs/adr/TRANSPORT_DATA_MODEL.md`; candidate for a future pass if multi-animal requests become
  common in practice.
- Quotation RLS column-scoping gap: **fixed in Stage L** (see table above). No longer open.
- Driver status state-machine enforcement: **fixed in Stage CC** (see table above). No longer open.
- ~127 foreign-key columns across the schema have no covering index (found during Stage N's audit;
  see the table row above). Deliberately not indexed now — the local seed dataset is too small for
  EXPLAIN to distinguish a real need from a guess, and indexing all of them would be exactly the
  "blind indexing" this stage was told to avoid. Revisit once real usage data (pg_stat_statements
  or equivalent) can identify which are actually hot; the full list is in
  `supabase/migrations/20260101008500_marketplace_listing_indexes.sql`'s reasoning comment and can
  be regenerated via `supabase db dump --local --schema public` + a diff against `pg_indexes`.
- Marketplace search (`/find-a-dog`) does 100% client-side filtering over an unpaginated full
  fetch of every published puppy (`listPublishedPuppies()`'s `breed`/`country` filter parameters
  are never actually passed by its only caller, and there's no `LIMIT`/pagination at all). Found
  Stage W; not fixed because the route file is frontend-owned (outside this session's scope) and
  because adding a `LIMIT` without pagination UI would silently drop listings. Candidate for
  whoever next works on marketplace UX/scale.
- `animal_ownership_history` has RLS but no consumer, and — more fundamentally — no code path
  anywhere ever actually changes `animals.owner_profile_id`/`organization_id` after creation, so
  there is no real "ownership transfer" business action to record yet. Found Stage Y; a real
  product gap (a missing feature: what should happen to an animal's ownership record when a sale/
  adoption/rehoming actually completes), not a bug in existing reachable code. Candidate for a
  future phase once that business action itself is designed and built.
- No outbound email delivery mechanism exists anywhere (no provider SDK, `email` fields are only
  ever stored, never sent — the org-invitation flow works entirely via an in-app token link).
  Found Stage AC; needs a real provider integration with real credentials this session cannot
  configure, not something to build/fake here. Candidate for a future phase once a provider is
  approved.
- Error-message translation (Stage BQ, new `src/lib/errors.ts`) is wired into only the two real
  call sites in `_public.transport.request.tsx` — the one customer-facing file explicitly carved
  out of the frontend-file boundary for transport work. 69+ other `toast.error(err.message)` call
  sites across frontend-owned dashboard/marketplace routes still show raw, untranslated Postgres
  errors. Left alone deliberately rather than a broad unrequested frontend sweep; the shared
  utility now exists for whoever does that pass next.
- `src/lib/supabase/types.ts` (the hand-maintained stub, confirmed by its own header comment) has
  no `Enums` section at all — every Postgres enum-typed RPC parameter/column (e.g.
  `advance_transport_job_status`/`change_ops_request_status`'s `p_new_status`, really
  `Database["public"]["Enums"]["transport_status"]` in the real generated schema) is currently
  typed as a plain `string` in the stub, losing compile-time protection against an invalid status
  value (Postgres itself still rejects one at runtime, so this is a lost safety net, not a live
  bug). Confirmed via a real `supabase gen types typescript --local` diff during Stage BR: 6044
  generated lines vs. 1722 hand-written, every RPC name still accurate, only the type fidelity has
  drifted. Full reconciliation is IR-5's dedicated scope — not attempted piecemeal here.

## Files likely to conflict with `ux-marketplace-frontend-pass`

`_public.transport.request.tsx` (transport-specific, explicitly carved out of the frontend
session's excluded-file list — touched again in Stage BQ for error-message translation),
`dashboard.driver.index.tsx`/`src/lib/queries/driver.ts` (Stage BG, evidence upload wiring), and
`dashboard.breeder.{reservations,transport,settings,index}.tsx`/`dashboard.foundation.
{settings,index}.tsx`/`src/lib/queries/profile.ts` (Stage CE, data-access consolidation — swapped
a duplicated inline query for an existing shared function, no behavioural change) are the only
frontend-adjacent files touched this session; none are marketplace UX, so low real conflict risk
with `ux-marketplace-frontend-pass`'s stated scope (`src/components/cards.tsx`, `site-chrome.tsx`,
`src/routes/_public.*` marketplace pages, `dashboard.buyer.*`, `src/lib/i18n/**`,
`docs/MARKETPLACE_UX_AUDIT.md`). No other frontend-owned file has been touched.

## How to resume if this session stops mid-stage

Check the table above for the last committed row, then re-read this file's "Remaining stages"
section and continue from the next uncompleted stage. Always re-run the Stage A baseline checks
(`git status`, `db reset`, `test:db` ×2, `tsc`, `build`) before resuming feature work.
