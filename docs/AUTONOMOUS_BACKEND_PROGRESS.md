# Autonomous Backend Progress

Running log for the full-day autonomous backend/operations/launch-hardening session. Kept concise
and current so another session can resume without reconstructing the day's work. See
`docs/BACKEND_TAKEOVER_LOG.md` for the session before this one (transport-domain hardening,
Phases 0–9) and `docs/adr/TRANSPORT_DATA_MODEL.md` for the architecture it produced.

## Starting point

- **Starting HEAD**: `933e1ca` — "Update docs for the completed transport-data-model hardening pass."
- **Baseline** (re-verified at the start of this session): working tree clean, 76 migration files
  (no duplicate numeric prefixes), 170/170 database/API tests passing on two consecutive runs,
  `tsc --noEmit` clean, `npm run build` clean.
- **Correction**: an earlier report said "77 migrations, range 20260101006500–20260101007400" —
  that range is 10 migration versions, and the real filesystem count is 76. Corrected here rather
  than treated as a discrepancy to chase.

## Phases/stages completed this session

(One row per commit, newest last. "Stage" letters match the mega-prompt that superseded the
Phase-10-onward numbering mid-session — same content, renamed.)

| Commit | Stage | Summary |
|---|---|---|
| `f5cb8c7` | B (Phase 10) | Operations calendar: `src/lib/queries/calendar.ts` (unscheduled queue, date-range route listing, deterministic conflict detection), `dashboard.operations.calendar.tsx` real UI (day/week, filters, conflicts banner), `tests/db/calendar-scheduling.test.ts`. 184/184 tests. |
| `fd33235` | C (Phase 11) | Real transport timeline (`getCustomerTimeline`/`getOpsTimeline`/`getDriverTimeline`, `TransportTimeline` component) sourced only from `transport_status_history`/`transport_request_amendments`. Found + fixed a real gap: named `transport_parties` (not the legacy inline columns) had no visibility into the request/history/amendments at all — added `is_named_transport_party()` + 3 policies. `tests/db/transport-timeline.test.ts`. 199/199 tests. |
| `7bac2c0` | D (Phase 12) | Real urgent welfare/rescue workflow: `welfare_cases` + `welfare_case_documents` tables, eligibility gated to verified foundation/shelter/rescue orgs, ops acknowledge/review actions, `convert_welfare_case_to_transport_draft()` reusing `create_transport_draft()`. `dashboard.foundation.urgent.tsx` (real) + new `dashboard.operations.welfare-cases.tsx`. `tests/db/welfare-cases.test.ts`. 221/221 tests. |
| `f40ac47` | E (Phase 13) | Real organisation team/invitation management (`organisation_invitations`, 3 new member roles, `status` active/suspended on `organisation_members`, tier-protected invite/remove/suspend/role-change RPCs). Found + fixed a real gap: `owner_user_id` could be changed by any org owner via a plain update, silently transferring/orphaning ownership — locked to admin-only via trigger. `dashboard.foundation.team.tsx` (real) + new `_public.invitations.$token.tsx`. `tests/db/organisation-team.test.ts`. 255/255 tests. |
| `d52da87` | F (Phase 14) | Completed the adoption questionnaire on `buyer_applications` (landlord permission, veterinary plan, consent metadata, org supplemental answers jsonb, `internal_notes`, `draft`/`interview_planned`/`expired` statuses). Found + fixed a real gap: buyers had unrestricted `for all` RLS on their own application row, so could self-approve/forge `breeder_response`/forge `internal_notes` — added a default-deny lock trigger. `dashboard.foundation.applications.tsx` now renders the full questionnaire + internal notes + a transport-draft action on approval. `tests/db/adoption-questionnaire.test.ts`. 273/273 tests. |
| `cca8f02` | G (Phase 15) | Real user-facing moderation decisions and appeals: `affected_profile_id` (auto-populated for user/animal_listing targets), `public_decision_summary`, `appeal_deadline` (14 days, fixed a bug where it only fired on UPDATE not INSERT), `my_moderation_case_view` (fixed a bug where `security_invoker=true` inherited an always-false RLS result — switched to a definer-style view like `public_transport_requests`), `moderation_appeals` with a same-moderator-conflict check. New `_public.moderation.$caseId.tsx` + extended `dashboard.admin.moderation.tsx`. `tests/db/moderation-appeals.test.ts`. 299/299 tests. |
| `3026f22` | H (Phase 16) | Real notification preferences (`notification_preferences`, opt-out default, mandatory `security` category, `get_notification_preference()`/`create_notification_if_enabled()`), scoped to the 4 notification types that actually exist in the codebase (not a fabricated full category list). Replaced the "coming soon" placeholder on both breeder/foundation settings pages. `tests/db/notification-preferences.test.ts`. 320/320 tests. |
| `229cb35` | I (Phase 17) | Real `dashboard.admin.organisations.tsx` (suspend/restore + new `is_featured` flag, with a lock-trigger fix for owner self-featuring), real `dashboard.admin.settings.tsx` (markets table, previously zero UI), real `dashboard.buyer.scheduled.tsx` (scheduled-or-later transport list + timeline). Left 3 document-library placeholders honest (no backing schema, out of Stage I's priority list). `tests/db/admin-placeholders.test.ts`. 335/335 tests. |
| `77c4252` | J (Phase 18) | Real per-actor rate limiting (`rate_limit_events` + `enforce_rate_limit()`) applied to 7 previously-unprotected abuse vectors (reports/messages/welfare_cases/applications via triggers; transport-draft/amendment/invitation via RPC calls). Found + fixed a missing GRANT (same `auto_expose_new_tables=false` class as before) and — found by actually running the suite twice — initial thresholds tighter than legitimate test-fixture usage, breaking repeatability; raised with margin. `docs/RATE_LIMITING_AND_ABUSE_PROTECTION.md`. `tests/db/rate-limiting.test.ts`. 358/358 tests, verified on 3 consecutive runs without reset. |
| `545972d` | K (Phase 19) | CI hardening: duplicate-migration-prefix check, route-tree consistency check, and a repeated `test:db` run for repeatability in CI — all matching real gaps found this session. A full-repo `eslint .` surfaced ~38 pre-existing errors in untouched files (several `_public.*`, frontend-owned) — left alone deliberately. |
| `2e05a9f` | L (Phase 20) | Full database consistency/security audit via `npx supabase db dump --local --schema public` + grep (no `psql`/`pg` available in this sandbox). Systematic checks run against all 63 `public` tables: (1) every table has `ROW LEVEL SECURITY` enabled — 63/63, clean; (2) every table with at least one policy also has a `GRANT` reaching `authenticated` — 100% clean (Supabase's default privileges cover new tables automatically; the two earlier `auto_expose_new_tables=false` incidents this session were about the Data API schema cache, not this); (3) every `SECURITY DEFINER` function pins `search_path` — 57/57 clean, no search_path-hijack exposure; (4) view security model (`security_invoker` vs. definer-style) reviewed against the established convention — consistent; (5) foreign keys to `profiles(id)` reviewed: user-owned content correctly `ON DELETE CASCADE`s, audit-trail columns (`reviewed_by`/`assigned_moderator_id`/`uploaded_by`/etc.) correctly have no `ON DELETE` action (`RESTRICT`) so a moderator/ops account can't be hard-deleted out from under the trail it left — by design, consistent with account deletion being its own workflow (`account_deletion_requests`), not a raw `DELETE FROM profiles`; real execution of that workflow is still the deferred Stage AI. Closed the one concrete, already-documented gap found: quotation RLS was row-level only — `"requesters accept or reject their own quotation"` restricted `WITH CHECK (status in (...))` but nothing stopped the same UPDATE from also changing `total_price`/other columns. Added `prevent_requester_writes_to_ops_controlled_quotation_fields()` trigger (`20260101008400_quotation_requester_field_lock.sql`), not reachable through the real UI (`respondToQuotation()` only ever sends `{ status }`) but real via a raw API call. `tests/db/quotation-field-lock.test.ts`. 364/364 tests, verified on 2 consecutive runs without reset. |
| `326a00f` | M (Phase 21) | Operational scenario suite: audited existing coverage first (368 tests across 15 files already exercise draft creation, amendments, quotations, applications, moderation, welfare cases, rate limiting, role suspension) rather than re-deriving what already exists. Found and closed the one genuine coverage gap: no test had ever driven a request through the *full* driver-owned delivery journey (driver_assigned → pickup_confirmed → animal_collected → in_transport → rest_or_care_stop → approaching_destination → delivered → handover_confirmed → completed), nor exercised route assignment/unassignment end-to-end, nor a cross-tenant attack against routes/vehicles/drivers/route_assignments. New `tests/db/transport-lifecycle-scenarios.test.ts`, 3 separate scenario tests (not one giant test). No state-machine enforcement exists yet for driver-set status transitions (a driver can currently set any status value, including skipping steps or going backwards) — noted here as a known gap but *not* fixed in this stage, since "route/stop state machines" and "state-machine/chaos tests" are explicitly named as their own later stages (CB, CG) in the third queue, not Stage M's scope. 390/390 tests, verified on a fresh reset plus 2 more runs without reset. |
| `38cc74f` | N (Phase 22) | Backend performance pass (see table row above for full description). |
| `d6ddd10` | O (Phase 23) | Privacy/GDPR backend audit (see table row above for full description). |
| `10fd7ba` | P (Phase 24) | Launch blocker cleanup / doc reconciliation (see table row above for full description). |
| `1b70015` | Q (Phase 25) | Final verification: 20-point checklist re-run against the real repo state (concurrent-writer check, migration hygiene, fresh `db reset`, `test:db` ×2 at 395/395, `tsc`, scoped + full-repo `eslint` with the ~38 pre-existing baseline unchanged, `build`, route-tree consistency, RLS/grant/search_path coverage, CI parity, no committed production credentials, no push/deploy performed, frontend files untouched, rate limiting wired, privacy audit current, progress log current, docs reconciled) — all 20 green. New `docs/BACKEND_FINALISATION_REPORT.md` closes out the original queue (Stages A–Q) with this checklist plus a summary of Stages L–Q and the known open items carried forward. This closes the original Phase 10–25 queue; continuing directly into the first supplemental queue (Stage R onward) per standing instruction. | Launch blocker cleanup / doc reconciliation. Reconciled `docs/PRODUCTION_READINESS_REPORT.md` (dated 2026-07-17) against everything Stages B–O of this session actually built: corrected 4 stale "Partially ready"/"Blocks launch" entries that are now real (operations calendar, notification preferences, welfare-urgent intake, organisation team management, admin org/settings pages), downgraded "No CI pipeline"/"No automated tests" from launch blockers to resolved/partially-resolved (CI and ~395 DB/API tests both exist now), added a new "Partially ready" entry for account-deletion execution (found during Stage O), and corrected the admin-configurable-settings and notification-preference claims in the lower sections. Added matching staleness pointers to `docs/MVP_TEST_REPORT.md` and `docs/DATABASE_TESTING.md` (the latter's file-by-file test coverage list was frozen at 2026-07-22, 8 files; now 19) rather than rewriting either wholesale. Docs-only stage, no code/schema change — `tsc`/`build` re-verified clean, no `test:db` re-run needed since nothing DB-relevant changed. | Privacy/GDPR backend audit. New `docs/PRIVACY_DATA_LIFECYCLE.md`: catalogued every personal-data-bearing table/column and re-verified (not just read from comments) its actual RLS/column-grant/Storage-bucket protection against a live local instance. Confirmed clean: profiles email/phone lockdown, exact-address RLS, transport_parties external contact, welfare_cases contact/location (entire row org+ops scoped, no public policy at all), both document Storage buckets private with signed-URL-only access, `audit_logs.before`/`after` snapshots checked across every INSERT site and found to log narrow field diffs only (never a full-row PII dump), no third-party analytics/tracking SDK in `package.json`. Confirmed and clearly flagged (not fixed, correctly out of this stage's scope) that account deletion is request-tracking only — `markDeletionRequestProcessed()` flips a status flag but performs no actual deletion/anonymisation, matching the already-tracked Stage AI item — and that there's no platform-wide consent/ToS-version tracking beyond the adoption-specific `consent_version` field, matching the already-tracked "consent versioning" supplemental stage. No code/schema changes — a pure audit stage that found the existing protections were already correct rather than needing a fix, which is itself the useful (and honestly reported) outcome. | Backend performance pass. Found a real N+1 pattern in `src/lib/queries/marketplace.ts`: `listPublishedLitters`/`listApprovedKennels`/`listLittersForKennel`/`listFollowedBreeders` each called `mapLitterRow`/`mapOrgToBreeder` per row via `Promise.all(rows.map(...))`, issuing 2 count/select queries per row (2N for a page of N) instead of 2 total. Fixed by batching into `mapLitterRows`/`mapOrgsToBreeders`, each running exactly one `IN (...)` query across every id on the page regardless of N. Ran a systematic FK-index audit (`supabase db dump` + a Python pass cross-referencing every single-column FK against every existing index): ~130 foreign-key columns across the schema have no index (Postgres only auto-indexes the referenced PK side, never the FK side). EXPLAIN ANALYZE against the local seed data (max ~31 rows in any table) confirms Postgres correctly picks a sequential scan over an index scan regardless of whether an index exists at this row count — meaningfully validating "should this be indexed" via EXPLAIN isn't possible on this dataset. Added only 3 indexes with a non-speculative justification: `animals.litter_id`, `animals.organization_id`, `parent_dogs.kennel_id` — the direct, demonstrated target of the N+1 fix's new batched `IN (...)` queries, which degrade closer to linearly without an index even at moderate row counts, unlike a single-row equality lookup. The other ~127 unindexed FK columns are deliberately left alone and documented in the migration rather than indexed blindly, per this stage's own instruction — candidates for a future pass once real usage data (pg_stat_statements or equivalent) can identify genuinely hot ones. `tests/db/marketplace-listing-batching.test.ts` proves the batched query's client-side grouping produces identical counts to the original per-id queries against real seeded data. 395/395 tests, verified on a fresh reset plus one more run without reset. |

## Supplemental queue appended mid-session

A second large instruction set ("SUPPLEMENTAL OVERNIGHT BACKEND QUEUE," Stages R–AP: messaging
security, attachments, listing lifecycle, multi-species, taxonomy, search, application/handover
state machines, quotation/pricing hardening, payment abstraction, fundraising gating, outbox/email,
domain events, audit quality, feature flags, idempotency audit, data export, account
deletion/anonymisation execution, support tooling, backup/DR docs, migration quality, test
factories, load readiness, a security red-team pass, and a final release-candidate report) was
appended to the original Phase 10–25 queue. Per its own instruction, the original queue (I–Q) is
being finished first, then this session continues directly into R onward without stopping.

## Third queue appended mid-session

A third instruction set ("SECOND OVERNIGHT BACKEND PLATFORM, OPERATIONS, COMPLIANCE AND
RELIABILITY QUEUE," Stages BA–CI: background jobs, document expiry engine, org verification
workflow, driver/vehicle eligibility, route/stop state machines, proof of pickup/delivery,
incidents, service areas, i18n normalisation, consent versioning, support cases, moderation
workload, risk signals, duplicate detection, search read models, error taxonomy, API contracts,
webhooks (disabled), bulk import, archival, Storage cleanup, health checks, SLOs, incident
runbooks, maintenance mode, a migration preflight command, a database invariant catalogue,
state-machine/chaos tests, a permission inventory, data-access consolidation, a tech-debt register,
a final backend PR review, and a second release-candidate report) was appended on top of the
original and first supplemental queues. **Complete as of Stage CH** — see the table above and
`docs/BACKEND_RELEASE_CANDIDATE_REPORT_2.md`.

A fourth and fifth instruction set ("THIRD AND FOURTH LONG-RUN AUTONOMOUS BACKEND QUEUE") was
appended mid-session, ahead of stages BA–CI above: Stages CJA–CJZ (a deep security/compliance
sweep — authorization matrix, public/private projection layer, data-classification catalogue,
server-controlled actor attribution audit, cross-tenant fuzz testing, immutable lifecycle
history, soft-deletion/archival semantics, legal-hold mechanism, deletion-blocker graph,
versioned export manifest, support-access justification, impersonation-policy decision, admin
command log, privileged-reauthentication hooks, secrets/config boundary audit, rate-limit policy
config, messaging abuse controls, notification deduplication, and — cut off mid-transmission at
"CJS: notification/email template versioning" — the rest of CJS onward and the entire CKA–CKZ
queue were never received). Per its own instruction and the standing rule throughout this session
("do not manufacture schema merely to create a commit," "do not build speculative infrastructure
with no reachable product use"), each CJ stage is being treated with the same discipline as every
earlier stage: real audits close with a documented "no fix needed" outcome when the design is
already correct, and only demonstrated, reachable gaps get an actual code change — not a
mechanical checklist run through regardless of findings. This queue starts now that BA–CH is
complete, per its own stated precedence ("previously assigned order remains authoritative").

## Remaining stages (not started this session)

**Original queue**: complete (Stages A–Q, see table above and
`docs/BACKEND_FINALISATION_REPORT.md`).

**First supplemental queue**: complete (Stages R–AP, see table above and
`docs/BACKEND_RELEASE_CANDIDATE_REPORT.md`).

**Second supplemental queue**: complete (Stages BA–CH, see table above and
`docs/BACKEND_RELEASE_CANDIDATE_REPORT_2.md`).

**Third/fourth supplemental queue**: in progress. Stages CJA through CJJ complete (see table
above). Stages CJA through part of CJS have a full definition already in context (a deep
security/compliance sweep — authorization matrix, data classification, actor-attribution audit,
cross-tenant fuzzing, immutable history, soft-deletion semantics, legal holds, deletion-blocker
graph, export manifest, support-access justification, impersonation policy, admin command log,
reauthentication hooks, secrets/config audit, rate-limit config, messaging abuse controls,
notification dedup, and more); the message that defined this queue was truncated mid-transmission
at "CJS: notification/email template versioning" — the rest of CJS onward and the entire CKA–CKZ
queue were never received and cannot be executed without further detail from the user. Next stage:
CJK (support-access justification).

**IR-1 through IR-18** (integration-readiness/scale/release-hardening queue): not started, per its
own stated precedence, after all of the above.

**A restated "FOUR-HOUR AUTONOMOUS BACKEND QUEUE FOR CLAUDE CODE BOT 2" message arrived mid-session
a second time**, describing an "expected current state" of "Stages BA through BK completed, next
stage BL" — stale by the message's own admission ("this state may now be stale. Trust the real
repository") and superseded by real progress (BA–CH long since closed, CJA–CJI in progress at time
of receipt). Correctly not acted on as a restart signal; its stage-workflow/testing/security-
invariant rules are the same standing rules already being followed, not new instructions.

**A genuinely new "NEXT 6 HOURS AUTONOMOUS BACKEND QUEUE" arrived immediately after**, explicitly
append-only ("first finish every stage already assigned... do not restart completed stages... do
not overwrite newer repository reality with old prompt assumptions"). Adds Stages XR-1 through
XR-24 — protected-field mutation matrix, SECURITY DEFINER/privilege-escalation audit, grant/Data-
API exposure audit, Storage path canonicalisation, signed-URL revocation safety, immutable
history/evidence preservation (overlaps CJF — will be treated as a revalidation pass if still
needed by the time it's reached, not a duplicate build), transactional workflow boundaries,
optimistic concurrency/stale-write protection, idempotency key registry, dead-letter/poison-job
handling, backpressure/bounded workers, outbox payload versioning, notification/email template
versioning, data export object security, anonymisation referential integrity, legal-hold
propagation (overlaps CJH — same revalidation approach), pagination drift/cursor stability, public
contract drift scanner, seed/fixture determinism, schema snapshot/migration drift detection,
query performance budget audit, backend integration manifest, a final adversarial matrix, and a
six-hour closeout report. Per its own explicit precedence, this queue starts only after every
earlier-assigned stage (the rest of CJ, then IR-1–IR-18) completes.

## Next up

Stage CJO (secrets and configuration audit). Re-run the Stage A baseline checks (`git status`, `db
reset`, `test:db` ×2, `tsc`, `build`) before starting, per the "how to resume" section below.

## Second supplemental queue: stages completed

| Commit | Stage | Summary |
|---|---|---|
| `64beacd` | BA | Background jobs/scheduler foundation audit — no reachable use yet (see table row above). |
| `8bdf62d` | BB | Document requirements/expiry engine audit — no fix needed (see table row above). |
| `d650b35` | BC | Organisation verification workflow. Found a real idempotency gap: `approve_user_verification()` had no guard against being called twice on the same verification. For breeder/organisation types (which create a real `organisations` row) this failed messily on a deterministic-slug unique-constraint violation rather than a clean business error, and there was no protection against a genuine concurrent double-approval race (two admins, or one double-click) — both transactions could read `status != 'approved'` before either committed. Fixed with `20260101009700_verification_approval_idempotency.sql`: `select ... for update` to serialize concurrent callers, plus an explicit status check before any side-effecting insert, matching the pattern already used in `execute_account_deletion()` (Stage AI) and `change_ops_request_status()` (Stage AD). New `tests/db/verification-approval-idempotency.test.ts` proves both the sequential-double-call case and a genuine concurrent race (`Promise.all`) resolve to exactly one organisation, never two — using disposable signed-up accounts throughout rather than the shared `breederPending`/`foundationPending` personas other test files read as a given pending state. 501/501 tests, verified on a fresh reset plus two more runs without reset (a third run for this concurrency-touching stage). |
| `d8fd63d` | BD | Driver onboarding/eligibility. Found the exact same bug class already fixed once this session (`20260101006100_owns_org_checks_active_role.sql`, "suspending the exact role that earned someone their organisation had no effect on their ability to keep managing it") — but never applied to drivers. Four independent policies/functions all checked only `drivers.profile_id = auth.uid()`, never `user_roles.status`: `is_my_driver_id()`, `is_assigned_driver_for_request()`, the `transport_documents` driver-read policy, and the Storage `transport-documents` driver-read policy. A suspended driver kept full access to their assigned transport requests, route stops, status history, and job documents. Fixed all four in one migration (`20260101009800_driver_id_checks_active_role.sql`), each adding `and public.has_role(auth.uid(), 'driver')`. New subtest in `tests/db/workflows.test.ts` (alongside the existing ops/org-owner suspension tests) proves suspending the driver role revokes their assigned-request access and restores correctly afterward. 502/502 tests, verified on a fresh reset plus one more run without reset. |
| `af9ac2c` | BE | Vehicle fleet/capacity/maintenance audit — no fix needed (see table row above). |
| `1377dd1` | BF | Route execution/stop state machines. `route_stops` had no uniqueness on `(route_id, stop_order)` — two stops on the same route could silently share the same order, making the route's sequence ambiguous. Fixed with a real unique constraint (`20260101009900_route_stops_order_uniqueness.sql`); `route_stops` is ops-only-write (structurally rules out the self-approval bug class, same as vehicles/routes), so this is a data-integrity safeguard, not a security fix. Deliberately did **not** build a full `routes.status` state-machine trigger here: ops needs real override ability (cancelling from any non-terminal state is legitimate), and this session already learned the hard way that a restrictive state machine on an ops-trusted table needs the "explicit reason + audit record" override path IR-8 is explicitly scoped to build — a partial, unaudited version now would conflict with that dedicated later stage rather than complement it. New `tests/db/route-stops-integrity.test.ts`. 507/507 tests, verified on a fresh reset plus one more run without reset. |
| `e4f9c10` | BG | Proof of pickup/delivery. `transport_status_history.evidence_url` existed since the original migration but was never wired to anything (zero references anywhere in `src/`) — a driver logging "delivered"/"handover_confirmed" produced only a bare status string, no photo evidence, despite the schema clearly being designed for it (the same "schema exists, never wired" shape as `messages.attachment_url` before Stage S). Also found `advanceJobStatus()` (the real function drivers use to progress their job) had the exact same non-atomic + forgeable-actor shape already fixed for `changeOpsRequestStatus()`/`assignRequestToRoute()` — two separate client-side writes with a client-supplied `driverProfileId` trusted as the actor. Fixed both together in `20260101010000_pickup_delivery_evidence.sql`: a new private `transport-evidence` Storage bucket (driver upload/read for their active job, requester read, ops manage-all) plus a new `advance_transport_job_status()` SECURITY DEFINER RPC doing the status update + history insert atomically with a server-stamped actor and an optional evidence object path. `src/lib/queries/driver.ts`'s `advanceJobStatus()` and its one call site (`dashboard.driver.index.tsx`) updated to match; the driver UI itself doesn't yet have a photo-attach control (the backend accepts an optional `evidencePhoto` File and is fully ready — building the actual capture UI is a real, separate, deliberately-deferred frontend task, documented rather than rushed). New `tests/db/pickup-delivery-evidence.test.ts`. 514/514 tests, verified on a fresh reset plus one more run without reset. |
| `08933f7` | BH | Incidents/animal-welfare events. Audited `transport_incidents` and found it already correctly built: `reported_by` is RLS-enforced (`with check (reported_by = auth.uid())`), not a client-trusted field — the forgeable-actor pattern found and fixed several times elsewhere this session doesn't apply here, it was already closed. Drivers have no UPDATE policy at all (can't edit their own report after filing — ops-only resolution, preserving report integrity), and `is_assigned_driver_for_request()` (already fixed in Stage BD to check active role) correctly gates who may report. What was missing: zero test coverage. New `tests/db/transport-incidents.test.ts` covers assigned-driver reporting, forged-actor rejection, unrelated-driver rejection, no-self-edit, ops resolution, and customer/requester exclusion from raw incident records. No code change — closing a real coverage gap on an already-correct design. 523/523 tests, verified on a fresh reset plus one more run without reset. |
| `ce1b646` | BI | Service areas/market availability audit — duplicates Stage AF, no fix needed (see table row above). |
| `50d0d5b` | BJ | Country/currency/timezone/unit normalisation. Found a real gap: `currency` is free-text with zero validation across 8 tables (`profiles.preferred_currency`, `animals`, `reservations`, `quotations`, `pricing_rules`, `fundraising_campaigns`, `fundraising_contributions`, `markets`), and some of these columns are writable by a party this session otherwise treats as "trusted but not fully trusted" (an org sets `animals.currency`/`reservations.currency` on their own rows) — the exact "row-level access doesn't validate the column's *value*" shape this session has repeatedly closed elsewhere. Grepped every currency literal in the schema/seed: only `'EUR'`/`'PLN'` are ever used. Added a `CHECK` constraint to all 8 columns (`20260101010100_currency_code_validation.sql`); the migration applying cleanly against existing seed data confirmed no other value was ever actually in use. Deliberately did **not** attempt a full country-normalisation overhaul: `country` stays free-text almost everywhere while only `markets.country_code` is ISO-constrained — a real inconsistency, but Stage BI's audit already confirmed nothing currently joins the two together, so fixing it now would be a large, risky migration for no current behavioural gain; documented here instead of rushed. New `tests/db/currency-validation.test.ts`. 528/528 tests, verified on a fresh reset plus one more run without reset. |
| `c0b0edf` | BK | Legal-document/consent versioning. The signup page already says "By creating an account, you agree to Havenpaw's Terms and Privacy" — but nothing recorded that a specific user actually consented to a specific version of either document. New `legal_document_versions` (admin-managed, publicly readable, at most one `is_current` row per document type via a partial unique index) and `user_consents` (append-only — no update/delete policy for ordinary users at all; INSERT requires the version to genuinely match a current row, not an arbitrary string) in `20260101010200_legal_consent_versioning.sql`. Wired into the real `signUp()` server action (`src/lib/auth/actions.ts`, not a frontend-owned file) as a best-effort step after account creation, matching the promise the signup page already makes, without inventing any new UI. Seeded the current terms/privacy/cookies versions honestly labelled `2026-07-24-draft`, matching that the underlying pages are still pending lawyer review — the versioning mechanism works the same regardless of draft-vs-final content. **Found and fixed a real bug while testing**: hit the `auto_expose_new_tables=false` GRANT gotcha a third time this session — `user_consents` was granted `select, insert` but not `update, delete`, so the admin "manage all" RLS policy was silently unreachable for those two operations via the Data API despite RLS itself allowing it; caught because the test's cleanup step now actually asserts the delete succeeded instead of ignoring its result. New `tests/db/legal-consent-versioning.test.ts`, including a full simulation of the exact sequence `signUp()` performs (this harness can't invoke the TanStack Start server function directly, but proves every DB operation it depends on). 537/537 tests, verified on a fresh reset plus two more runs without reset (a third run given the grant bug found mid-stage). |
| `c14a760` | BL | Support case management audit — no fix needed at the time (see table row above; note: a much more detailed spec for this stage arrived mid-session after this commit — see the BL-addendum row below, added out of order once that detail was available). |
| `a5ab979` | BM | Moderation assignment/reviewer workload. Found a real gap in the "investigate" UI action (`dashboard.admin.moderation.tsx`): a plain, unconditioned `UPDATE` setting `assigned_moderator_id` to a client-supplied value, with no guard against the case already being claimed by someone else. Two moderators clicking "investigate" on the same unassigned case at the same moment would race — whichever write landed last silently won, and any moderator could set `assigned_moderator_id` to a *different* moderator's id (the same forgeable-actor shape closed repeatedly this session). Fixed with a new `claim_moderation_case()` SECURITY DEFINER RPC (`20260101010300_moderation_case_claim_rpc.sql`): `select ... for update` to serialize concurrent claimers, a clear "already claimed by another moderator" error (idempotent if it's already you), and a server-stamped actor — matching `approve_user_verification()`'s pattern from Stage BC. Deliberately narrow: broader moderation actions (resolving, reassigning, adding notes) keep using the existing `is_moderator()` policy directly, unchanged. New `tests/db/moderation-case-claim.test.ts`, including a genuine `Promise.all` concurrency race proving exactly one of two simultaneous claims wins. 551/551 tests, verified on a fresh reset plus two more runs without reset (a third run for this concurrency-touching stage). |
| `ab25ee2` | BL-addendum | Support case management (real build). A much more detailed spec arrived mid-session, explicitly requesting this be built despite the earlier "no reachable consumer" audit conclusion (Stage AJ, re-confirmed at Stage BL) — building it now on that explicit instruction. New `support_cases`/`support_case_messages` tables (`20260101010400_support_cases.sql`) following patterns already proven elsewhere this session: server-controlled requester actor (RLS validates `requester_profile_id = auth.uid()`, matching the convention used throughout rather than an auto-fill trigger), the `is_internal` message split from Stage R (customer can never post or read internal notes), a customer-only "reopen a resolved/closed case" self-service transition (every other status/priority/assignment change is staff-only), and a `claim_support_case()` atomic-claim RPC identical in shape to `claim_moderation_case()` (Stage BM) — `select ... for update`, clear conflict error, server-stamped actor, and an `audit_logs` entry for every claim. No dedicated `'support'` platform role exists and none was invented; `is_ops_staff()` gates staff access, the closest already-real, already-trusted staff concept — inventing a new role with no staff-facing UI to use it would itself be speculative. New `tests/db/support-cases.test.ts` (25 tests) covers case creation/ownership, forged-actor rejection, staff-only-field rejection, internal-note privacy, unrelated-user denial, atomic claim + audit, and reopen-preserves-history. 576/576 tests, verified on a fresh reset plus one more run without reset. |
| `7251bf1` | BN | Explainable risk signals. New `risk_signals` table (`20260101010500_risk_signals.sql`): rule-based only (an enum of concrete signal types plus a plain-language `explanation` and `rule_version`, never an opaque ML score), deduplicated one row per `(subject_profile_id, signal_type)` via `record_risk_signal()`'s upsert (a repeat increments `occurrence_count`/`last_seen_at` instead of creating a new row), advisory-only by design — no code anywhere reads this table to automatically suspend, block, or otherwise punish an account, only `mark_risk_signal_reviewed()` lets ops staff mark a signal reviewed/false-positive. No INSERT/UPDATE policy exists for any client at all, staff-only SELECT gated by `is_ops_staff()`. **Found a real bug while testing, not just a coverage gap**: the first version tried recording a signal inside `enforce_rate_limit()` immediately before its rejection `raise exception` — proven broken by actually driving a real disposable account past a real limit and finding `rate_limit_events`/`risk_signals` never grew past what earlier *successful* calls had already committed. PostgREST runs each RPC call as one transaction; an uncaught `raise` rolls back the entire transaction, including anything inserted earlier in the same call — the signal-on-rejection write could never actually persist, dead code that looked functional. Redesigned around an `AFTER INSERT` trigger on `rate_limit_events` that fires once an actor's count of *committed* (accepted) uses of a rate-limited action crosses ~75% of that action's real configured `max_count` (values read directly from each `perform enforce_rate_limit(...)` call site in `20260101008200`/`20260101008300`) — a genuinely durable, real, explainable event source. `enforce_rate_limit()` itself is left byte-identical to its original definition. New `tests/db/risk-signals.test.ts` drives 15 genuinely successful `request_transport_amendment()` calls (not 21 rejected ones) against a disposable account with a real submitted transport request, proving the threshold crossing, non-duplication, staff-only read/review, non-staff rejection, and that the flagged account's other access is completely unaffected. 589/589 tests, verified on a fresh reset plus two more runs without reset. |
| `6e941e6` | BO | Duplicate detection. Two different kinds of duplicate, two different mechanisms. (1) `animals.microchip_number` had zero uniqueness — a real chip number is a physical identifier that can never legitimately belong to two different animal rows, the same "the column's value was never validated" gap this session has repeatedly closed elsewhere. Fixed with a case/whitespace-insensitive partial unique index (`20260101010700_duplicate_detection.sql`), a hard block since this case is genuinely unambiguous. (2) A duplicate *transport request* (same requester submitting the same request twice — a slow double-click, a retried network call that actually succeeded, someone testing the flow) is fuzzy, not unambiguous — a hard block risks rejecting two genuinely different requests that happen to share values. Wired into Stage BN's `risk_signals` instead: an `AFTER INSERT OR UPDATE OF status` trigger flags (never blocks) a same-requester, same-animal-name, same-route submission within 24 hours, gated on the transition to `status = 'submitted'` specifically (not every draft edit, which would be pure noise). Needed a second migration (`20260101010600`) purely to add the new `risk_signal_type` enum value in its own transaction before consuming it — the same safe split-migration pattern this codebase already established in Stage E/F (`20260101007700`/`800`), confirmed correct by a clean `db reset`. **Found a real test-repeatability bug while writing this, the same class of lesson as Stage BN**: `risk_signals` intentionally has no DELETE policy for any client (a signal is closed via `mark_risk_signal_reviewed()`, never deleted, so staff can't quietly erase evidence of a flagged pattern) — so a test against a *shared* seeded persona left a permanent signal row with no way to clean it up, corrupting the next run's "no signal yet" assertion (caught by actually re-running the suite a second time, not just once). Fixed by switching to a disposable signed-up account, matching Stage BN's own test. New `tests/db/duplicate-detection.test.ts`. 604/604 tests, verified on a fresh reset plus two more runs without reset. |
| `87a9df2` | BP | Search read models audit. Confirmed there is genuinely no server-side search of any kind anywhere in this codebase yet: grepped every query file and route for `ilike`/`.textSearch`/`to_tsvector` — zero matches. `listPublishedPuppies()`'s `filters` param (Stage W already found this) is 100% client-side JS filtering over an unpaginated full fetch; no other list function does even that much. Considered building a denormalized search read model (a materialized view or dedicated flat table joining animals/breeds/organisations/litters for fast faceted/full-text search, CQRS-style) and rejected it now: with zero real consumers of any search index today, and the actual "server-side marketplace search contract" already scoped as its own dedicated later stage (IR-2 in the integration-readiness queue), building read-model infrastructure ahead of that stage would be exactly the "speculative infrastructure with no reachable use" pattern this session has consistently avoided elsewhere (Stage BA/BB/BE, CLAUDE.md's own rule against designing for hypothetical future requirements). Separately verified the one real thing that *would* matter regardless of read-model existence — public listing visibility — is already correctly enforced at the RLS layer, not depending on any query-side filter: `"public reads published org-listed animals"` requires `is_published` **and** a live join proving the owning organisation is `verification_status = 'approved' and is_public`, so a suspended organisation's animals already cannot leak into any query (search-read-model-backed or not) regardless of what a future search feature's query looks like. No code change; correctly deferred to IR-2 rather than duplicated under a different stage name. |
| `30a9aae` | BQ | Error taxonomy. Confirmed a real, reachable gap directly named by CLAUDE.md's own UX principle: every RPC/trigger this session has written raises with the same generic `P0001` errcode (95/95 raise sites, confirmed by grep) but a deliberately plain-language message, while real Postgres constraint violations (unique/FK/check — e.g. Stage BO's own new `animals_microchip_number_unique`) carry technical text never meant for an end user; the frontend was passing errors straight through via `toast.error(err.message)` at 69+ call sites codebase-wide with zero translation. Rewriting all 95 raise sites to distinct custom SQLSTATEs would be large, risky migration churn for what's actually needed — the message text at those sites is already customer-safe. Instead: new `src/lib/errors.ts` (`getFriendlyErrorMessage()`) passes `P0001` messages through as-is and replaces recognised technical codes (`23505`/`23503`/`23514`/`42501`/`PGRST301`/`PGRST116`) with a generic, friendly equivalent, never risking a raw constraint name reaching the screen. Wired into the two real call sites in `_public.transport.request.tsx` — the one customer-facing file explicitly carved out of CLAUDE.md's frontend-file boundary for this session. The other 69+ call sites span explicitly frontend-owned dashboard/marketplace pages outside this session's scope, correctly left alone rather than a broad unrequested frontend sweep (the shared utility is now there for whoever does that pass); internal ops/admin dashboards correctly keep showing raw technical errors per CLAUDE.md's own exemption for those surfaces. No DB/migration change — `tsc`/lint/build clean, 604/604 DB tests unaffected (confirmed, not just assumed, since the change touches a route file). |
| `30fa378` | BR | API contracts / RPC grant hygiene. Audited all 30 RPCs granted execute to `authenticated` against this codebase's own established convention: an explicit `revoke all ... from public` before the grant, not relying solely on an internal `auth.uid()`/role check — Postgres grants EXECUTE to `PUBLIC` by default on every new function, and a later `grant ... to authenticated` layered on top never actually removes that default since GRANT is purely additive. Found four RPCs missing the explicit revoke that 25+ siblings already have (`20260101010800_rpc_grant_hygiene.sql`): `approve_user_verification` (admin-gated, the same shape as `claim_moderation_case`/`execute_account_deletion`, which already had this), `get_my_profile`, `start_application_conversation`, `start_transport_conversation`. Every one's internal check was already correct and unconditional — functionally safe either way, a missing defense-in-depth layer rather than an active vulnerability — but a real, demonstrated inconsistency in contract enforcement, not a hypothetical one. Also spot-checked the hand-maintained `src/lib/supabase/types.ts` stub against a real `supabase gen types typescript --local` output: every RPC actually called from `src/` is declared and no RPC name in the stub is stale (both cross-referenced directly, not assumed); full type-fidelity reconciliation (enum-typed params like `p_new_status` are currently widened to plain `string` in the stub, and the stub has no `Enums` section at all) is real but already its own dedicated later stage (IR-5) — not duplicated here. New `tests/db/rpc-grant-hygiene.test.ts` proves anon is now rejected at the grant layer for all four, and a real authenticated non-admin caller still reaches the same business-logic rejection as before (behavior unchanged for legitimate callers). 612/612 tests, verified on a fresh reset plus two more runs without reset. |
| `3bd87b5` | BS | Webhooks (disabled) audit. Confirmed the queue's own naming ("disabled") matches reality: grepped the whole repository (`src/`, `supabase/migrations/*.sql`, `docs/*.md`, `package.json`) for "webhook" case-insensitively — zero matches anywhere except this progress log's own record of the queue description. No incoming webhook receiver route, no outgoing webhook dispatch, no webhook-related table, config, or signature-verification helper exists. This is the same shape as Stage AA (payment abstraction, also disabled) and Stage AC (outbox/email, never built): there is no external integration today that would either push events into this system or need this system to push events out — no payment provider, no email provider, no third-party service of any kind is wired up yet. Building webhook-receiver/dispatch infrastructure now, with no real endpoint or consumer to protect or call, would be exactly the "speculative infrastructure with no reachable use" pattern this session has consistently avoided (Stage BA/BB/BE/BP). No code change; correctly stays unbuilt until a real external integration (a payment or email provider) is actually approved and configured, at which point that integration's own webhook contract (signature verification, replay protection, idempotent event handling) should be built alongside it, not speculatively ahead of it. |
| `f71858f` | BT | Bulk import audit. Grepped for any existing bulk-creation/CSV-upload/batch-insert path — none exists (`createLitter()` only creates the litter record itself; every individual puppy is still added one at a time through the ordinary `animals` insert path, and no admin/ops bulk-onboarding tool exists anywhere). Checked launch-readiness docs for a documented day-one bulk-onboarding need (e.g. migrating an existing kennel's spreadsheet catalog at launch) — none found; the only "bulk" hits in `docs/*.md` are unrelated coincidental matches ("bulk-harvesting" in the PII-lockdown writeup, "bulk-read" in the MVP test report). The one plausible real candidate — letting a breeder add several puppies for one litter in a single action instead of one form submission per puppy — has no existing UI trigger at all (no "bulk add" button, no CSV widget anywhere in `dashboard.breeder.*`/`dashboard.foundation.*`), so a backend bulk-insert RPC built now would have zero real consumer and be dead code the moment it's written; building the missing UI itself is outside this session's backend/frontend-file-boundary scope. Same shape as Stages BA/BB/BE/BP/BS. No code change; candidate for whoever next builds the litter/puppy-management UI, not for this session. |
| `bddb9cd` | BU | Archival. `rate_limit_events` (Stage J) has grown unbounded since day one — every accepted rate-limited action inserts a permanent row and nothing ever removed one. Found the fix already half-designed: the original Stage J migration's own comment explicitly proposed opportunistic pruning ("Old rows are pure noise once their own window has passed... cheap enough to prune opportunistically rather than needing a scheduled job") and even added `rate_limit_events_created_at_idx` to support it, but the actual delete was never written — the same "schema/index designed for X, X never wired up" shape as a few other gaps closed earlier this session. No background job/scheduler exists (Stage BA confirmed no other reachable need for one), so `enforce_rate_limit()` (`20260101010900_rate_limit_events_archival.sql`) now deletes the calling actor's own stale rows for the same `action_key` before counting — a free byproduct of a read it already has to do, never new infrastructure, and it can never remove a row still inside its own window. New `tests/db/rate-limit-events-archival.test.ts` proves it via a very short custom window passed directly to the (genuinely arbitrary-args-callable) RPC plus a short real wait, since this suite has no service-role/direct-SQL path to backdate rows with. 617/617 tests, verified on a fresh reset plus two more runs without reset. |
| `0c8f957` | BV | Storage cleanup audit. Investigated the real orphan-file risk (a metadata row deleted while its Storage object survives, or vice versa) across all 5 buckets (`kennel-media`, `transport-documents`, `transport-evidence`, `message-attachments`, `welfare-case-documents`) rather than assuming one exists. Found no reachable path today: grepped every `.delete()` call in `src/lib/queries/*.ts` against every Storage-object-referencing table (`transport_documents`, `welfare_case_documents`, `animal_images`, `messages.attachment_url`) — zero matches, nothing in the real app ever deletes one of these rows at all yet. Every real upload flow (`submitDocument()`, driver evidence upload, message attachments, welfare documents) already does correct best-effort Storage cleanup on its own insert-failure path (`.remove([objectPath])` if the DB insert errors) — the "clean up on the way out" pattern already proven elsewhere this session. The one genuinely reachable edge case — `kennel-media`'s real `storage.objects for delete` policy lets an org owner delete a photo file directly via the Storage SDK, which no app code calls today (no "remove this photo" UI button exists anywhere), so a sophisticated user manually calling it would delete the file while `animal_images.image_url`/`organisations.logo_url`/`cover_image_url` still pointed at it — requires deliberate direct Storage-API use, not an ordinary UI action, and building a cleanup trigger for a delete path nothing in the app actually exercises would be speculative. No code change; candidate for whoever builds the first real "delete this photo/document" UI feature, at which point the deletion action itself should clean up both sides atomically from the start rather than needing a trigger retrofitted after. |
| `0622282` | BW | Health checks. Unlike Stages BA/BB/BE/BP/BS/BT/BV, this one found a real, standard gap with an obvious always-relevant consumer: no `/health` (or any liveness/readiness) endpoint existed anywhere, needed by Cloudflare's own health checks, an external uptime monitor, or a deploy-verification step — none of which can use TanStack Start's RPC-style `createServerFn` protocol, only a plain GET. Added directly to `src/server.ts` (the actual Worker `fetch()` entry point, already customized for error handling/security headers) rather than a TanStack Start file-route, since this Start version has no file-based server-route API (confirmed against the installed package's exports). Does a real DB probe, not just "the process responded" — a `head: true` count against `legal_document_versions` (Stage BK, small, always-populated, public) — returning `200 {status:"ok"}` or `503 {status:"degraded"}`, always `cache-control: no-store`. Verified for real against a running dev server: `GET /health` returns 200 with the DB check genuinely succeeding, `POST /health` correctly falls through to the normal SSR 404, ordinary pages render unaffected. Could not additionally verify against the real `wrangler dev`/Cloudflare Worker build target — wrangler in this sandbox rejects the environment's current `compatibility_date` as unsupported, a pre-existing, unrelated environment limitation. `tsc`/lint/build clean, 617/617 DB tests unaffected (no migration this stage). |
| `b316b88` | BX | Service level objectives. No production deployment or real traffic exists, so no numeric SLO (an uptime %, a latency target) can be honestly justified — a fabricated number would be worse than none, the same discipline already applied to `docs/BACKUP_AND_DISASTER_RECOVERY.md`/`docs/PRODUCTION_SETUP.md`. New `docs/SERVICE_LEVEL_OBJECTIVES.md` instead catalogs what's already measurable once real monitoring exists, using only infrastructure already built this session rather than proposing anything new: `/health` (Stage BW) for availability, `rate_limit_events` (Stage J/BU) for volume, `risk_signals` (Stage BN) for abuse-rate, `audit_logs` (Stage AE) for ops-action timing, and the backup/DR doc's own open items for data-durability. Defines SLI categories (availability, core-journey latency, error rate explicitly excluding expected `P0001` business rejections, data durability) and lists concretely what's needed before any target becomes real: a production deployment, real traffic, and an actual observability tool. Docs-only, no code change. |
| `bdf198b` | BY | Incident runbooks. New `docs/INCIDENT_RUNBOOKS.md`, five concrete scenarios built entirely from mechanisms already verified this session (not requiring real production traffic to write, unlike Stage BX's SLOs): `/health` reporting degraded (Stage BW), a specific RPC failing for all callers (the same `npx supabase db query` diagnostic pattern used throughout this session), legitimate users hitting rate limits or `risk_signals` false positives (Stage J/BN/BU — including the honest gap that there's no manual "reset this user's limit" RPC today, only the window itself elapsing via Stage BU's pruning), suspected account compromise (role suspension via `user_roles.status`, non-forgeable `audit_logs`, `execute_account_deletion()`'s unresolved-obligation checks), and a suspected private-data/document leak (RLS-first triage before assuming a wider breach, signed-URL expiry, bucket-public checks). The bad-migration case is deliberately not duplicated — already covered in `docs/BACKUP_AND_DISASTER_RECOVERY.md`'s "Incident response" section. Docs-only, no code change. |
| `42d4a20` | BZ | Maintenance mode. No mechanism existed to take the app down for a planned migration/deploy without a code change and redeploy. New `app_maintenance_mode` (`20260101011000_maintenance_mode.sql`): a single always-exactly-one-row table (`id` pinned to `true` via a `check`), publicly readable, admin-only write, `enabled_by`/`enabled_at` server-stamped on *every* update that leaves `enabled = true` (not only the transition) — closing a forgeable-actor gap a naive "only stamp on transition" trigger would have left open on a same-value update; disabling always preserves who last turned it on from `old`, never client-supplied. Enforced directly in `src/server.ts` — the real Worker `fetch()` entry point, same layer as Stage BW's `/health` — so a broken app/SSR state can never block the maintenance page itself; cached 15s per Worker isolate so normal traffic doesn't add a DB round-trip to every request. `/health` is always exempt and now also reports `maintenance:true/false`, distinguishing "intentionally down" from "actually broken." New `src/lib/queries/maintenance.ts` (matching the existing `markets.ts` admin-toggle pattern) with a real toggle wired into `dashboard.admin.settings.tsx` (admin-only, not excluded by CLAUDE.md's frontend boundary). Verified end-to-end against a real running dev server, not just `tsc`/tests: enabling via direct SQL made the homepage return the real maintenance page (503, custom message) while `/health` stayed live and correctly reported `maintenance:true`; disabling and waiting past the 15s cache TTL restored normal service, confirmed by re-checking after the wait rather than assuming the cache logic works. 625/625 tests, verified on a fresh reset plus two more runs without reset. |
| `c3321f1` | CA | Migration preflight command. Turned Stage AL's one-time manual audit into a real, reusable `npm run db:preflight` command instead of a snapshot that goes stale the moment the next migration is added — a fast, offline, static scan over `supabase/migrations/*.sql`, wired into CI's fast no-Docker job. Checks four concrete bug classes this session actually hit: (1) GRANT-vs-RLS gap — a table with RLS + policies but no matching GRANT *anywhere* in the migration history (checked globally, not per-file, since grants commonly live in a different/later file — a naive same-file-only first draft produced 44 false positives against this schema's own already-correctly-granted tables before being fixed); this exact class was found and fixed FIVE separate times this session, always caught late by a real "permission denied." (2) `NOT NULL` column added with no `DEFAULT`. (3) An enum value added via `ALTER TYPE` and used elsewhere in the *same* file — Postgres can't use a newly-added enum value in the same transaction it was added in (Stage BN hit this for real). (4) A bare `drop table`/`drop column`. Verified against the real, current 112-file migration set (clean, as expected) and unit-tested each check individually against synthetic bad SQL to confirm the detection logic actually fires, not just that the real migrations happen to pass. `tsc`/lint/build clean, 625/625 DB tests unaffected (no migration this stage). |
| `b22f6f6` | CB | Database invariant catalogue. New `docs/DATABASE_INVARIANTS.md`, complementary to `docs/DOMAIN_MODEL.md` (which describes what tables *mean*; this describes what's *guaranteed to always be true*, and exactly where). Catalogues real, currently-enforced invariants by category — uniqueness ("at most one X"), server-stamped actors (every non-forgeable actor column this session locked down, with its enforcement mechanism and migration reference), state locks ("once X, Y can never change again"), role/suspension behavior, referential/business-value constraints (currency, simulated-only fundraising, self-pruning rate limits), deletion/lifecycle rules, and visibility guarantees. Every entry cites the real migration file it comes from — nothing aspirational. Docs-only, no code change. |
| `c55ff99` | CC | Driver status state machine + chaos tests. Closed the gap Stage M/BF both found and deliberately deferred to this dedicated stage: `prevent_non_staff_operational_field_changes()` (Stage L) exempted the assigned driver entirely, so a raw-API UPDATE could set `status` to *any* enum value (skip straight to `completed`, move backwards, or set an ops-only hold state) and reassign `assigned_route_id`/`assigned_vehicle_id`/`assigned_driver_id`/`compliance_review_result`/`visibility` — none reachable through the real driver UI, real via a raw API call (`20260101011100_driver_status_state_machine.sql`). The legal transition graph isn't invented — it matches exactly what the app already treats as canonical in two places (`driverStatusSteps`, the UI's own linear sequence, and the full-journey scenario test, which additionally exercises the `rest_or_care_stop` detour the UI array skips). `veterinary_hold`/`compliance_hold` deliberately stay unreachable by a driver — no real code path sets either from that side yet. Ops staff remain completely unconstrained (Stage BF's own reasoning: the override-with-reason path is IR-8's scope). New `tests/db/driver-status-state-machine.test.ts`: every illegal jump rejected, the new field lock, the legal detour both directions, and two `Promise.all` chaos passes firing many invalid concurrent transitions from the same state — proving none sneaks through regardless of ordering. **Fixed a real regression this surfaced**: `tests/db/pickup-delivery-evidence.test.ts`'s fixture jumped `driver_assigned` straight to `delivered` in one call, only ever possible because this exact gap existed — updated to advance through the real intermediate steps first. 635/635 tests, verified on a fresh reset plus two more runs without reset (a third given the chaos tests' concurrency). |
| `f2e4490` | CD | Permission inventory. New `docs/PERMISSION_INVENTORY.md` — a role-first, practical "what can this kind of user actually do" reference grounded in real RLS/RPCs, complementing Stage CB's invariant catalogue. Covers `anon`, `customer`, `buyer`, `animal_owner`, `breeder`, `foundation_member`/`shelter_member`, `operations`, `driver`, `moderator`, `admin`, and organisation sub-roles, each entry citing the real enforcement mechanism (a specific RPC, RLS policy, or Stage CC's state machine) rather than restating every policy verbatim. Deliberately not a full table×role matrix or exhaustive automated coverage — that's IR-6's dedicated later scope. Docs-only, no code change. |
| `8743f91` | CE | Data-access consolidation. Surveyed every route file calling Supabase directly (24 files) for repeated query shapes and found real, exact duplication: two pairs of route files independently reimplemented the "get my organisation" query byte-for-byte (`dashboard.breeder.reservations.tsx`/`dashboard.breeder.transport.tsx` identical; `dashboard.breeder.tsx`/`dashboard.foundation.tsx` near-identical variants) — while `src/lib/queries/breeder.ts`/`foundation.ts` already had canonical `getMyKennel()`/`getMyKennelProfile()`/`getMyFoundation()`/`getMyFoundationProfile()` functions doing the exact same thing, unused by these four files. Also found `dashboard.breeder.settings.tsx`/`dashboard.foundation.settings.tsx` duplicated both the `get_my_profile()` RPC read and the phone-update mutation byte-for-byte with no shared function at all — added `getMyProfile()`/`updateMyPhone()` to `src/lib/queries/profile.ts` (clearly separated from that file's existing public-profile functions, which deliberately never return private fields). No behavior change — every replacement returns a superset or exact match of what was inline before. `tsc`/lint/build clean, 635/635 DB tests unaffected (no migration this stage), and a real smoke test against a running dev server confirms all six changed routes still resolve correctly (307 to sign-in, no 500s). |
| `f18668b` | CF | Tech-debt register. New `docs/TECH_DEBT_REGISTER.md` consolidates every real, currently-open item this session found and deliberately deferred — the "Known open items" section plus several per-stage deferred notes never promoted there (Stage BT's bulk-add UI gap, Stage BV's delete-photo/document feature gap) — organized by kind rather than chronology: missing features (nothing to fix, something to build on real need), known-incomplete hardening (a real fix exists but was deliberately narrower than the whole problem), and scale/UX limitations. Each item cites the stage that found it and what should trigger picking it up. Docs-only, no code change. |
| `8c98caa` | CG | Final backend PR review — second supplemental queue (BA–CF). A genuine review pass over the whole queue (61 commits, 58 files, ~4580 insertions), not a re-statement of per-stage summaries. Re-ran the full verification chain fresh: `db reset` + `test:db` ×3 (635/635 every run), `npm run db:preflight` against the real 113-file migration set, full-repo `eslint` (38/13, matching the documented baseline exactly), `tsc`, `build`, route-tree consistency. Additionally checked things only visible looking at the whole range together: no function redefined twice within this range in a way that could silently contradict an earlier stage's fix (`enforce_rate_limit()`/`prevent_non_staff_operational_field_changes()` each legitimately redefined once — read both final bodies directly to confirm), no secrets anywhere in the diff, nothing pushed, frozen frontend branches confirmed untouched. New `docs/BACKEND_PR_REVIEW_SECOND_SUPPLEMENTAL_QUEUE.md`. Verdict: clean, no blocking issues. |
| `def9b72` | CH | Second release-candidate report — closes the second supplemental queue (Stages BA–CH, 34 stages total). Verification re-run fresh: 113 migrations no duplicate prefixes, `npm run db:preflight` clean, fresh `db reset`, 635/635 tests on three consecutive runs, `tsc` clean, `build` clean, full-repo lint at the same 38/13 baseline with no new regressions across the whole range, nothing pushed, frozen frontend branches untouched. Test suite grew 576 → 635 (+59) from 14 new test files. Categorizes all 34 stages: 17 real code/schema fixes, 9 audits confirming an already-correct design or a genuinely unbuilt feature correctly deferred, 1 test-coverage-only stage, 1 new CI/tooling command (`db:preflight`), and 6 documentation deliverables. New `docs/BACKEND_RELEASE_CANDIDATE_REPORT_2.md`. **This closes the second supplemental queue.** Continuing directly into the third/fourth supplemental queue (Stage CJA onward, as far as content was actually received) and then IR-1 through IR-18, per standing instruction. |

## Third/fourth supplemental queue: stages completed

| Commit | Stage | Summary |
|---|---|---|
| `4634e32` | CJA | Authorization matrix. A systematic, table-centric sweep distinct from Stage CD's role-first permission inventory: (1) grepped every RLS policy for `using (true)`/`with check (true)` on a non-SELECT operation (the classic "accidentally grants unrestricted write access" red flag) — found 6 `using (true)` policies, all genuinely `for select` on legitimately public data (profiles' public columns, breeds, legal document versions, maintenance-mode status, group membership lists), zero `with check (true)` anywhere. (2) Cross-referenced every `alter table ... enable row level security` against every `create policy` — zero tables have RLS enabled with no policy at all (which would fully lock the table, even for admins, via the Data API). (3) Checked every table for an admin/staff override policy and manually verified each apparent gap (a quick grep heuristic false-flagged `reports`/`moderation_cases` as missing one — they use `is_moderator()`, not `is_admin()`/`is_ops_staff()`, confirmed by reading the actual policy text) — the real, remaining gaps (`follows`, `reactions`, `saved_posts`, `notification_preferences`) are uniformly low-stakes, purely personal, user-owned preference/social-graph data where no admin override is a reasonable, intentional design choice, not a bug; nothing security- or moderation-relevant lacks one. No code change — a genuine, verified "no gap found" outcome. |
| `3593f16` | CJB | Public/private projection layer. Read all 7 public views in the schema column-by-column (`public_transport_requests`, `public_routes`, `public_fundraising_contributions`, `public_fundraising_totals`, `public_transport_rating`, `driver_transport_job_view`, `my_moderation_case_view`) and confirmed every one correctly hand-picks a safe column list, never leaking an exact address, phone number, document field, supporter/reviewer identity, or internal-only moderation note. `driver_transport_job_view` and `my_moderation_case_view` are `security_invoker`/per-caller-scoped views (correctly relying on the base table's row-level RLS, only narrowing columns), while the fully-public ones (`public_transport_requests`, `public_routes`, the two fundraising views, `public_transport_rating`) are deliberately definer-style since their base tables have no anon-facing RLS policy at all — the view itself is the entire security boundary for those. Complements Stage O's earlier table-by-table privacy audit with a view-by-view column-projection lens instead. No code change — a genuine, verified "no leak found" outcome. |
| `29660b5` | CJC | Data classification catalogue. New `docs/DATA_CLASSIFICATION.md` — a simple four-tier scheme (Public, Internal, Restricted, Confidential/staff-only) covering everything sensitivity-relevant in this schema, personal and non-personal alike. Deliberately broader and shallower than `docs/PRIVACY_DATA_LIFECYCLE.md` (Stage O), which stays the authoritative, detailed GDPR-focused source for personal data — cross-referenced, not duplicated. Covers marketplace/community public data, contact details/exact addresses (Restricted), risk signals/internal moderation notes/audit logs/rate-limit telemetry (Confidential — never shown to the subject even though they can see the parent row, verified for `reports` specifically that the reported party has no read access to the row at all, not just the reporter identity), and business/pricing configuration. Docs-only, no code change. |
| `838acce` | CJD | Server-controlled actor attribution audit. Systematically re-grepped every `*_by`/`*_profile_id` actor-attribution column across the whole schema (13 distinct column names, ~20 tables) against Stage CB's already-documented list and found two real stragglers, both still client-insertable with no trigger tying them to the real caller: `quotations.created_by` (the real, demonstrated gap — `createQuotation()` inserted a client-supplied value directly, called from a real, live UI, `dashboard.operations.quotations.tsx`) and `legal_requirements.created_by` (no real call site today, closed at the same time for consistency at near-zero cost). New `stamp_created_by_actor()` trigger (`20260101011200_actor_attribution_stragglers.sql`) forces `created_by := auth.uid()` on insert for both tables. `createQuotation()` no longer accepts or sends a `createdBy` param at all — matching the pattern already used elsewhere: once an actor is fully server-stamped, drop the now-forgeable client param entirely. New `tests/db/actor-attribution-stragglers.test.ts`. 641/641 tests, verified on a fresh reset plus two more runs without reset. |
| `03cb13f` | CJE | Cross-tenant fuzz testing. New `tests/db/cross-tenant-fuzz.test.ts` — a single, consolidated, data-driven sweep across the major domain tables (`transport_requests` with 3 different owners, `buyer_applications`, `transport_status_history`, `animals` write-side, `saved_animals`) rather than the many individual, scattered cross-tenant checks already spread across feature files, so a future regression in any of them shows up in one place. Reuses existing seeded fixtures rather than creating new ones; each case includes a positive-control check (the real owner must still see their own row) so a "blocked" result can't be vacuously true from a nonexistent row. Found and fixed a real test bug while writing this: the seed data already has `buyer -> animalMaja` saved, so inserting a fresh `saved_animals` row for that exact pair hit the table's own unique constraint — switched to reading the existing seeded row. Also hit and resolved the same well-documented cumulative rate-limit exhaustion this session has hit before, confirmed not a real regression via a fresh reset. 650/650 tests, verified on a fresh reset plus two more runs without reset. |
| `044f507` | CJF | Immutable lifecycle history. `transport_status_history` is meant to be the permanent record of what actually happened during a delivery — both existing write policies were `for all`, not `for insert` ("ops staff manage all status history", "assigned drivers view and log status on their own requests"), so both ops/admin and the assigned driver could UPDATE or DELETE an existing historical row, not just append new ones. Grepped every real call site in `src/lib/queries/*.ts`: every single one is either `.insert(` or `.select(` — zero UPDATE/DELETE anywhere in the real app — so narrowing this (`20260101011300_immutable_status_history.sql`, splitting both `for all` policies into separate `select`/`insert` policies with the same predicates, then revoking update/delete at the table-grant level too) closes a real, reachable-via-raw-API gap with zero effect on any real code path. Matches `audit_logs`' own existing precedent exactly — no update/delete policy for anyone, not even admin, since Stage AE. New `tests/db/immutable-status-history.test.ts`. 658/658 tests, verified on a fresh reset plus two more runs without reset. |
| `2ff2b06` | CJG | Soft-deletion/archival semantics. Audited every table for a consistent soft-delete/archival pattern: `profiles` already has real soft-deletion (Stage AI), organisations use suspend/restore (Stage I), animals use status transitions, and every real `.delete()` call elsewhere targets genuinely ephemeral records with no audit value. One real inconsistency found: `dismissReport()` hard-deleted a `reports` row — permanently destroying evidence for detecting a real abuse pattern (repeated reports from one reporter), the exact signal `risk_signals.multiple_independent_reports` (Stage BN) was deliberately left unwired for exactly this future need. `reports` had no status column at all. New `report_status` enum (`20260101011400_reports_soft_dismissal.sql`); `dismissReport()` now updates status instead of deleting, `escalateReportToCase()` also marks the source report escalated, `listReports()` filters to `status='open'` so the admin queue's observable contents stay identical to before. Ops/admin's raw DELETE capability is deliberately left intact at the RLS layer (a rare real removal need is still legitimate) — only the normal dismiss workflow is now non-destructive by default. New `tests/db/reports-soft-dismissal.test.ts`. 663/663 tests, verified on a fresh reset plus two more runs without reset; smoke-tested the admin reports page against a running dev server. |
| `584be04` | CJH | Legal-hold mechanism. `execute_account_deletion()` (Stage AI) already refuses while a real business obligation is unresolved — a legal hold is the same shape of blocker for a different reason (litigation/investigation/regulatory need to preserve a specific account's data). No UI exists to place one yet — explicitly requested by name in this queue regardless, the same "build the backend mechanism on explicit instruction" precedent as Stage BL-addendum's support cases. New `legal_holds` table (`20260101011500_legal_holds.sql`), admin-only (`is_admin()`, narrower than the usual `is_ops_staff()` since this overrides someone's deletion rights entirely); `place_legal_hold()`/`release_legal_hold()` server-stamp the real actor; released holds are never deleted, only marked `released_at`/`released_by`, matching Stage CJG's own just-established discipline. `execute_account_deletion()` extended (not rewritten) with one more blocker check — carefully diffed line-by-line against the original migration to confirm the rest of the function body (the `processed_by` stamp, the closing `audit_logs` insert) carried over exactly unchanged. New `tests/db/legal-holds.test.ts`. 674/674 tests, verified on a fresh reset plus two more runs without reset. |
| `b1e1b59` | CJI | Deletion-blocker graph. `execute_account_deletion()` only ever reports the first blocker it finds via an `elsif` chain — an admin discovers blockers one at a time, by repeatedly attempting and re-reading the error after fixing each one. New `get_account_deletion_blockers(profile_id)` RPC (`20260101011600_deletion_blocker_graph.sql`) reuses the exact same conditions but returns every one that currently applies at once — read-only, admin-only. Wired into `dashboard.admin.users.tsx`: each pending deletion request now shows its current blockers inline (new `getAccountDeletionBlockers()` in `src/lib/queries/privacy.ts`, new `DeletionBlockers` subcomponent) — a real, already-existing admin page was the natural place to wire this in immediately rather than deferring the UI. New `tests/db/deletion-blocker-graph.test.ts` proves admin-only access and that multiple real blockers (a transport request + a legal hold) are reported together, not just the first. 679/679 tests, verified on a fresh reset plus two more runs without reset. |
| `f7389ca` | CJJ | Versioned export manifest. `exportMyData()` (Stage AH) had no self-describing metadata about itself — a downloaded export file today is indistinguishable from one downloaded after the function's shape changes later. Wrapped the return value in `{ manifest, data }`: `manifest` carries `schema_version` (bumps only on a breaking shape change, never just for an added category), `generated_at`, `subject_profile_id`, and the real category list. Confirmed this is a real, already-wired UI feature, not a schema-only stub — called from `account-privacy-card.tsx`'s "Download my data" button (`dashboard.buyer.profile.tsx`), which passes the result straight to a generic `downloadJson(data: unknown, ...)` with no field-specific access, so the change is fully backward-compatible for that call site. No DB/migration change — `tsc`/lint/build clean, 679/679 DB tests unaffected, smoke-tested the buyer profile page against a running dev server. |
| `3a8f062` | CJK | Support-access justification audit. Checked whether staff access to a customer's data through the support-case system (Stage BL-addendum, the one place this session built dedicated support tooling) carries a real justification trail. Found it already does: `claim_support_case()` is atomic and audited (`audit_logs`, actor server-stamped, before/after state), and every `support_cases` row already carries an explicit `related_entity_type`/`related_entity_id` context field ("never trusted for authorisation by itself; it's context for staff, not a capability" — Stage BL-addendum's own words) that a reviewer can join against the audit log's `target_id` to answer "why did this staff member access this case." Considered denormalising the related-entity context directly into the `audit_logs` row and rejected it — the information is already one join away via `target_id`, and duplicating it would be redundant storage for a completeness gain nobody has asked for. Considered building a general "staff must justify every read of sensitive data" system across the whole app and rejected it as clearly disproportionate: `is_ops_staff()`-gated "manage all" access on operational tables (transport requests, quotations) is core to how ops actually does its job — a mandatory reason prompt on every view would be disruptive to a real, high-volume, legitimate workflow with no demonstrated abuse this session found evidence of. No code change — a genuine "already adequate for the one real support-access surface that exists" outcome, not a manufactured fix. |
| `f71c0e5` | CJL | Impersonation-policy decision. Confirmed no staff "sign in as user" impersonation mechanism exists anywhere in this codebase. Recorded a deliberate policy decision in `docs/DECISIONS.md` (the existing home for product/architectural decisions) rather than building the feature: staff impersonating a real user is a materially larger trust concession than the operational `is_ops_staff()` access ops already has, and shouldn't be added as a side effect of a support-tooling stage with no demonstrated need. Documents what a future implementation would minimally require if a real need ever arises (visible-to-user notification, a second confirmation for any mutating action while impersonating, time-boxed sessions, a real `audit_logs` entry per impersonated request) without building any of it now. Docs-only, no code change. |
| `0d09749` | CJN | Reauthentication ("step-up") hooks. Confirmed directly against a real signed-in session in this local instance (not assumed) that Supabase GoTrue's JWT carries a genuine `amr` (Authentication Methods Reference) claim recording actual authentication *events* with their own timestamps — unlike `iat`/`exp`, `amr` timestamps are never advanced by a silent token refresh, so they honestly answer "when did this session last actually authenticate," making a real, provider-neutral step-up check possible without inventing an identity provider. New `require_recent_auth(p_operation, p_max_age default 15 minutes)` (`20260101011800_recent_auth_step_up.sql`), fail-closed (raises a stable `reauthentication_required` P0001 message if unauthenticated or stale), server-enforced with no client-supplied bypass (reads only `auth.jwt()`, never a request parameter). Deliberately narrow scope: wired into exactly the three rarest, most destructive, already-admin-only RPCs this session has built that this stage explicitly names — `execute_account_deletion()` (Stage AI/CJH, irreversible), `place_legal_hold()`/`release_legal_hold()` (Stage CJH, legal-sensitive) — each carefully diffed line-by-line against its prior definition to confirm only one `perform require_recent_auth(...)` line was added, placed *after* the existing `is_admin()` check so a non-admin still gets the original authorization error, never a reauth-error side-channel confirming the operation exists. Deliberately NOT wired into higher-volume routine admin actions (`approve_user_verification()`, the CJM verification/owner/featured trigger, moderation/support case claims) — no reauthentication UI exists anywhere in this app yet (no "re-enter your password" step), so hard-gating a frequently-used workflow behind an unrefreshable freshness window would be a real, demonstrated regression, not a security improvement; documented as the honest current scope rather than silently narrowed. New `tests/db/recent-auth-step-up.test.ts`: valid/expired/missing markers (expired forced deterministically via a `0 seconds` window rather than a real wait; missing-marker via anon, correctly caught at the grant layer before the function body's own fail-closed branch, confirmed as defense-in-depth not a gap), authorization-before-reauthentication ordering, and a genuinely fresh admin session succeeding at all three wired RPCs. 697/697 tests (+9 from CJM's 688), verified on a fresh reset plus two more runs without reset. |
| `413099f` | CJM | Admin command log + a real self-approval fix. Investigating why admin-only organisation actions (verification status, featuring) had no audit trail surfaced a more serious finding: `organisations.verification_status` was never added to `prevent_org_owner_transfer_by_non_admin()`'s protected-field set (Stage E/I locked `owner_user_id`/`is_featured`, but not this column) — "owners update their own organisation" is a plain row-level policy with no column restriction, so an org owner could self-approve (or self-suspend) their own organisation's verification via a raw UPDATE, completely bypassing `approve_user_verification()` (Stage BC's admin-gated, idempotency-hardened RPC this is meant to go through) — the same self-approval bug class this session has closed repeatedly elsewhere (`rehoming_reviews`, `buyer_applications`). Closed both findings together in `20260101011700_organisation_verification_lock_and_admin_audit.sql`: `verification_status` joins the protected set, and every real admin change to any of the three protected fields (`owner_user_id`, `is_featured`, `verification_status`) now writes a real `audit_logs` entry atomically in the same trigger, actor server-stamped from `auth.uid()`, never a separate client-side write that could be forgotten or fail silently. **Found and fixed a real test-design bug while writing `tests/db/organisation-verification-lock-and-audit.test.ts`**: the first draft used `breederPending` (whose own `breeder` role is still `status='pending'`) as the actor attempting self-approval — but `owns_org()`'s RLS `using` clause requires an *active* role for the org's type, so RLS itself would silently filter the raw UPDATE to zero matched rows (`data: [], error: null`) before the trigger ever ran, making the test pass for the wrong reason and proving nothing about the new lock. Fixed by using `breeder1` (an owner with an already-active role) for a disposable pending org instead, so the UPDATE genuinely reaches the trigger and the assertion exercises the real P0001 rejection (`assert.ok(attempt.error, ...)`, not the `isBlocked()` RLS-style helper, which doesn't recognize a trigger-raised P0001 as a rejection). Also fixed an absolute-zero audit-count assertion that would have gone flaky against `orgWolnaDolina`, a shared seeded org `tests/db/admin-placeholders.test.ts` also legitimately admin-toggles in the same non-reset suite run — switched to a before/after delta. 688/688 tests (+9 from CJI's 679), verified on a fresh reset plus two more runs without reset (a third given this is a security-trigger/audit stage). `tsc`/full-file lint/build/`db:preflight` clean; confirmed the function's `set search_path = public` stayed pinned through the rewrite. |

## First supplemental queue: stages completed

| Commit | Stage | Summary |
|---|---|---|
| `6f8717a` | R | Messaging/conversation security. Found and closed a real gap: "participants send messages in their conversations" (`20260101002000_messaging.sql`) checked `sender_profile_id`/conversation membership but never restricted `is_internal` — an ordinary participant's INSERT could set `is_internal = true` on their own message, spoofing what's meant to be a trusted ops-only annotation channel (`is_internal` is the actual visibility gate: the SELECT policy filters `not is_internal`). Not reachable through the real UI today (`chat-thread.tsx`'s only `sendMessage()` call site never passes `isInternal`, and no route yet renders internal-note content), only via a raw API call. Fixed with `20260101008600_messages_internal_flag_lock.sql` (`is_internal = false` added to the participant WITH CHECK; ops staff unaffected, they write through a separate broad policy). Also closed a real coverage gap: no prior test touched `messages`/`conversations`/`conversation_participants` at all. New `tests/db/messaging-security.test.ts` covers the lock, normal message round-trip, internal-note visibility (ops only), and cross-tenant isolation (an unrelated third party has zero read/write access). 405/405 tests, verified on a fresh reset plus one more run without reset. |
| `3a2ef1d` | S | Message attachments (see table row above for full description). |
| `72c2865` | T | Listing lifecycle self-approval bugs (see table row above for full description). |
| `2ac548a` | U | Multi-species audit — no fix needed (see table row above for full description). |
| `6b1745d` | V | Taxonomy audit — no fix needed (see table row above for full description). |
| `0c31009` | W | Search/discovery indexing (see table row above for full description). |
| `3902a60` | X | Application/handover state machine audit — no fix needed (see table row above for full description). |
| `b157d8a` | Y | Handover/ownership transfer audit — real gap documented, not built (see table row above for full description). |
| `34de6ed` | Z | Quotation/pricing test-coverage gap (see table row above for full description). |
| `f3e636e` | AA | Fundraising outcome-status self-declaration gap (see table row above for full description). |
| `74b4655` | AB | Fundraising compliance gating audit — no fix needed (see table row above for full description). |
| `fe17e95` | AC | Outbox/email delivery reliability audit — nothing to harden, documented (see table row above for full description). |
| `8b89243` | AD | Atomic transport status changes, server-stamped actor (see table row above for full description). |
| `7c18d3e` | AE | Audit-log actor lock, atomic route assignment (see table row above for full description). |
| `ff74ea0` | AF | Feature flags/market configuration audit — no fix needed (see table row above for full description). |
| `3552611` | AG | Concurrency races closed: double-sell reservations, duplicate conversations (see table row above). |
| `bfa0ec6` | AH | User data export audit + extension (see table row above for full description). |
| `0711f35` | AI | Real account deletion execution (see table row above for full description). |
| `0f72b7f` | AJ | Support/diagnostic tooling audit — deferred to Stage BL (see table row above). |
| `ac3f54c` | AK | Backup/DR documentation (see table row above for full description). |
| `fe6e329` | AL | Migration quality re-audit (see table row above for full description). |
| `bcccb0b` | AM | Test factory for deterministic transport-request fixtures (see table row above). |
| `7ed2ff6` | AN | Bounded load/volume readiness (see table row above for full description). |
| `8d2ff9c` | AO | Document review/substitution lock (see table row above for full description). |
| `dfe6aeb` | AP | Release-candidate verification + report. Re-ran the full verification chain fresh for this report (not carried over from earlier stages' output): 98 migrations, no duplicate prefixes, fresh `db reset` clean, 490/490 tests on two consecutive clean runs, `tsc` clean, `build` clean, route-tree consistent, full-repo lint at the same 38-error/13-warning baseline first documented at Stage K (no new regressions across the whole R–AP range). New `docs/BACKEND_RELEASE_CANDIDATE_REPORT.md` closes out the first supplemental queue with a summary of all 18 stages (10 real fixes, 6 confirmed-correct audits, 2 correctly-deferred unbuilt features) and the known-open-items list carried into the second supplemental queue. Test suite grew 395 → 490 (+95) across R–AP. This closes the first supplemental queue (Stages R–AP); continuing directly into the second supplemental queue (Stage BA onward) per standing instruction. | Security red-team pass. Found a real, serious gap: `transport_documents`' requester policy is `FOR ALL` with zero column restriction beyond row ownership — a requester could self-set `status='accepted'` and forge `reviewed_by`/`reviewed_at` (faking an ops review that never happened), and more seriously, **substitute the underlying file after a real ops approval** — swap `file_url` to point at a different Storage object while `status`/`reviewed_by`/`reviewed_at` still showed the original ops approval, making a never-reviewed replacement document (passport scan, health certificate, transport authorisation) appear ops-approved. Fixed with `20260101009600_transport_document_review_lock.sql`: a requester keeps the legitimate ability to upload new documents and replace a not-yet-accepted one, but can never set/forge review fields, and an accepted document is completely locked from requester-side changes. Also closed a smaller, related gap found while writing the trigger: `reviewDocument()` (ops-side) trusted a client-supplied `reviewedBy` — one ops account could credit a different ops profile as the reviewer; now always server-stamped. New `tests/db/document-review-integrity.test.ts` proves both, plus the legitimate not-yet-accepted replacement path stays allowed. Hit one transient, unrelated failure while verifying repeatability — a `transport_draft_creation` rate-limit exhaustion from the sheer cumulative volume of `test:db` runs across this unusually long session (Stage J's thresholds are calibrated for a normal session, not 50+ consecutive non-reset runs) — confirmed not a real regression via a fresh reset plus two more clean runs. 490/490 tests. | Bounded load/volume readiness. Audited every ops/admin/moderation list query (`listOpsTransportRequests()`, `listReports()`, `listModerationCases()`, etc.) for the "returns the whole table as data grows" risk — none call `.limit()` explicitly, but confirmed they're not actually unbounded: `supabase/config.toml`'s `api.max_rows = 1000` caps every PostgREST response platform-wide regardless of app code, and every one of these queries is already ordered `created_at desc`, so the cap degrades gracefully. Documented in `docs/PRODUCTION_SETUP.md` §3b, including the real, actionable production gap this surfaces: `max_rows` is a local `config.toml` setting that does **not** automatically carry over to a hosted production project — must be independently verified/configured via the Supabase dashboard before launch. No code change; a genuine "already protected, but by platform config that needs re-verifying in production" finding, not a code gap. | Test factories/deterministic fixtures. Found 5 test files (`workflows`, `security-regressions`, `transport-lifecycle-scenarios`, `concurrency-hardening`, `data-export`) had converged on the same ~10-line "insert a fresh Poland→Germany transport_requests row" block, each with its own slightly different (and in one case `Date.now()`-only, theoretically collision-prone in a fast run) `request_number` scheme. Added `createTestTransportRequest()` to `tests/db/helpers.ts` — deterministic, collision-free `request_number` (timestamp + random suffix), throws on failure like `as()` already does rather than returning a `{data,error}` tuple. Refactored the two files added this session (`concurrency-hardening.test.ts`, `data-export.test.ts`) to use it, proving the factory in real use; deliberately left the three older, already-established files untouched rather than churning already-correct, already-reviewed tests for a stylistic-only gain. 478/478 tests, verified on a fresh reset plus one more run without reset. | Migration quality/upgrade path. Re-audited all 96 migration files for the three concrete unsafe patterns that actually matter for a future production push: `add column ... not null` without a `default` (zero found — every one pairs with a default, the safe pattern), `alter type ... add value` used elsewhere in the same transaction (zero found — Postgres would reject this outright, confirmed both instances are purely additive), and genuinely destructive drops (zero — every `drop policy`/`drop trigger`/`drop function` is immediately followed by a `create` in the same file, never a data-destroying drop). Also documented a real, previously-undisclosed limitation: `CREATE INDEX CONCURRENTLY` cannot be used in this repo's transactional migration flow, so the plain `CREATE INDEX` statements added in Stages N/W would lock their target table for the duration against a production database with real rows (not a concern for the current empty-database day-one push, but a real future constraint). Updated `docs/PRODUCTION_SETUP.md` §3 with all of the above and corrected its stale migration count (54 → 96). No code change. | Backup/disaster-recovery documentation. New `docs/BACKUP_AND_DISASTER_RECOVERY.md`: documents the two independent recovery paths (schema replay from migrations vs. real data restore, which migrations can never provide), the local recovery commands actually verified this session (`db reset`, `db dump`, `db query`), the concrete checklist for whoever picks a production Supabase plan tier (PITR vs. daily-only, RPO/RTO, Storage-bucket coverage — none decided yet, correctly not guessed at), and the fix-forward incident-response convention for a bad migration. Explicitly does not claim backups are active (no production project exists) or invent an RPO/RTO/retention number. No code change — a documentation-only stage matching its own name. | Support/diagnostic tooling. Confirmed no dedicated support-ticket/case system exists anywhere (`support_case`/`support_ticket`/`diagnostic` all grep to zero) — `reports`/`moderation_cases` (built in earlier sessions) already serve as the real, working "user flags a problem, staff triages it" mechanism today, and a formal, separate support-case system with staff-workload/access-justification semantics is explicitly its own dedicated later stage (Stage BL, "support case management," in the second supplemental queue). Building a second, competing concept now would duplicate that stage rather than close a reachable gap. No code change; deferred to BL by design, not oversight. | Deletion/anonymisation execution. Closed the real, repeatedly-flagged gap first documented in Stage O: `markDeletionRequestProcessed("processed")` used to only flip the request's own status flag, never actually touching the account. New `execute_account_deletion()` admin-only RPC (`20260101009500_account_deletion_execution.sql`) does real anonymisation (never a hard delete — Stage L's audit already established audit-trail FK columns have no `ON DELETE` action, so a hard delete would fail outright for any account with history): clears `display_name`/`first_name`/`last_name`/`email`/`phone`/`avatar_url`/`city`/`country`, sets new `profiles.is_deleted`/`deleted_at` columns, and refuses while a real unresolved obligation exists (active transport request, active reservation, active application, or un-transferred organisation ownership) — every check a real, reachable table this schema already has, nothing speculative. `dashboard.admin.users.tsx`'s stale "still needs Supabase Studio" copy corrected to describe what the button now actually does. New `tests/db/account-deletion-execution.test.ts`: the successful-anonymisation path is tested against a throwaway signed-up account (`anon().auth.signUp()`, email confirmation disabled locally), never against any of the ~10 shared seeded personas this whole suite reuses — anonymising a shared persona would corrupt it for every later test file in the same run, the same lesson as Stage J's rate-limit exhaustion bug. Blocker tests use existing personas safely since they only prove rejection. 478/478 tests, verified on a fresh reset plus one more run without reset. | User data export. Audited `exportMyData()` (`src/lib/queries/privacy.ts`) against the exclusion requirements (must never include reporter identity, internal moderation notes, another user's private data, unrelated org data) — confirmed clean by construction: every query is filtered by the caller's own id column, and reports/moderation_cases/internal_notes/organisation-owned tables are never selected at all. Added two real, missing-but-safe completeness categories (sent messages excluding internal notes, received notifications) since they're unambiguously the user's own data and trivially RLS-safe. New `tests/db/data-export.test.ts` proves the export's own query shape returns real data and that a forged `userId` argument (simulating a client bug) still can't pull another user's data. While writing the cross-tenant test, hit a false positive from accumulated test-fixture pollution (an earlier stage's persona had legitimately accumulated dozens of "named recipient" `transport_parties` rows across many non-reset `test:db` runs this session) — not a real bug, confirmed by direct inspection of `transport_parties` and the `is_named_transport_party()` policy; fixed by isolating the test to a freshly-created fixture instead of relying on global persona state. 469/469 tests, verified on a fresh reset plus one more run without reset. | Idempotency/concurrency audit. Walked through the concrete concurrent-actor scenarios named for this stage and found three real races, closed in `20260101009400_concurrency_hardening.sql`: (1) `reservations` had no uniqueness on `animal_id` — two different *approved* applications for the same animal (an org can approve more than one buyer's application for a popular puppy; nothing prevented that) could both become active reservations, a real double-sell race. Closed with a partial unique index (`where status <> 'cancelled'`). (2) `start_transport_conversation()` did a classic check-then-insert with no protection — two concurrent callers (e.g. the requester and ops both opening the thread for the first time) could each create a duplicate conversation for the same transport request. Closed with a partial unique index (one conversation per transport request). (3) `start_application_conversation()` has the same check-then-insert shape, but its natural key is (animal, buyer) — not expressible as a plain unique index since multiple buyers legitimately each get their own conversation about the same animal. Closed with a transaction-scoped `pg_advisory_xact_lock` keyed on the (animal, buyer) pair. New `tests/db/concurrency-hardening.test.ts` fires genuinely parallel requests via `Promise.all` (not sequential calls) and proves each race resolves safely: exactly one reservation wins, both conversation calls converge on the same id. 461/461 tests, verified on a fresh reset plus two more runs without reset (a third run for this concurrency-touching stage, per the standing testing discipline). | Feature flags/market configuration. Confirmed `FUNDRAISING_ENABLED` (`src/lib/fundraising-flag.ts`) is the only real feature flag in the codebase (grepped every `import.meta.env.VITE_*` usage) and it was already fully audited in Stage AB. Audited `markets` (RLS trivially correct — single admin writer, same shape as taxonomy) and confirmed its per-market state columns (`marketplace_state`, `adoption_state`, `transport_*_state`, `fundraising_state`, `enabled`) are read *only* by `src/lib/queries/markets.ts` (the admin settings display page) — nothing in the real marketplace/transport/adoption flow checks a market's state before allowing an action, not even `enabled`. Purely a forward-looking admin roadmap tracker today, not a security boundary, the same "schema exists, no real enforcement built yet" shape as Stages U/V. Building enforcement now (blocking actions from an under-ready market) would be inventing new product behaviour with no existing reachable trigger for it. No code change. | Audit-log quality. Found the general-case gap Stage AD's fix was one instance of: "ops staff and admins write audit logs" checked `is_ops_staff()` but never restricted `actor_profile_id` — any ops/admin account could credit a *different* profile as an audit entry's actor, project-wide. Locked it at the RLS layer (`actor_profile_id = auth.uid()`) in `20260101009300_audit_logs_actor_lock_and_route_assignment_rpc.sql`; verified every existing SQL-side `audit_logs` insert already used `auth.uid()` correctly (440/440 pre-existing tests still passed unchanged after the lock). Same migration also fixed `assignRequestToRoute()` (`src/lib/queries/routes.ts`): two non-atomic client-side writes (insert `route_assignments`, update `transport_requests.assigned_route_id`) with the second one's error never checked — a failure there silently left the two tables inconsistent — plus a client-supplied `assigned_by`. Folded into a new `assign_request_to_route()` SECURITY DEFINER RPC, matching Stage AD's pattern; both call sites (`dashboard.operations.matching.tsx`, `dashboard.operations.routes.$id.tsx`) updated. New `tests/db/audit-log-and-route-assignment-integrity.test.ts`. 450/450 tests, verified on a fresh reset plus one more run without reset. | Transactional domain events. Found `changeOpsRequestStatus()` (`src/lib/queries/operations.ts`) did 3 separate client-side network round-trips (update `transport_requests`, insert `transport_status_history`, insert `audit_logs`, the last one's error not even checked) — not atomic, and both the history and audit rows trusted a client-supplied `actorId` instead of the real caller, the same forgeable-actor shape already closed for `notifications.actor_profile_id` in an earlier session. Fixed with a new `change_ops_request_status()` SECURITY DEFINER RPC (`20260101009200_change_ops_request_status_rpc.sql`) doing all three writes in one Postgres transaction with `auth.uid()` always the real actor; `changeOpsRequestStatus()` and its one real call site (`dashboard.operations.requests.$id.tsx`) updated to match, dropping the now-removed `actorId` param. New `tests/db/change-ops-request-status.test.ts`: atomic write proven end-to-end, non-ops and driver callers rejected, actor is always the real caller, a nonexistent request id errors rather than silently no-oping. 440/440 tests, verified on a fresh reset plus one more run without reset. | Outbox/email delivery reliability. Confirmed there is no outbound email delivery mechanism anywhere in this codebase to harden — no email provider SDK in `package.json` (checked for resend/nodemailer/sendgrid), and `email` only ever appears as a stored data field (`organisation_invitations.invited_email`, `profiles.email`, `transport_parties.external_email`), never actually sent. The organisation invitation flow (Stage E) works entirely via an in-app token link (`_public.invitations.$token.tsx`), not email. Building a real outbox/retry/queue system now would mean inventing a provider integration from scratch, which needs real credentials this session is explicitly barred from using ("do not use production credentials") — there is nothing reachable to harden, only a larger, genuinely-unbuilt feature to defer, the same shape as Stage Y's ownership-transfer finding. Documented rather than built. No code change. | Fundraising compliance gating. Audited every route that touches fundraising for the `FUNDRAISING_ENABLED` flag (`src/lib/fundraising-flag.ts`): every real content route (`dashboard.foundation.fundraising.{index,$id}.tsx`, `_public.fundraising.{index,$id}.tsx`, `dashboard.admin.fundraising.tsx`) checks it and shows an honest "not yet available" state; the two routes with no check (`dashboard.foundation.fundraising.tsx`, `_public.fundraising.tsx`) are pure layout/`<Outlet />` wrappers with no content of their own, correctly not needing one. Confirmed the real compliance boundary — "never claim real money moved" — is enforced at the RLS layer independent of the frontend flag: `fundraising_contributions`' INSERT policy forces `is_simulated = true` regardless of what a raw API call requests, so even a complete frontend-flag bypass could never insert a real (non-simulated) payment record. Also checked `markets.fundraising_state`: purely an admin-facing readiness display badge (`dashboard.admin.settings.tsx`), not consumed by any gating logic — correctly informational-only, not a security boundary. No code change; the one real gap in this feature area was already found and fixed in Stage AA. | Disabled payment-provider abstraction. Audited fundraising (the closest thing this schema has to a payment-adjacent feature, correctly disabled behind `src/lib/fundraising-flag.ts` pending a real provider) and found a real integrity gap: an org could set its own campaign's status to `target_reached`/`partially_funded` — but per `docs/FUNDRAISING_POLICY.md`, these are meant to be *outcomes* of real fundraising activity ("active → (target_reached \| partially_funded \| expired)"), not org-declarable states, and both labels are shown publicly with no correlation to the campaign's real computed total. An organisation could publicly claim "Target reached ✓" on a campaign that collected nothing. Fixed with `20260101009100_fundraising_outcome_status_lock.sql`, moving both to the same admin-only side of the line the policy already drew for approved/suspended/refund_review/completed. New test in `tests/db/fundraising.test.ts` proves the org is blocked from both while admin retains access. 435/435 tests, verified on a fresh reset plus one more run without reset. | Quotation/pricing hardening. Audited `pricing_rules` and quotation creation and confirmed the design already correct: `pricing_rules` is ops-only-write, and the client-side estimate calculator (`src/lib/queries/pricing.ts`) only ever reads active rules to compute a display-only range — never trusted as the charged amount, which ops always sets manually and directly on `quotations` (already column-locked in Stage L). What was genuinely missing: zero test coverage existed for either boundary. New `tests/db/pricing-and-quotation-security.test.ts`: active pricing rules are public, inactive ones are hidden from anon/ordinary users, only ops can write them; and — a gap Stage L's field-lock test didn't cover — a customer cannot INSERT a new quotation at all (not just restricted on update). No code change, closing a real coverage gap rather than manufacturing a fix for an already-correct design. 429/429 tests, verified on a fresh reset plus one more run without reset. | Handover/ownership transfer. Audited `animal_ownership_history` (`20260101000900_animals.sql`): has RLS (owner/org/admin SELECT, admin-only write) but zero consumers anywhere in `src/` — confirmed by grep, same as `handover_protocols` before it. Went one level deeper than the Stage S precedent before concluding: checked whether any real code path ever actually changes `animals.owner_profile_id`/`organization_id` after creation (reservation completion, adoption finalisation, rehoming completion) — none does. Unlike Stage S (attachments), where the underlying action being extended — sending a message — already existed and just needed a file parameter, here the underlying action itself (transferring an animal's real ownership record on a sale/adoption/rehoming) was never built at all: no RPC, no "mark as transferred" UI action, nothing. Building only a history-recording trigger on top of that would watch for a column change that no code path ever produces — permanently dead code, not a fix. Correctly deferred as a real, documented product gap (a missing business action, not a bug in existing reachable code) rather than invented as new unrequested feature scope. No code change. | Application/handover state machines. Audited both halves. Application side: confirmed org-side `buyer_applications.status` transitions are intentionally unrestricted (the org is the trusted decision-maker for its own applications by design) and safe — verified `reservations`' INSERT policy already requires `buyer_applications.status = 'approved'` **and** `owns_org()` before a reservation can be created, so the org can't be tricked by anything client-controlled; the buyer side was already correctly locked in Stage F/T. Handover side: `handover_protocols`/`compliance_reviews` are single-writer (ops-only, `for all`), structurally ruling out the self-approval bug class the same way Stage V's taxonomy tables did. The one real, related concern — no state-machine enforcement on which `transport_requests.status` values an assigned driver can set (found Stage M) — remains correctly deferred to its own dedicated later stage ("route/stop state machines" / "state-machine/chaos tests" in the third supplemental queue), not attempted here under a differently-scoped stage name; a partial/rushed version now risks exactly the "blocked the whole workflow" failure this session already hit once before with a first-draft lock trigger. No code change. | Search/discovery. Audited the marketplace browse query path in `src/lib/queries/marketplace.ts` for the same missing-index class Stage N found: `is_published` (animals, litters) and `is_public`/`org_type`/`verification_status` (organisations) gate nearly every public marketplace query and none were indexed — not a guess, the single most-executed query pattern in the whole public marketplace, same justification tier as Stage N's indexes. Added 3 partial indexes (`20260101009000_public_listing_query_indexes.sql`). Also found and deliberately did **not** fix (documented instead): `listPublishedPuppies()`'s `filters?: { breed, country }` parameter fetches the full result set and filters in JS afterwards instead of pushing into the query, and its only real caller (`_public.find-a-dog.tsx`, frontend-owned, outside this session's scope) never actually passes those filters — 100% client-side search over an unpaginated full fetch. Not fixed here: the caller doesn't use the filters today (wiring them server-side would be dead code with no observable effect), and adding a `LIMIT` without corresponding pagination UI would silently drop listings with no way for a user to see the rest — that UI work belongs to whoever owns marketplace UX next. No new tests needed (index-only change, doesn't affect query results). 422/422 tests, verified on a fresh reset plus one more run without reset. | Taxonomy. Audited `breeds` (`20260101000600_breeds.sql`) and `product_service_categories` (`20260101005700_product_service_categories.sql`, itself deliberately schema-only/disabled per its own header, matching the multi-species pattern from Stage U) — the full extent of "taxonomy" in this schema; no separate tags/labels table exists. Both are single-writer (admin-only INSERT/UPDATE/DELETE, no other role has any write policy at all), which structurally rules out the self-approval/column-lock bug class this session has repeatedly found elsewhere — there is no second, less-trusted writer whose column access could need restricting. No code change; a correct, verified "nothing to fix" outcome. | Multi-species. Audited the existing `species` table/`species_id` columns (`20260101005300_species.sql`, built in a prior session, predating this session's Stage B baseline) rather than assuming a gap existed just because the stage name suggested one. Confirmed: the foundation is deliberately schema-only ("Species-aware forms and per-species field/document/eligibility rules are explicitly NOT built in this pass" — the migration's own words), and `grep -rl "species_id\|speciesId" src/` returns zero matches — no query or route anywhere sets or reads it. Considered two theoretical gaps (a listing could reference a disabled/not-yet-activated species; an animal's `breed_id` and `species_id` could point to different species) and rejected fixing either: neither is reachable through any real UI (nothing sets `species_id` at all), neither has any real consequence today (no query filters or displays by species, and no non-dog breed rows exist in the schema at all yet, making the breed/species mismatch currently impossible with real data), and CLAUDE.md's engineering rules explicitly say not to add validation for scenarios that can't happen or design for hypothetical future requirements. No code change — the correct, honest outcome of this stage's audit, matching Stage O's precedent for "audited, found already appropriately scoped." | Listing lifecycle. Found and closed two real self-approval bugs, the same bug class as the Stage L quotation fix (an INSERT/UPDATE policy checks row ownership but not the initial value of a status/decision column): (1) `rehoming_reviews`' INSERT policy never restricted `admin_status` — an owner could insert their own review already `admin_status='approved'`, and since `animals.is_published` is fully owner-controlled, this made a private-rehoming listing publicly visible without ever going through real admin review, a direct violation of CLAUDE.md rule #5 ("Private rehoming requires a separate moderated workflow"). Fixed with `20260101008800_rehoming_review_self_approval_lock.sql`. (2) `buyer_applications`' write-lock trigger (`prevent_buyer_writes_to_org_controlled_fields`, Stage F) only ever fired on UPDATE — a buyer's INSERT could set `status='approved'` directly, bypassing organisation review entirely. Fixed with `20260101008900_buyer_application_insert_status_lock.sql`, extending the same trigger to INSERT (same fix shape as Stage G's `set_moderation_case_appeal_deadline`). Neither reachable through the real UI, both real via a raw API call. Found and fixed a real regression while doing this: `tests/db/fundraising.test.ts`'s `buildEligibleFixture()` and one other fixture used the exact same self-approval shortcut to set up test data — updated both to submit as the buyer then approve as admin, a realistic path. New `tests/db/listing-lifecycle-security.test.ts`. 422/422 tests, verified on a fresh reset plus one more run without reset. | Attachments. `messages.attachment_url` existed on the schema since the original messaging migration but was completely unwired — no Storage bucket, no RLS, no upload UI (confirmed by grep: zero references anywhere in `src/` before this stage). Added the missing backend layer following the established private-bucket-plus-signed-URL pattern (`transport-documents`/`welfare-case-documents`): new `message-attachments` bucket, RLS scoped via the existing `is_conversation_participant()` helper so an attachment is exactly as protected as its conversation (`20260101008700_message_attachments_storage.sql`). Wired a real, minimal UI in the shared `src/components/chat-thread.tsx` (attach-a-file button, upload on send, signed-URL-on-demand rendering — never a bare link) since it was genuinely unbuilt, not just unwired at the DB layer; `chat-thread.tsx` is a shared component outside the frontend-worktree's excluded-file list. `tests/db/message-attachments.test.ts`: upload + signed URL for the uploader and the other participant, ops access, and two negative cases (an unrelated user can neither read nor upload into someone else's conversation folder). 413/413 tests, verified on a fresh reset plus one more run without reset. |

**First supplemental queue**: R (messaging/conversation security), S (attachments), T (listing
lifecycle), U (multi-species), V (taxonomy), W (search/discovery), X (application/handover state
machines), Y (handover/ownership transfer), Z (quotation/pricing), AA (payment abstraction,
disabled), AB (fundraising gating), AC (outbox/email jobs), AD (domain events), AE (audit quality),
AF (feature flags), AG (idempotency/concurrency audit), AH (data export), AI (deletion/
anonymisation execution), AJ (support tooling), AK (backup/DR docs), AL (migration quality), AM
(test factories), AN (load readiness), AO (security red-team pass), AP (release-candidate report).

**Second supplemental queue**: BA–CI (see above) plus a final post-CI adversarial review pass.

## Known open items carried forward

- `driver_transport_job_view` and the new timeline queries don't yet expose the multi-animal list
  (`transport_request_animals`) — a driver/timeline viewer still only ever sees the primary/first
  animal snapshot on multi-animal requests. Documented as a known non-goal in
  `docs/adr/TRANSPORT_DATA_MODEL.md`; candidate for a future pass if multi-animal requests become
  common in practice.
- Quotation RLS column-scoping gap: **fixed in Stage L** (see table above). No longer open.
- Driver status state-machine enforcement: **fixed in Stage CC** (see table above). No longer open.
- ~127 foreign-key columns across the schema have no covering index (found during Stage N's audit;
  see the table row above). Deliberately not indexed now — the local seed dataset is too small for
  EXPLAIN to distinguish a real need from a guess, and indexing all of them would be exactly the
  "blind indexing" this stage was told to avoid. Revisit once real usage data (pg_stat_statements
  or equivalent) can identify which are actually hot; the full list is in
  `supabase/migrations/20260101008500_marketplace_listing_indexes.sql`'s reasoning comment and can
  be regenerated via `supabase db dump --local --schema public` + a diff against `pg_indexes`.
- Marketplace search (`/find-a-dog`) does 100% client-side filtering over an unpaginated full
  fetch of every published puppy (`listPublishedPuppies()`'s `breed`/`country` filter parameters
  are never actually passed by its only caller, and there's no `LIMIT`/pagination at all). Found
  Stage W; not fixed because the route file is frontend-owned (outside this session's scope) and
  because adding a `LIMIT` without pagination UI would silently drop listings. Candidate for
  whoever next works on marketplace UX/scale.
- `animal_ownership_history` has RLS but no consumer, and — more fundamentally — no code path
  anywhere ever actually changes `animals.owner_profile_id`/`organization_id` after creation, so
  there is no real "ownership transfer" business action to record yet. Found Stage Y; a real
  product gap (a missing feature: what should happen to an animal's ownership record when a sale/
  adoption/rehoming actually completes), not a bug in existing reachable code. Candidate for a
  future phase once that business action itself is designed and built.
- No outbound email delivery mechanism exists anywhere (no provider SDK, `email` fields are only
  ever stored, never sent — the org-invitation flow works entirely via an in-app token link).
  Found Stage AC; needs a real provider integration with real credentials this session cannot
  configure, not something to build/fake here. Candidate for a future phase once a provider is
  approved.
- Error-message translation (Stage BQ, new `src/lib/errors.ts`) is wired into only the two real
  call sites in `_public.transport.request.tsx` — the one customer-facing file explicitly carved
  out of the frontend-file boundary for transport work. 69+ other `toast.error(err.message)` call
  sites across frontend-owned dashboard/marketplace routes still show raw, untranslated Postgres
  errors. Left alone deliberately rather than a broad unrequested frontend sweep; the shared
  utility now exists for whoever does that pass next.
- `src/lib/supabase/types.ts` (the hand-maintained stub, confirmed by its own header comment) has
  no `Enums` section at all — every Postgres enum-typed RPC parameter/column (e.g.
  `advance_transport_job_status`/`change_ops_request_status`'s `p_new_status`, really
  `Database["public"]["Enums"]["transport_status"]` in the real generated schema) is currently
  typed as a plain `string` in the stub, losing compile-time protection against an invalid status
  value (Postgres itself still rejects one at runtime, so this is a lost safety net, not a live
  bug). Confirmed via a real `supabase gen types typescript --local` diff during Stage BR: 6044
  generated lines vs. 1722 hand-written, every RPC name still accurate, only the type fidelity has
  drifted. Full reconciliation is IR-5's dedicated scope — not attempted piecemeal here.

## Files likely to conflict with `ux-marketplace-frontend-pass`

`_public.transport.request.tsx` (transport-specific, explicitly carved out of the frontend
session's excluded-file list — touched again in Stage BQ for error-message translation),
`dashboard.driver.index.tsx`/`src/lib/queries/driver.ts` (Stage BG, evidence upload wiring), and
`dashboard.breeder.{reservations,transport,settings,index}.tsx`/`dashboard.foundation.
{settings,index}.tsx`/`src/lib/queries/profile.ts` (Stage CE, data-access consolidation — swapped
a duplicated inline query for an existing shared function, no behavioural change) are the only
frontend-adjacent files touched this session; none are marketplace UX, so low real conflict risk
with `ux-marketplace-frontend-pass`'s stated scope (`src/components/cards.tsx`, `site-chrome.tsx`,
`src/routes/_public.*` marketplace pages, `dashboard.buyer.*`, `src/lib/i18n/**`,
`docs/MARKETPLACE_UX_AUDIT.md`). No other frontend-owned file has been touched.

## How to resume if this session stops mid-stage

Check the table above for the last committed row, then re-read this file's "Remaining stages"
section and continue from the next uncompleted stage. Always re-run the Stage A baseline checks
(`git status`, `db reset`, `test:db` ×2, `tsc`, `build`) before resuming feature work.
