# Bot 1 — Due-Diligence Review (Finalisation Pass)

One row per acquisition-relevant claim a buyer's technical diligence would need to check. This is a
narrower, more targeted pass than a full standalone due-diligence audit — see the main report's
Limitations section for exactly which stages remain uncovered. **Updated after a resumption round**
that empirically live-tested most High findings and caught two stale claims from this same pass's
own first round (§5.1, §7.5 — both now confirmed genuinely fixed); see
`docs/BOT1_FINALISATION_AUDIT.md` §51–§57 for the full correction. Real-repo snapshot for this
version: `8201f17dd4c8abc36cc816d63c52f3620ae7e44f`.

| Claim | Verdict | Evidence |
|---|---|---|
| "70/70 public tables have RLS enabled" | Credible | Live-confirmed across multiple checks this pass, no counter-evidence found. |
| "All `SECURITY DEFINER` functions pin `search_path`" | Credible | Every function touched by the 5 new migrations found mid-pass (§54 of the main report) was read in full — all use `set search_path = public`. |
| "No duplicate migration prefixes, no destructive unsafe migration pattern" | Verified | 142 migrations at the resumption-round snapshot, no duplicates; the 5 new migrations read in full, no destructive pattern. |
| "`tsc`/build are clean" | Verified (first-round snapshot) | Both exit 0, zero errors. |
| "Extensive regression test suite (1013/1013 tests at Stage FA-3)" | **Not independently executed by this pass at either snapshot** | Bot 2's own progress log makes specific, checkable claims (exact counts per stage, incrementing consistently: 1001→1002→1006→1013) which is itself weak positive evidence of a real, non-fabricated counter (a fabricated log would be less likely to show this exact incremental pattern across 25+ stages) — but this pass did not run `npm run test:db` itself to confirm, for the shared-instance-safety reasons documented throughout. A buyer's diligence should run it directly. |
| **"An independent security audit's findings have been tracked and addressed"** | **Materially misleading as a general claim — and the specific, verifiable reason why has changed since this pass's first round** | Bot 2's own progress log (Stage FA-2) states explicitly: *"No 'Bot 1' audit/remediation process, or any report referencing one, exists anywhere in this repository or its git history... there is no finding to map."* This is not a gap in Bot 2's diligence — **it is architecturally correct**: all three Bot 1 audit passes (including this one) work from isolated clones outside `/p/the-puppy-passport` and have never committed a finding into the real repository Bot 2 actually works in. Bot 2 cannot "address Bot 1's findings" because Bot 2 has no visibility into them at all. Every fix that happens to close a Bot 1 finding (§5.1, §7.5, half of §6.8 this pass) is Bot 2 **independently rediscovering the same bug through its own unrelated adversarial sweeps**, not remediation of a known, tracked issue. A buyer evaluating "was the independent audit's feedback loop closed" should understand there currently is no feedback loop between these two processes at all — a real, structural finding, not a code bug. |
| "3 High-severity findings from the original independent audit are confirmed still open" (corrected from "4") | **3 confirmed still open, live-empirically exploited this pass**, 1 (§5.1) confirmed genuinely fixed this pass, 1 regression (NEW-H1) also confirmed still open, live-empirically | §5.2/§5.3/§5.4/NEW-H1 all actually exploited against `HEAD` `8201f17` and succeeded, then reverted. §5.1 actually attempted and rejected by a real trigger — genuinely fixed. A buyer relying on either "nothing was fixed" or "everything was fixed" would be equally wrong; the accurate claim is a specific, itemized 3-open/1-open-regression/1-fixed breakdown, not a round number. |
| "This session's own security-hardening pattern (revoke the raw grant on RPC-fronted tables) is proven and reusable" | Confirmed, independently exercised | Multiple live-verified instances now (`animal_ownership_history`, `get_notification_preference()`, `has_role()`); this pass's own candidate fix (`7ba7b32`) reuses the identical pattern for `legal_holds`, unchanged and still valid against `HEAD` `8201f17`. |
| "Frontend integration is well-understood, only 3 real conflicts, all previously mapped" | Confirmed still current | Frontend ref hash unchanged (`727d551`) across this pass's entire duration, including the resumption round; **caveat**: Bot 2's own Stage YR-23 independently re-ran the same `git merge-tree` check and found the real conflict count is now **6, not 3** — 3 new conflicts self-inflicted by Bot 2's own Stage YR-16 error-message wiring touching 3 files the frontend branch also touches. This pass did not independently re-verify Bot 2's YR-23 claim (time-budgeted against higher-priority live exploit testing) but flags it as the more current number a buyer should ask about, not this report's own stale "3." |
| "Local Supabase/shared test infrastructure is stable enough for reliable due diligence" | **Caveat, not pass/fail — and this pass's resumption round adds a positive counter-data-point, not just more caution** | The instance was confirmed mid-reset at the very start of the resumption round, then genuinely idle and stable for a real, extended window of authenticated-actor testing shortly after — meaning the instability is real but not constant; a diligence team hitting a bad window should simply retry shortly after, not conclude the infrastructure is unusable. |
| "The account-deletion/legal-hold subsystem is production-grade" | **Overstated, live-empirically confirmed this pass, not just policy-text-inferred** | Both the admin-actor and ordinary-user-actor raw bypasses were actually executed against the live schema this pass and both succeeded; `require_recent_auth()`'s step-up gate was actually bypassed empirically, not just shown to be theoretically bypassable. |
| **NEW — "Bot 2's own newest self-audits demonstrate a repeatable, structural review-process gap, not just two isolated missed bugs"** | **New finding this pass, confirmed across two independent instances** | Stage YR-1 (`docs/NOTIFICATION_PRODUCER_INVENTORY.md`) reasons only from documented TS call sites, missing the raw-RPC surface (NEW-M1). Stage YR-15 (`docs/RAW_API_BYPASS_AUDIT.md`) has the *exact right methodology* for finding bugs like §5.2/§6.3/§6.4 but scopes its sweep to "this stage's own new migrations" only, never revisiting older tables (NEW-H2). Both are Bot 2's own newest, most careful, best-evidenced audit documents — the gap is not carelessness, it's a scoping default ("audit what I just changed") that a buyer's technical diligence should specifically ask Bot 2 to widen to a full-schema sweep, since Bot 2's own tooling and judgment for this exact bug class is already proven correct twice over. |
| **NEW — "The self-approval bug class Bot 2 has now found and fixed twice (`organisations.verification_status`, `fundraising_campaigns.status`) has a third live instance neither Bot 2 nor either prior Bot 1 pass had found"** | **New finding this pass, via a genuinely undirected fuzz sweep, not a directed re-check of a known finding** | `achievements.verification_status` — a kennel's own self-declared award/certification claim — can be raw-set to `'approved'` (publicly visible, no admin review) by the owning kennel, the identical bug shape closed twice elsewhere this session. This directly strengthens rather than merely repeats the "narrow self-audit scope" finding above: it shows the *undiscovered* surface for this exact, well-understood bug class is not empty even after two rounds of the team finding and fixing it themselves — a buyer's diligence should treat "we've already found and fixed this bug class" claims as evidence of good process, not evidence of completeness. **Candidate fix available** (`3f4db66`, same proven pattern), not yet applied. |
| **NEW — "The core privilege and tenant-isolation model is solid against undirected adversarial testing, not just the specific findings this report already knew to check"** | **Confirmed, positive evidence, 14 of 15 probes** | A 15-probe undirected fuzz sweep (privilege escalation via `user_roles` self-grant, anonymous reads of 5 sensitive tables, cross-tenant row visibility, organisation-membership self-promotion including cross-org) found the platform's foundational access-control layer held completely firm in every case but one (the achievements finding above). This is the first time any Bot 1 pass has produced this breadth of *positive*, evidence-backed confirmation in one place, rather than only cataloguing gaps — a real, checkable asset for a buyer's risk assessment, not just an absence of bad news. |

**Overall due-diligence verdict**: the codebase's engineering discipline (test culture, migration
hygiene, actor-attribution awareness, real fix velocity — 5 genuine fixes landed in the single delta
window this pass observed, including one of this report's own findings) is genuinely above-average
and a real asset. But a buyer's diligence should understand three specific, verifiable things this
pass adds beyond the prior two: (1) there is currently no mechanism at all connecting Bot 1's
findings to Bot 2's work — every overlap is coincidental rediscovery, not remediation of a tracked
issue; (2) where this pass empirically tested rather than only read policy text, every open finding
was confirmed genuinely, presently exploitable — not merely theoretical; (3) two separate examples
this pass found of Bot 2's own newest self-audits show it already has the right methodology for
this exact bug class, just applied too narrowly (new code only) to reach its own older findings —
the single highest-leverage, lowest-effort change available to close most of what remains open.
