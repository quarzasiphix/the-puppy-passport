# Bot 1 — Candidate Fix Ledger

Both candidate fixes live only on `candidate-fixes/bot1-legal-hold-deletion-raw-write-20260727` in
the **finalisation clone** (`/p/the-puppy-passport-bot1-finalisation-20260727-235034`), not this
clone. Never merged, never pushed, never applied to any live database. This ledger records this
pass's own staleness review against `LATEST_MAIN` = `ac612690c1741d7879d747f7e13b40fd0cb2cc04`.

| Branch/commit | Finding | Source snapshot | Files | Still applies to LATEST_MAIN? | Staleness issue found | Bot 2 recommendation |
|---|---|---|---|---|---|---|
| `7ba7b32` | §5.2 `legal_holds`/`account_deletion_requests` raw-write bypass | `26f1b2e` (finalisation pass's first-round snapshot) | `supabase/migrations/20260101013600_close_legal_hold_and_deletion_request_raw_write_bypass.sql`, `tests/db/legal-hold-and-deletion-request-raw-write-lock.test.ts` | **Content: yes.** Independently re-verified this pass: the exact `grant`/`policy` statements it targets (`legal_holds` line 40's `insert, update` grant to `authenticated`; `account_deletion_requests`' `"users manage their own deletion request"` self-service `for all` policy and `"admins manage all deletion requests"` admin `for all` policy) are still present, byte-for-byte unchanged, in the current `20260101011500_legal_holds.sql`/`20260101004800_account_deletion_requests.sql` at `LATEST_MAIN` — no later migration (including the two new ones since the candidate fix was written, `20260101014100`/`20260101014200`/`20260101014300`) touches either table's grants or these two policies. | **Filename/prefix collision, real, confirmed this pass**: the candidate migration's own filename is `20260101013600_close_legal_hold_and_deletion_request_raw_write_bypass.sql` — but Bot 2's own real migration set on `main` now includes `20260101013600_admin_command_audit_coverage.sql` (Stage YR-7, landed on `main` *after* this candidate branch was created from an older snapshot). Confirmed via `ls supabase/migrations \| grep ^20260101013600` on the real repo: exactly one file, the real one — the candidate's own copy was never actually placed in the real repo's directory, but if Bot 2 tried to apply this candidate fix's file as-is into `supabase/migrations/`, it would collide on the numeric-prefix key `supabase db reset` actually tracks (the same failure mode `.github/workflows/ci.yml`'s own "Check for duplicate migration version prefixes" step exists to catch) — two files, one prefix. | **Do not apply this file verbatim.** The SQL body itself is still correct and applies cleanly against `LATEST_MAIN`'s actual schema (confirmed via independent static re-read, not just the original author's own claim) — Bot 2 should either (a) rename the file to a fresh, currently-unused prefix (anything after `20260101014300`) before applying, or (b) reimplement the same two changes (the `revoke`+two `drop policy`/`create policy` statements) directly in a new, correctly-numbered migration. Either way, the underlying fix design is sound and worth using — only the filename needs to change. |
| `3f4db66` | NEW-H3 `achievements.verification_status` owner self-verification | `6dbba45` (finalisation pass's own resumption-round-2 snapshot) | `supabase/migrations/20260201000100_close_achievement_self_verification_bypass.sql`, `tests/db/achievement-self-verification-lock.test.ts` | **Content: yes.** Independently re-verified this pass: `public.achievements` (`20260101004200_achievements.sql`) is untouched by any migration after its own creation — confirmed via `grep -rl "public.achievements" supabase/migrations/*.sql` on the real repo returning only that one file. The `"owners manage their kennel's achievements" for all owns_org(kennel_id)` policy with no `verification_status` restriction, and the single `set_achievements_updated_at` trigger (no self-approval guard), are both exactly as the candidate fix's own commit message describes. | **None found.** The migration's own `20260201000100` prefix (deliberately chosen out-of-band, per its own commit message, specifically to avoid the exact collision `7ba7b32` has) does not collide with anything on `LATEST_MAIN` (highest real prefix is `20260101014300`, still in the `2026-01` range). | **Safe to apply as-is**, pending Bot 2's own review/testing — no renumbering needed. This is the cleaner of the two candidates from a mechanical-application standpoint. |

## What this ledger does not do

Neither candidate fix was applied to any database (shared or otherwise) by this pass, re-tested via
`tsc`/`test:db`, or diffed line-by-line against a hypothetical merge into `main` — "still applies"
above means "the SQL statements target objects that still exist, unchanged, in the exact shape the
migration assumes," verified by reading the actual current migration files, not by a dry-run apply.
Bot 2 should run its own `tsc --noEmit` and `test:db` pass after renumbering/reimplementing before
trusting either candidate fully.
